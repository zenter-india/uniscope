# Backend host only — do NOT include the /api/v1 path.
# The API client appends the global prefix (see src/api/client.ts).
# Local dev example: http://localhost:3000
EXPO_PUBLIC_API_URL=https://uniscope-api.onrender.com
