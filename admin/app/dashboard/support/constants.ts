/** Must match backend SUPPORT_ACCOUNT_ID (chat.service.ts) — the sentinel
 * sender id every staff reply carries, used client-side to tell staff
 * messages from user messages in a thread. */
export const SUPPORT_ACCOUNT_ID = 'uniscope-support';
