<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <!-- The INTERNET permission is required for development. Specifically,
         the Flutter tool needs it to communicate with the running application
         to allow setting breakpoints, to provide hot reload, etc.
    -->
    <uses-permission android:name="android.permission.INTERNET"/>

    <!-- Debug-only: allow cleartext (http://) traffic so the app can reach
         the local dev backend (e.g. http://10.0.2.2:3001 on the Android
         emulator). Scoped to the debug manifest so release builds keep the
         default HTTPS-only restriction. -->
    <application android:usesCleartextTraffic="true" tools:targetApi="28" />
</manifest>
