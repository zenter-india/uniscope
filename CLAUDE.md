# Uniscope — Project Context for Claude Code

Mentorship marketplace connecting prospective medical students (Aspirants) with verified mentors (current students/alumni) for chat and paid audio-call guidance, plus honest college reviews.

**Branch:** `feature/flutter-migration` (mobile was ported from React Native to Flutter mid-project; backend/admin unaffected).

## Stack

| Layer | Tech |
|---|---|
| Mobile | Flutter (`mobile_flutter/`) — Riverpod, go_router, Dio |
| Backend | NestJS + TypeScript (`backend/`) — Prisma 7 ORM |
| Database | PostgreSQL on Supabase (pooler connection; `DIRECT_URL` bypasses pooler for migrations) |
| Admin panel | Next.js (`admin/`) — single hardcoded admin login, kept as a **separately-deployed, non-public** surface (not linked from user-facing app) |
| Auth | Phone OTP (Twilio Verify, or `mock` provider for local dev) + JWT access/refresh |
| Real-time chat | Stream Chat |
| Audio calls | Agora (RTC token server built; see below) |
| Payments | Razorpay (Orders API + webhook, also a direct client-confirm fallback for local dev since webhooks need a public URL) |

Root is an npm workspace monorepo (`backend`, `admin`; `mobile_flutter` is separate/Flutter, not an npm workspace member).

## Local dev setup

- Backend: `cd backend && npm run start:dev` (or build+run `dist/main.js` directly — see below). Runs on port **3001**, prefix `/api/v1`.
- Admin: `npm run dev --workspace=admin` — **must use `next dev --webpack`**, not the Turbopack default (Turbopack can't resolve `lightningcss`'s native binding in this monorepo's hoisted `node_modules`; already fixed in `admin/package.json`).
- Mobile: `flutter run --dart-define=API_URL=http://10.0.2.2:3001/api/v1` for Android emulator (localhost aliasing), or your LAN IP for a physical device.
- All secrets live in `backend/.env` and `admin/.env.local` (both gitignored). See `backend/.env.example` for the full list — Supabase, Twilio, Redis (Upstash), Razorpay, Stream Chat, Agora, JWT secrets are all already populated in the working `.env` for this project's own accounts.
- Admin login (local dev): credentials are in `admin/.env.local` (`ADMIN_EMAIL`/`ADMIN_PASSWORD`/`ADMIN_SESSION_SECRET`) — not committed.
- **Mobile login in `mock` OTP mode**: any phone number, code is always the fixed `MOCK_OTP_FIXED_CODE` in `backend/src/auth/otp/mock-otp.provider.ts` (currently `111111`) — deliberately not random, so anyone can log in without reading server logs. Only applies when `OTP_PROVIDER_TYPE=mock`; switching to `twilio` sends and requires a real SMS code again. Don't ship this backend reachable outside localhost while in mock mode — the fixed code means anyone who can reach it can log in as any phone number.
- **A recurring dev-server gotcha:** the Supabase free-tier project auto-pauses after inactivity; if DB calls start failing, check the Supabase dashboard shows the project "Active" and retry.
- **Backend restart pattern used throughout this project's history** (Claude_Preview MCP tools were unreliable mid-session): `rm -f tsconfig.build.tsbuildinfo && npx tsc -p tsconfig.build.json && node dist/main.js` backgrounded, health-check via `curl localhost:3001/health`.

## Prisma workflow (Prisma 7 — syntax changed from earlier versions)

- No `url` in `datasource db` block — reads `DATABASE_URL`/`DIRECT_URL` from `prisma.config.ts`.
- Generate a migration from a schema diff (works against the live dev DB, non-interactive-safe):
  ```
  npx prisma migrate diff --from-config-datasource --to-schema prisma/schema.prisma --script
  ```
  Then hand-create the migration folder (`prisma/migrations/<YYYYMMDDHHMMSS>_<name>/migration.sql`, 14-digit timestamp prefix required) and `npx prisma migrate deploy`.
- For **enum value remaps** (not just additions), Prisma's diff tool can't express the data transformation — hand-write the migration using the create-new-type / `ALTER TABLE ... USING (CASE ...)` / drop-old-type / rename pattern. See `prisma/migrations/20260712125549_collapse_user_role_to_aspirant_mentor/migration.sql` for a worked example (this preserved existing user data instead of resetting).
- `prisma migrate dev` and `migrate reset` require a TTY — don't work in a non-interactive agent session. Use `migrate diff` + `migrate deploy` instead.

## Business model decisions (locked in, don't re-litigate without asking)

- **Roles:** collapsed to `ASPIRANT` / `MENTOR` / `ADMIN` (previously 4 granular roles — migrated, not reset).
- **Mentors are current medical students or alumni only — never practicing doctors.** The `UserProfile.specialty` field (free-text string, no DB-level enum) represents an *area of guidance* an aspirant would ask about (e.g. "NEET Preparation", "MBBS Academics", "Campus & Hostel Life"), not a clinical specialty like Cardiology. Mobile UI: labeled "Area of guidance" in `EditProfileScreen`'s mentor-role fields (`kGuidanceAreas` in `profile_options.dart`) and "Can help with" as the section header on `MentorDetailScreen`. Keep this distinction in mind for any future copy, seed data, or picklist touching mentor profiles — a clinical-specialty framing has been introduced and corrected once already.
- **Currency:** "Uniminutes" wallet. Internally still paise-denominated (`Wallet.balanceMinor`, `LedgerEntry.amountMinor` — architecture principle: money is always integer minor units, never floats), but 1 Uniminute = 1000 minor units = ₹10, matching the **flat mentor call payout rate** (all mentors currently earn the same ₹10/min for calls — this was a late-session decision that supersedes an earlier "per-mentor custom rate" discussion; worth double-checking with the user if extending this).
- **Recharge margin:** min ₹250 recharge credits 20 Uniminutes (₹200 of spendable value) — the 20% margin is baked into the recharge conversion only, **never** shown to the user as a line item, and **never** taken again at session-billing time (mentor gets paid the exact same amount debited from the aspirant). See `WalletService.computeTopupCredit`.
- **Uniminutes are the only student-facing unit. Rupees appear at wallet top-up and nowhere else.** 1 Uniminute = 1 minute of call time = 1000 minor units, so a balance reads directly as "minutes I can talk". Call cost is never expressed in ₹ on any aspirant surface (mentor cards, mentor profile, slot picker, in-call, session list, ledger) — only the top-up sheet shows ₹, because that's the only moment real money changes hands. Mentor-side surfaces (earnings, payouts) stay in ₹ since mentors withdraw to a real bank account. Helpers: `minorToUniminutes` / `slotUniminutes` / `uniminutesLabel` in `mobile_flutter/lib/core/network/wallet_api.dart`.
- **Never advertise that chat is free.** Chat being free is the baseline assumption, not a feature to sell — no "Free chat" chips, no "Chat · Free" buttons, no "chatting is always free" footnotes. The Chat button just says "Chat". (This reverses earlier copy that labelled chat as free in several places.)
- **Audio call billing:** fixed pre-paid slots only — 5/10/20 min (`CreateSessionDto.CALL_SLOT_MINUTES`), not live per-minute metering. Student picks a slot at booking; balance is checked and a `WalletHold` placed **at booking time**, before the mentor ever sees the request.
- **Free tier:** 2 free chat sessions + 10 free call minutes per aspirant (`UserProfile.freeChatsRemaining` / `freeCallSecondsRemaining`).
- **Call connect confirmation:** billing only fires once **both** the aspirant's and mentor's clients independently report "I joined" (`POST /sessions/:id/call/joined`) — an interim fraud-resistance measure until a real Agora server-side "user joined channel" webhook is configured (needs additional Agora Console setup the user hasn't done yet). Neither party alone can trigger a charge.
- **Call overrun:** hard cutoff at slot end, with a "continue?" popup offering another fixed +5 min block (`POST /sessions/:id/call/extend`), billed the same way as the original booking.
- **Mentor-side call drop:** still bills the aspirant in full — **never** an automatic refund. The aspirant gets a Report action; an admin manually reviews and can issue a `REFUND` ledger entry via `PATCH /reports/:id/resolve`. See `ReportsService.resolve`.
- **Support chat:** a persistent, session-independent Stream Chat channel per user with a fixed "UniScope Support" identity (`SUPPORT_ACCOUNT_ID` in `chat.service.ts`), lazily provisioned via `GET /chat/support/token`. This is what the "Need Help?" banner in the UI is wired to.

## What's built (backend modules, all tested live against the real Supabase DB + real Stream/Razorpay/Agora APIs — not just unit-style tests)

- `auth` — Phone OTP (Twilio/mock) + JWT
- `users` — profile CRUD, role transitions
- `universities` — listing/search (mobile Colleges tab wired to real data, not hardcoded placeholders)
- `mentors` — discovery/search, eligibility filtering, `{rating, reviewCount}` attached from `reviews`
- `sessions` — full booking lifecycle: request → accept/reject/cancel → (for calls) token issuance → dual-confirm connect → billing → extend → end
- `wallet` — balance, ledger, Razorpay topup (order + webhook + direct-confirm fallback), Uniminute conversion, holds
- `chat` — Stream Chat: session-scoped channels (CHAT-type sessions) + persistent support channel
- `agora` — RTC token generation (`RtcTokenBuilder.buildTokenWithUserAccount`)
- `reports` — user-submitted reports + admin resolution/manual refund
- `verification` — mentor ID verification: Supabase Storage upload, admin FIFO queue, signed document URLs, approve/reject
- `reviews` — mentor ratings (`MentorReview`, distinct from the university-scoped `Review` model)
- `notifications` — in-app notifications (durable) + best-effort Firebase push (no-ops if Firebase isn't configured — it currently isn't, see below)
- `payouts` — mentor payout requests: amount is always derived from unpaid `SESSION_CREDIT` ledger history (never mentor-chosen), ₹200 min enforced (`MIN_PAYOUT_MINOR`), admin-only PROCESSING/COMPLETED/FAILED transitions, wallet only debited on COMPLETED (`LedgerEntryType.PAYOUT`). Still no auto-disbursement — that's an intentional architecture decision, not a gap.
- `wishlist` — aspirant saved/favorited mentors (`SavedMentor`), idempotent save/unsave, hydrated mentor cards via `MentorsService.findById` (a saved mentor who's since become ineligible silently drops off the list)
- `university-reviews` — the university-scoped `Review` model (distinct from `MentorReview`), gated to `VERIFIED` users only, one review per (author, university) enforced at the DB level, anonymized to the author's pseudonym in every response. `{rating, reviewCount}` attached to `GET /universities/:slug` the same way mentor ratings attach to mentor responses.
- `university-wishlist` — same idempotent save/unsave pattern as mentor `wishlist`, for `SavedUniversity`. Mobile: heart toggle on college cards + detail screen, Saved Colleges screen at `/colleges/saved`.
- Mentors are now fully self-service end to end: `PATCH /users/me` lets a MENTOR-role user set `specialty`, `bio`, `languages`, and toggle `isMentorAvailable` themselves (guarded to MENTOR role) — no admin/DB script needed anymore to become discoverable. Combined with self-serve verification (already existed) and the verification-approval → `userProfile.universityId` link (see `VerificationService.review`), a mentor can go from signup to fully bookable using only the app. Mobile: "Available for mentoring" switch on the Profile screen, plus `EditProfileScreen` is now role-aware — a MENTOR sees specialty/bio/languages fields there instead of the aspirant fields (gender/qualification/stream/state/city/goals). The specialty/languages inputs didn't exist in the UI until now even though the backend DTO always supported them — a mentor had no way to actually set their own expertise tags before this.

Admin panel (`admin/`) now has real UI beyond login: `/dashboard` (live pending-verification/open-report counts), `/dashboard/verification` (approve/reject with signed document preview), `/dashboard/moderation` (resolve/dismiss reports, optional manual refund), `/dashboard/users` (search/filter by role, ban/unban), `/dashboard/universities` (search/filter by type, inline edit, create, activate/deactivate toggle — backed by new ADMIN-guarded `GET /universities/admin/list`, `POST /universities`, `PATCH /universities/:id` routes; deactivated universities are hidden from the public `GET /universities` listing and mobile Colleges tab but remain visible in the admin list, verified live against the real DB). It authenticates to the backend's ADMIN-guarded endpoints via a short-lived JWT minted server-side in `admin/lib/backend.ts` for one seeded ADMIN `User` row (id in `admin/.env.local` as `ADMIN_PANEL_USER_ID`) — this is separate from the admin panel's own hardcoded-credential session cookie. No delete endpoint for universities (or users) by design — deactivate/ban is the only removal path, consistent with the rest of the app's non-destructive data model.

**Ban enforcement**: `User.isBanned` existed in the schema for a long time but was never actually checked anywhere except mentor-listing eligibility — a banned user's existing JWT kept working fully (could still chat, book, etc.) until it happened to expire. Fixed in `JwtStrategy.validate()`: it now does one DB lookup per authenticated request and rejects immediately if `isBanned`/`!isActive`/`deletedAt` is set. This is a deliberate stateless→lookup tradeoff (small latency cost, indexed by id) specifically so a ban takes effect instantly rather than waiting out `JWT_ACCESS_TTL`.

Mentors now have a dedicated profile detail screen (`mentor_detail_screen.dart`, route `/mentors/:id`) — stats (rating, free-chat/call-rate), bio, expertise tags (specialty + languages), and a reviews list, all from the already-existing `GET /mentors/:id` and `GET /reviews/mentor/:mentorId` endpoints (no backend work needed). On the mentor list, tapping the avatar/name opens this detail screen; tapping the rest of the card still starts a free chat unchanged — two distinct `GestureDetector` tap zones on the same `MentorCard`. Reviews are still only ever written from the post-session flow in `SessionListScreen`, never from this screen.

**Post-signup onboarding wizards**, matching the Figma flow, replacing the old display-name-only `ProfileSetupScreen` → `/home` path: after role selection + display name, an ASPIRANT is routed to `AspirantOnboardingScreen` (3 steps: DOB/gender/state → qualification/status/stream/course → language/goals/timing) and a MENTOR to `MentorOnboardingScreen` (4 steps: DOB/gender → qualification/status → area of guidance → languages/available days, then the existing `POST /verification` call folded in as a final identity-verification step, ending in a "Verification Submitted!" screen reusing the same college-dropdown + doc-type + photo-upload UI as the standalone `VerificationScreen`). Both wizards share `onboarding_widgets.dart` (progress bar, step scaffold, dropdown, date field, chip group) and `profile_options.dart` (picklists, also used by `EditProfileScreen`) so the two flows can't drift apart. New `UserProfile` fields added for this: `dateOfBirth`, `courseInterested`, `preferredLanguage`, `preferredMentorshipTiming`, `availableDays`. "Skip for now" is preserved at every step, going straight to `/home`. Deliberately has no per-mentor "Call Rate" field even though Figma's design shows one — see the locked-in flat ₹10/min pricing decision above. The aspirant wizard was verified live end-to-end (fresh signup, all 8 profile fields round-tripped through a real fetch); the mentor wizard's backend round-trip was verified live via curl with the exact payload the wizard sends, but its own UI could not be click-tested end-to-end in this session due to browser-pane click flakiness that was also reproduced on pre-existing, previously-verified screens (Role Selection, the Welcome screen's Get Started button) — worth a real click-through next time the mobile web preview is touched.

## What's NOT built yet

- Real push notification delivery — `FIREBASE_PROJECT_ID` in `backend/.env` is still the placeholder value; in-app notifications work regardless, push is a drop-in addition once a real Firebase project is provisioned
- **Pre-chat flow** — Figma shows a "Ready for [Mentor]?" confirmation + "Need Ideas? See sample questions" screen before a chat starts; the app currently starts the chat immediately on tap with no intermediate step.
- **Richer mentor stats** — Figma's mentor detail view shows students-helped count and average response time alongside rating; `MentorDetailScreen` only shows rating + reviewCount today (the counters don't exist in the backend).
- **Low-balance interstitial** — Figma shows a "Recharge UniMinutes to continue chatting" sheet triggered mid-chat when balance runs low; not confirmed to exist in the current chat flow.
- **Nav structure difference** — Figma's bottom nav is Home / Discover / Wallet / Messages / Profile, i.e. **Wallet is a top-level tab**; the app currently nests Wallet inside the Profile tab.
- Note: Figma's mentor wizard has a per-mentor "Call Rate" field, which conflicts with the locked-in flat ₹10/min-for-every-mentor decision above — don't build that field without re-confirming the pricing model with the user first.
- Real Agora server-side "user joined channel" webhook — still the interim dual-client-confirm approach
- Android Agora native-call crash — **root-caused and fixed at the build level, not yet confirmed live on a device.** `io.agora.rtc:iris-rtc` and `io.agora.rtc:agora-special-full` both ship an `AndroidManifest.xml` with `package="io.agora.rtc"`, which AGP 8.3+/9.x hard-fails the build on. The previous workaround (`exclude(group="io.agora.rtc", module="iris-rtc")` in `android/app/build.gradle.kts`) let the app build but dropped iris-rtc's JNI wrapper (`libAgoraRtcWrapper.so`) entirely, crashing "Join Call" with `UnsatisfiedLinkError`. Real fix: both AARs are pure native-lib containers (empty `classes.jar`, no manifest components in either) — the `package` attribute is inert at runtime, so it's safe to vendor a copy of iris-rtc with its manifest package rewritten to `io.agora.rtc.iris` (see `mobile_flutter/android/app/libs/README.md` for how it was produced/regenerated) and exclude only the unpatched upstream module. `flutter build apk --debug` now succeeds and `unzip -l` on the built APK confirms both `libAgoraRtcWrapper.so` and `libagora-rtc-sdk.so` are packaged for all four ABIs (previously only the latter was present). Still needs a live run on a real Android device or a working local emulator to confirm the Agora engine actually connects a call end-to-end — this dev sandbox can't boot a hardware-accelerated Android emulator (qemu process starts but never leaves `offline` in `adb devices`), so this remains the one thing only verifiable on the user's own hardware. Untested on iOS/web — Agora doesn't work on Flutter web at all (native-only plugin), and the iOS build has never been exercised this project (the `permission_handler` MissingPluginException fix is a hand-rolled Android-only `MethodChannel`, no Swift equivalent exists yet).

## Local dev gotchas specific to this session

- **Flutter web caching**: `flutter build web` doesn't content-hash `main.dart.js`, so Chrome's HTTP disk cache can silently serve a stale copy after a rebuild even in a fresh tab with no service worker (confirmed via `transferSize: 0` in `performance.getEntriesByType('resource')`). Fixed two ways: (1) `mobile_flutter/scripts/no_cache_server.py` replaces plain `python -m http.server` in `.claude/launch.json`, sending `Cache-Control: no-store` and falling back unmatched paths to `index.html` (needed for go_router's path-based routing to survive a direct/deep-link load); (2) `mobile_flutter/scripts/build_web.sh` wraps `flutter build web` and appends a content-hash query string (`?v=<hash>`) to `mainJsPath` in the generated `flutter_bootstrap.js`, so every rebuild gets a guaranteed-fresh cache key. Use `build_web.sh` instead of calling `flutter build web` directly when testing web changes.
- **Backend hot-reload doesn't see new Prisma Client types**: after `npx prisma generate`, the already-running `nest start --watch` process keeps using its stale in-memory `PrismaService` types (`Property 'x' does not exist on type 'PrismaService'` for a model that very much exists) even though a fresh `tsc --noEmit` in a new shell sees it fine. Fix: stop and restart the "Backend (NestJS)" preview server (`preview_stop` + `preview_start`) after any migration, don't just wait for the watcher.
- **JWT role claims are stale after a role change**: `PATCH /users/me/role` doesn't reissue a token — the old JWT's `role` claim still says the previous role until the client calls `POST /auth/token/refresh` (which re-reads role from the DB). Relevant whenever testing a role transition (e.g. ASPIRANT → MENTOR) via curl: refresh before calling role-gated endpoints or you'll get a 403 that looks like a bug but isn't.

## Design reference

Figma file (Medconnect design): `https://www.figma.com/design/OObflKZgWgc2UaRZiMsPMP/Medconnect--TO-DEV`. Reviewed screen-by-screen against the built app twice — an early pass, then a full re-pull via the Figma MCP (`get_metadata` on the `SIGNUP, LOGIN, VERIFICATION, PINSETUP` / `Mentor` / `Aspirant` sections) that surfaced the structured onboarding wizards. The mobile UI is functional but styled with placeholder theme tokens, not the real design system. Non-visual gaps (imply schema/backend/flow work, not just restyling) are listed in "What's NOT built yet" above. Noise to ignore when reviewing this file: several frames ("Total Active Chits", "Branches", "Aadhaar, PAN", a "Prized" filter tab) are leftover fintech-template placeholder content, not real Uniscope features.

## Conventions to follow

- Explicit allowlist response projections (`toXResponse()` functions) for every entity — never return a raw Prisma row, to avoid leaking fields like `phoneHash`/`realNameEncrypted` as the schema evolves.
- 404 (not 403) for resources a user isn't a party to, everywhere — avoids leaking resource existence to unrelated users.
- Idempotency keys on every ledger write (`applyLedgerEntry`), derived from a stable source (hold id, report id, Razorpay payment id) — retries must never double-apply.
- Every new module gets tested against the **real** running backend + real Supabase DB (not just `tsc` passing) before being considered done — create throwaway test users/sessions via a `node -e` script with the Prisma client, `curl` the actual endpoints, verify DB state, then clean up the test data.
