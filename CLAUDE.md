# Uniscope — Project Context for Claude Code

Mentorship marketplace connecting prospective medical students (Aspirants) with verified mentors (current students/alumni) for chat and paid audio-call guidance, plus honest college reviews.

**Branch:** `feature/flutter-migration` (mobile was ported from React Native to Flutter mid-project; backend/admin unaffected).

## Pricing (settled 2026-09-06 — the earlier tiered-per-slot misread was reverted)

**Calls: flat ₹10/min, slots 6/10/20 min.** Commit `279a8c1` ("tiered per-slot call pricing") was a misread of the client brief and was **reverted** (this session) — `git revert 279a8c1`, keeping the one defensive call-site fix it carried (`slotUniminutes(kCallSlotMinutes.first)` in `session_chat_screen.dart` / `session_list_screen.dart` / `low_balance_sheet.dart`, harmless now that `slotUniminutes` is the identity again). `CALL_SLOT_MINUTES` / `kCallSlotMinutes` back to `[6, 10, 20]`; `MENTOR_RATE_PER_MINUTE_MINOR = UNIMINUTE_VALUE_MINOR` (₹10/min) restored; `CALL_SLOT_PRICE_MINOR`/`callSlotPriceMinor` deleted; every billing formula back to `minutes × MENTOR_RATE_PER_MINUTE_MINOR` (booking hold, settle debit/credit, no-show fee = `graceMinutes × rate` where `graceMinutes = slot × 0.5`, overrun extension = `+CALL_SLOT_MINUTES[0]` = +6 min). Live-verified: booking a 6/10/20-min call places a hold of exactly 6000/10000/20000 minor and `session.ratePerMinuteMinor = 1000`; slot 40 now `@IsIn`-rejected (400).

**Recharge: four fixed packages, not a continuous formula.** `₹250→10, ₹400→20, ₹750→40, ₹1000→60 Uniminutes` — non-linear, bigger packs give more per rupee (the opposite margin shape from the old flat 20%). `RECHARGE_PACKAGES` + `RECHARGE_AMOUNTS_MINOR` live in `backend/src/modules/wallet/dto/create-topup.dto.ts` (keyed by exact minor amount: 25000/40000/75000/100000). `CreateTopupDto.amountMinor` is `@IsIn(RECHARGE_AMOUNTS_MINOR)`; `WalletService.computeTopupCredit` is now a lookup that **throws `BadRequestException`** on any other amount (defence-in-depth for the webhook / direct-confirm paths that read the amount back from Razorpay). The old continuous `PAID_MINOR_PER_UNIMINUTE_CREDITED = 1250` formula and `MIN_TOPUP_MINOR` are gone. Mobile: `wallet_screen.dart`'s `_showTopupSheet` now renders one full-width button per package from `_kRechargePackages` (`[(250,10),(400,20),(750,40),(1000,60)]`, must match the backend), each showing `₹X` and its Uniminute count. Live-verified: all four amounts create a Razorpay order; 30000/50000/24999/20000 all `@IsIn`-rejected (400).
## Stack

| Layer | Tech |
|---|---|
| Mobile | Flutter (`mobile_flutter/`) — Riverpod, go_router, Dio |
| Backend | NestJS + TypeScript (`backend/`) — Prisma 7 ORM |
| Database | PostgreSQL on Supabase (pooler connection; `DIRECT_URL` bypasses pooler for migrations) |
| Admin panel | Next.js (`admin/`) — single hardcoded admin login, kept as a **separately-deployed, non-public** surface (not linked from user-facing app) |
| Auth | Phone OTP (Twilio Verify, or `mock` provider for local dev) + JWT access/refresh |
| Real-time chat | Postgres (persistence, via Prisma) + Supabase Realtime Broadcast (content-free "new message" ping — see "Chat architecture" below). Replaced Stream Chat (cost-driven migration, 2026-09-02). |
| Audio calls | **Live in production: Agora.** Client has since decided to move to **Stream Video** (not LiveKit — that path was built, briefly auto-deployed by accident, then reverted; see "Calls migration status" below) — fully built and live-tested against the real Stream account on `migration/agora-to-stream-calls`, not yet merged, blocked only on real-device testing (see below). |
| Payments | Razorpay (Orders API + webhook, also a direct client-confirm fallback for local dev since webhooks need a public URL) |

Root is an npm workspace monorepo (`backend`, `admin`; `mobile_flutter` is separate/Flutter, not an npm workspace member).

## Local dev setup

- Backend: `cd backend && npm run start:dev` (or build+run `dist/main.js` directly — see below). Runs on port **3001**, prefix `/api/v1`.
- Admin: `npm run dev --workspace=admin` — **must use `next dev --webpack`**, not the Turbopack default (Turbopack can't resolve `lightningcss`'s native binding in this monorepo's hoisted `node_modules`; already fixed in `admin/package.json`).
- Mobile: `flutter run --dart-define=API_URL=http://10.0.2.2:3001/api/v1` for Android emulator (localhost aliasing), or your LAN IP for a physical device.
- All secrets live in `backend/.env` and `admin/.env.local` (both gitignored). See `backend/.env.example` for the full list — Supabase, Twilio, Redis (Upstash), Razorpay, Agora, JWT secrets are all already populated in the working `.env` for this project's own accounts. (No separate chat-vendor secret — chat reuses the same Supabase project/keys already configured for storage.)
- Admin login (local dev): credentials are in `admin/.env.local` (`ADMIN_EMAIL`/`ADMIN_PASSWORD`/`ADMIN_SESSION_SECRET`) — not committed.
- **Mobile login in `mock` OTP mode**: any phone number, code is always the fixed `MOCK_OTP_FIXED_CODE` in `backend/src/auth/otp/mock-otp.provider.ts` (currently `111111`) — deliberately not random, so anyone can log in without reading server logs. Only applies when `OTP_PROVIDER_TYPE=mock`; switching to `twilio` sends and requires a real SMS code again. Don't ship this backend reachable outside localhost while in mock mode — the fixed code means anyone who can reach it can log in as any phone number.
- **A recurring dev-server gotcha:** the Supabase free-tier project auto-pauses after inactivity; if DB calls start failing, check the Supabase dashboard shows the project "Active" and retry.
- **Backend restart pattern used throughout this project's history** (Claude_Preview MCP tools were unreliable mid-session): `rm -f tsconfig.build.tsbuildinfo && npx tsc -p tsconfig.build.json && node dist/main.js` backgrounded, health-check via `curl localhost:3001/health`.

## Prisma workflow (Prisma 7 — syntax changed from earlier versions)

- No `url` in `datasource db` block — reads `DATABASE_URL`/`DIRECT_URL` from `prisma.config.ts`.
- Generate a migration from a schema diff (works against the live dev DB, non-interactive-safe):
  ```
  npx prisma migrate diff --from-config-datasource --to-schema prisma/schema.prisma --script
  ```
  Then hand-create the migration folder (`prisma/migrations/<YYYYMMDDHHMMSS>_<name>/migration.sql`, 14-digit timestamp prefix required) and `npx prisma migrate deploy`.
- For **enum value remaps** (not just additions), Prisma's diff tool can't express the data transformation — hand-write the migration using the create-new-type / `ALTER TABLE ... USING (CASE ...)` / drop-old-type / rename pattern. See `prisma/migrations/20260712125549_collapse_user_role_to_aspirant_mentor/migration.sql` for a worked example (this preserved existing user data instead of resetting).
- `prisma migrate dev` and `migrate reset` require a TTY — don't work in a non-interactive agent session. Use `migrate diff` + `migrate deploy` instead.

## Client priority checklist (status as of 2026-08-28 — read this first)

The client sent an 8-item priority list. Current status of each, and what's actually next — check this before assuming any of these is either done or untouched:

1. **Call integration** — backend (Agora token server, dual-confirm billing, overrun handling) built and live-tested; Android build-level crash fix done but never confirmed on real hardware (see "What's NOT built yet"). **Superseded mid-project**: client decided to move off Agora to **Stream Video** — see "Calls migration status" below for the current, non-production state of that.
2. **Colors — brand colour is now teal `#4C9E97` (2026-09-08, client-picked swatch), replacing green `#0B815A`.** `AppColors.primary` in `app_theme.dart` is `#4C9E97` — a lighter, more blue-leaning teal; the client is moving the app off the saturated green. This propagates everywhere via the token (`authBrandTeal = AppColors.primary` → bottom nav, buttons, "See all", search icon, links). **Home header specifically:** the `home_screen.dart` canopy is a flat `AppColors.primary` fill — no gradient (was `AppGradients.canopy`), per explicit request; the curved sheet pull-up is unchanged. **Then (2026-09-08 → 2026-09-09, a run of tight header iterations):** "make the transparency to 20" → "20% opacity + dark text" → "80% opacity with dark texts" → finally **"bring the same colour from [a screenshot], don't change the current curve, with the same font size and colour"**. The screenshot is the 2026-09-08 reverted-mockup header. Current state: the canopy fill is a **`const LinearGradient(topCenter→bottomCenter, [AppColors.primaryLight, AppColors.background])`** — the pale mint→page-background wash from that screenshot (the solid `#4C9E97` and the 20%/80% alpha washes were all same-window intermediates). Every white header element is dark now: greeting `Text.rich` back to the **split style at `AppFont.xxl` extraBold** — `"$greeting, "` in `AppColors.textPrimary` + first name in `AppColors.primary` (teal), matching the screenshot's size + colour; subtitle `AppColors.textSecondary` (`AppFont.xs`); `NotificationBell(color: AppColors.textPrimary)`; avatar-ring border `AppColors.border`; the white chip behind the logo mark **removed** (navy `AppColors.logoNavy` mark reads on the pale wash — icon 30→34px). **Deliberately NOT changed** (per "don't change the current curve"): the sheet pull-up / rounded-corner mechanism, and the green canopy/sheet handle-bar glow pill still sits at the seam (flagged to the user as a possible follow-up — it reads oddly on the pale fill and the reference screenshot has none). The building-watermark illustration from the mockup was **not** re-added. Search pill (white `AppColors.surface`) unchanged. `dart analyze` clean; not device-tested (browser pane can't drive the Flutter canvas). **Earlier known-contrast note (pre-2026-09-08, when the fill was solid `#4C9E97`):** white on `#4C9E97` is ~2.9:1 (below AA 4.5) and `#4C9E97` on white ~2.9:1 — the client chose the solid fill after being shown the numbers; the 20% wash + dark text sidesteps it. **Derived-token retune DONE 2026-09-10** (the client asked why the "Need help?" / Wallet / review cards still read green — those paint with `AppGradients.brand`): `primaryLight #E3F3EC→#E6F3F1`, `primaryDark #005E44→#1F5A54`, `canopyTop #1C9B78→#3C8F88`, `AppShadows.raised` tint `#0B815A→#4C9E97`, `AppGradients.brand [#0B815A,#005E44]→[#4C9E97,#35726B]`, `AppGradients.hero` start `#E3F3EC→#E6F3F1`, `AppGradients.canopy [#1C9B78,#0B815A,#6FBFA3,#E3F3EC]→[#3C8F88,#4C9E97,#86C4BF,#E6F3F1]`, the Medical stream-glyph in `stream_visuals.dart` `#0B8F6A/#E3F4EE→AppColors.primary/primaryLight`, and `call_screen.dart`'s dark call-surface gradient + radial glow to the teal family. `AppColors.success #12A150` deliberately left green (semantic — a credit/verified tick reads as green-means-good, and now that the brand is teal the two are clearly distinct). `dart analyze` clean; colour-value + comment edits only, not device-tested. History below is retained for context but the green is superseded. (changed 2026-09-04 from the prior `#008562`, close in hue/lightness — a refinement, not a direction change). `AppColors.primary` in `app_theme.dart` is `#0B815A`; `primaryLight`/`primaryDark` (`#E3F3EC`/`#005E44`) are independently-chosen complements, deliberately left unchanged both times, still verified consistent against the new primary (white-on-primary 4.88:1, primary-on-background 4.58:1, both AA). `AppShadows.raised`'s tint and both brand gradients (`AppGradients.brand`, `AppGradients.canopy` — canopy's two middle stops recomputed via linear interpolation against the new primary at the existing stop positions) were updated alongside it so nothing reads as a mismatched leftover shade. (`#008562` itself had superseded the original teal `#12A9A3`.) A navy+emerald "Option B" duotone exploration was built, screenshot-approved, then **explicitly reverted** by the client ("revert back to the previous colour pallet changes dont bring the blue inside") — don't reintroduce navy without asking again, even though it still exists as a one-time explored-and-rejected direction in git history. Home screen canopy (header gradient) went through several rounds after the original green swap: darkened the top stop (`AppColors.canopyTop`, `#1C9B78`) so it reads as a real green instead of a washed-out pastel, then held that green flat through ~65% of the canopy instead of fading to white immediately, so there's a visible boundary against "Quick actions" below instead of the canopy blending away to nothing. Also retired the leftover blue-navy `authBrandNavy` (was `#0F2A3E`) from Login/OTP/University Detail so those screens match the rest of the app (now aliases `AppColors.textPrimary`). **Then (2026-09-06) the canopy went to "option C"** — `AppGradients.canopy` is now `#1C9B78 → #0B815A → #6FBFA3 → #E3F3EC` (stops `0/.38/.78/1`), i.e. the bottom fades to `primaryLight` so the seam with the white content sheet dissolves instead of holding a hard green band (supersedes the "held flat through ~65%" round above). **Applied to the six bottom-nav tab headers** via a shared `GradientAppBar` (`app_widgets.dart` — an `AppBar` over `AppGradients.canopy`, white title/leading/action icons): Home already had the canopy; Sessions / Wallet / Colleges / Mentors / Profile now use `GradientAppBar` (Profile's logo sits on a white chip so it reads on the green, same as Home). Everything outside those 6 keeps the plain neutral theme bar. **`GradientAppBar` reverted to a plain neutral bar 2026-09-06 per explicit request** ("normal white with black text, no green box"): it now renders identical to the theme default (`AppColors.background` ground, `textPrimary` title/icons) — the class name is kept only so the ~7 call sites (the six bottom-nav tabs + Help Centre) don't all need touching; it's effectively an `AppBar` alias. **This applies to the six tab headers / "other screens" ONLY.** A parallel session briefly also flattened the two students' Home screens and mentor-detail (commit `501afd6`), but the client pushed back — "other screens only i asked to make the changes" — so `home_screen.dart` and `mentor_landing_screen.dart` were **restored** (2026-09-06, `git apply -R` of `501afd6`'s hunks for those two files): the aspirant Home keeps its green `AppGradients.canopy` header (logo on a white chip, white avatar ring, white `NotificationBell`, white greeting prefix + `AppColors.logoNavy` first name via `Text.rich`), and the mentor Home keeps its faint `AppGradients.hero` wash. Only `mentor_detail_screen.dart`'s `_GradientHeader` stays flat/neutral from `501afd6` (it's a detail screen, not a home screen). Home header text: `AppColors.logoNavy` (`#001A46`, sampled from the logo PNG's own pixels) for the **"Uniscope" wordmark** and the **first name in the greeting** only ("Good morning, " stays white). **Not yet done**: sessions/wallet/profile bodies haven't been audited for the same kind of hardcoded-color drift. Grep for `Color(0xFF` / `Color(0x` outside `app_theme.dart` to find the next batch. **2026-09-09:** the "Saved colleges" / "Saved mentors" heart in the Discover (`university_list_screen.dart`) and Mentors (`mentor_list_screen.dart`) app bars was hardcoded `Colors.white` — invisible on the neutral `GradientAppBar` — so the entry points looked missing. Changed both to `AppColors.primary`. (The mentor's Discover tab already uses the *same* `UniversityListScreen` and the mentor branch already registers `/colleges/saved`, so "mentor Discover synced with student Discover" was already true — only the invisible icon needed fixing.) The Sessions tab's `NotificationBell(color: Colors.white)` has the same bug — flagged as a follow-up, not fixed here.
3. **OTP integration — DONE, live in production, confirmed by the client's own real login (2026-09-01/02).** Client moved off Twilio entirely and onto **MSG91** (cheaper per-SMS for Indian numbers). **Production is Railway** (project "Uniscope Mobile", service `uniscope`), **not Render** — `render.yaml` was a stale leftover from an earlier migration and has been deleted (see `SECURITY.md` § S-1). `OTP_PROVIDER_TYPE=msg91` was set on Railway and redeployed; deployment confirmed `SUCCESS`, `/health` confirmed 200, and — the part that actually matters — the client logged in for real through the app and confirmed it worked. No further action needed on this item. `Msg91OtpProvider` (`backend/src/auth/otp/msg91-otp.provider.ts`) follows the exact same `OtpProvider` interface as `TwilioOtpProvider`/`MockOtpProvider` — `OTP_PROVIDER_TYPE` accepts `mock | twilio | msg91`. **Live-verified end to end** against the client's real MSG91 account and a real DLT-approved OTP template: two real phone numbers, two real SMS round trips through `POST /auth/otp/request` → `POST /auth/otp/verify`, both issuing real JWTs and correctly resolving to existing accounts by phone hash (provider-independent identity — switching OTP providers doesn't fragment users). One real bug found and fixed live: `VerifyOtpDto.code` was a strict `@Length(6, 6)` (sized for Twilio/mock's fixed 6-digit codes) but a real MSG91-generated code came back 4 digits — widened to `@Length(4, 8)`, and `Msg91OtpProvider.sendOtp` now also passes `otp_length=6` to ask MSG91 for a 6-digit code explicitly (whether that param or a DLT-template-locked format determines the actual length wasn't conclusively isolated — the DTO widening is the real safety net either way). Along the way: MSG91 accounts can have multiple auth keys with completely separate wallet balances not visible to each other — burned some time on a `0`-balance key before the client swapped to the correct one; worth remembering if OTPs ever silently stop sending again. `TwilioOtpProvider` is left in place, not deleted — an easy one-line fallback if MSG91 ever needs to be swapped back out.
4. **Admin profile** — client said "nothing to touch, enough as is." Closed, no further action.
5. **Payment gateway** — client is waiting on Razorpay live creds, but Razorpay is asking for a live payment-gateway site or a published app before approving Uniscope. **Unresolved — needs a real answer, not code.** The `web/` marketing site exists and could plausibly serve as that "payment gateway site" if it's fleshed out with the pages Razorpay's KYC typically wants (pricing/refund policy, terms, contact, about) — worth checking Razorpay's actual activation checklist and seeing if the existing site can be extended to satisfy it, rather than waiting on a store-published app first.
   - **Legal/policy pages (in progress, 2026-09-06):** client is supplying four docs — Terms & Conditions, Refund & Cancellation Policy, Privacy Policy, Community Guidelines. Decision: **host on the `web/` site, link from the app** (the app already has a `/legal` → `WebPageScreen` webview and links to `uniscope.in/{terms,privacy,guidelines}` from the login + Help screens). Pages are plain Next.js static routes styled to match `web/app/privacy/page.tsx` (`SiteNav`, "Legal" eyebrow, `<h1>`, `Section` blocks). **No "effective/last updated" date line on any of them, per explicit client instruction.** **`web/app/terms/page.tsx` is DONE and LIVE at `https://uniscope.in/terms`** — the prod site auto-deploys from the `prod/web-enrollment-site` branch (Vercel), and the Terms page was synced there via an isolated `git worktree` (commit `084371e` "Sync web/: add Terms and Conditions page") and confirmed 200 with all 35 sections + Grievance Officer block. The T&C source doc's own "Effective Date" / "Last Updated" header lines were omitted; the only "effective date" text on the page is the generic phrase in clause 34. **All four pages are now live** (`prod/web-enrollment-site`): `uniscope.in/{terms, privacy, community-guidelines, refund}` — verified 200 (all `uniscope.in` → `www.uniscope.in` 308-redirect then 200). **App-side wiring done (2026-09-06):** `mobile_flutter/lib/features/common/legal_links.dart` — a `LegalPage` enum (title + URL for all four) + `openLegalPage(context, page)` which pushes the existing `/legal` → `WebPageScreen` webview, so the URLs live in one place. Linked from: **Profile → Settings** (new "Legal & policies" `AppCard` — all four rows), the **Help Centre footer** (`_HelpFooter` — Terms · Privacy · Community guidelines · Refund policy; the old footer hard-linked a **404** `uniscope.in/guidelines` — fixed to `/community-guidelines`), the **wallet top-up sheet** (a "Refund & Cancellation Policy" `TextButton` under the recharge packs — pops the sheet then opens it), and the **Login screen** consent line (was one "Terms & Privacy" span → privacy only; now two spans, "Terms" → `/terms` and "Privacy Policy" → `/privacy`). Do NOT bundle the concurrent-session `web/` changes (`GetStarted.tsx` copy, `page.tsx`/`options.ts` comments) into a prod sync — the `GetStarted.tsx` one specifically regresses a fix prod already has (`fd459b3`).
   - **Legal pages moved from an in-app webview to fully native rendering (2026-09-11, per request "make the terms, policies… in app instead of redirecting to web").** The `/legal` → `WebPageScreen` webview (loading `uniscope.in/{terms,...}` live) is retired — `WebPageScreen`/`web_page_screen.dart` is now dead code (no caller left; kept in place, not deleted). All four documents' text was mechanically extracted from the **live web pages' own JSX source** (`prod/web-enrollment-site` branch — the canonical content source of truth is unchanged, still that branch) into flat, typed JSON — `mobile_flutter/scripts/extract_legal_content.py` (parses `<Section title="…">`/`<SubSection>`/`<p>`/`<ul>`/`<ol>` blocks, resolves the `{CONTACT_EMAIL}` / `` {`mailto:${CONTACT_EMAIL}`} `` JSX interpolations and multi-fragment paragraphs, verified against the source section-count-for-section-count with zero leftover JSX artifacts across all four docs) → `mobile_flutter/assets/legal/{terms,privacy,community_guidelines,refund}.json`. New `legal_content.dart` (`LegalBlock`/`LegalDocument`, `loadLegalDocument(assetPath)` — `rootBundle.loadString` + `jsonDecode`, no network) + `legal_page_screen.dart` (`LegalPageScreen` — a plain `ListView.builder` switching on block type, styled with the app's own `AppFont`/`AppColors`, no markdown parser dependency). `legal_links.dart`'s `LegalPage` enum now carries an `assetPath` instead of a URL; `openLegalPage` is synchronous (no more `kIsWeb`/`url_launcher` web-tab fallback — native rendering works identically on every platform, including web, unlike the webview it replaced) and still funnels through the same single `/legal` route so all 6 existing call sites (Settings, Help Centre footer, wallet top-up sheet, Login ×2) needed no changes. **A real, independent routing bug was found and fixed in the same pass**: `/legal` was in `_preAuthRoutes` (`app_router.dart`), and the redirect logic bounces every `_preAuthRoutes` member straight to `/home` for an *authenticated* user — meaning the Settings/Help/Wallet links to Terms/Privacy/etc. had **always silently bounced back to Home for a logged-in user** (confirmed live via the Browser pane: tapping "Terms & Conditions" in Settings did nothing but flash back to Home), regardless of whether `/legal` rendered a webview or the new native screen. Fixed with a one-line exemption (`if (loc == '/legal') return null;` ahead of the bounce check) — `/legal` now stays reachable both authenticated and anonymous, every other `_preAuthRoutes` member's bounce-when-authenticated behavior is unchanged. Found in passing: `test/flow_test.dart`'s `_FakeUsersApi.updateProfile` override had drifted from the real `UsersApi.updateProfile` signature (missing the `upiId` param added 2026-09-10) — a real `invalid_override` compile error that only surfaces on a project-wide `dart analyze` (not a per-file one), fixed alongside. `dart analyze` clean project-wide (0 errors, same 33 pre-existing infos); `flutter test` shows only the one pre-existing unrelated `SectionHeader` overflow failure. **Live-verified in the Browser pane**: Settings → "Legal & policies" section renders correctly (all four rows); the pre-fix bounce-to-Home bug was directly observed. **Not confirmed**: the post-fix click-through to an actual rendered document — the Browser pane went persistently unavailable for sustained interaction (screenshots worked, `left_click` timed out waiting on a composited frame) partway through this session; the fix is verified by direct code reading + a clean `dart analyze`, not by a final screenshot of rendered Terms/Privacy content on-device. Worth a quick manual tap-through next time the app is on a real device or a working browser preview. To refresh a document's content later (policy wording changes): re-run `extract_legal_content.py` against the current `prod/web-enrollment-site` JSX (usage in the script's own docstring) and commit the regenerated JSON — no Dart code changes needed for a content-only update.
6. **Profile creation & college entries matching the website** — client clarified: whatever stream/university updates went into the **web** lead-capture forms (`web/components/AspirantForm.tsx`, `MentorForm.tsx`, `web/lib/options.ts`) as form-submission options should be reflected in the app's onboarding/edit-profile pickers wherever relevant. Mobile side **done**: `mobile_flutter/lib/features/profile/profile_options.dart` ports `web/lib/options.ts`'s `DEGREES_BY_STREAM`/`MEDICAL_SPECIALIZATIONS` 1:1 — `degreesForStream(stream)` replaces the old flat `kDegrees` list, and a Medical-stream mentor's specialization is picked from the real curated list instead of free-typed; mentor onboarding wizard wired to both, resets degree+specialization on stream change. `dart analyze` clean. **This kept expanding on the web side after that** (several more rounds, all in `web/components/AspirantForm.tsx`, per explicit follow-up requests): Aspirant form's "Current qualification" now depends on "Field of interest" the same way MentorForm's Degree depends on Stream (same `DEGREES_BY_STREAM` source, "Higher Secondary (12th)" always prepended since a 12th-grader hasn't picked a stream-specific track yet); College + Specialization fields were added to the aspirant form (shown whenever qualification isn't "Higher Secondary (12th)"), reusing the exact same curated-per-stream college-search mechanism as the mentor form, with one deliberate difference — aspirant Specialization is never scoped to the picked college, it shows every specialization known for the whole degree; and Doctorate/Others for Engineering/Law/Dental now reuse that stream's normal college search + a stream-wide union of specializations, instead of a separate free-text field. Worth a periodic recheck that mobile's `profile_options.dart` hasn't drifted from these newer web-side additions. **2026-09-08 — the College + Specialization pickers in both onboarding wizards were brought to parity** after a user report (app college field wasn't a browsable dropdown, labels lacked the district, specialization list was wrong/missing for non-Medical streams): (a) `CollegeSearchField` (`college_search_field.dart`) rewritten — browsable on focus (fetch on empty query, not the old `>=2` char gate), and when the stream+degree has a curated key (`curatedDegreeKey`) it now uses `GET /universities/curated` whose labels are the full `"name, district, state"` string the website shows; otherwise the general `GET /universities?browse=true` scoped to `stream`+`level`. `UniversitiesApi.search` gained `stream`/`level` params. `onPick` signature changed `(University?, text)` → `(String? universityId, text)`; both wizards track `_universityId` instead of a `University` object. (b) Specialization now shows for **any** curated stream+degree (Engineering/Dental/Law included, not just Medical). Undergraduate degrees (MBBS/BDS/UG) still show no specialization. **2026-09-08 follow-up — mentor Specialization is scoped to the picked college** (matching the web MentorForm): `CollegeSearchField.onPick` now yields the picked `CuratedCollege`'s own `specializations`; the mentor wizard stores them in `_pickedSpecializations` and the Specialization field (which only appears once a college has been entered) shows *that college's* programs, falling back to the degree-wide union only when a free-typed college has no mapped programs. The old behaviour merged every college's specializations and, worse, fell back to the static `kMedicalSpecializations` while the union was still loading — so e.g. Medical+Diploma at a college that only offers a couple of diploma programs wrongly listed "General Medicine" etc. The **aspirant** wizard deliberately keeps the degree-wide union (`specializationsForDegreeProvider`), same as the web AspirantForm. (c) New `OnboardingSearchableField` (searchable bottom-sheet picker) replaces the plain `OnboardingDropdown` for Specialization (the Medical list is ~100 items). `flutter analyze` clean; not device-tested (aspirant onboarding is aspirant-only + the browser pane can't drive the Flutter canvas). Still not ported: the web form's free-text-specialization fallback for non-curated non-Medical degrees (mobile just hides the field there).
7. **Publish to Play Store / App Store** — client is waiting on others to complete it, but real build/config work happened this session (2026-09-12) trying to get an actual internal-testing `.aab` uploaded. **Android `applicationId` changed `com.uniscope.uniscope_mobile` → `in.uniscope.app`**, because Play Console's app record for this app is permanently locked (on first upload, forever) to `in.uniscope.app`, and the codebase's `com.uniscope.uniscope_mobile` (a Flutter-default-looking name, never actually uploaded anywhere) had simply never been reconciled with it — `in.uniscope.app` doesn't appear anywhere else in this repo's history, so it was set directly in Play Console at some point, independent of the code. Chose to rename the code to match Console (rather than create a brand-new Play Console app under the old name) since a Console app record already existing usually means real store-listing setup — content rating, data safety form, screenshots — already happened there; abandoning it would mean redoing all of that. Changed: `android/app/build.gradle.kts` (`namespace`/`applicationId`), and `MainActivity.kt`/`CallForegroundService.kt` moved from `android/app/src/main/kotlin/com/uniscope/uniscope_mobile/` to `.../kotlin/in/uniscope/app/` with their `package` line updated — **`in` is a reserved Kotlin keyword**, so the declaration needs backtick-escaping: `` package `in`.uniscope.app ``, not `package in.uniscope.app` (a real, easy-to-miss gotcha for any future `.in`-domain Android package). `AndroidManifest.xml` needed no change (its `.MainActivity`/`.CallForegroundService` references are relative, resolved via `namespace`). **Verified end to end, not just compiled**: a full `flutter build appbundle --release` with the new package genuinely produces a signed `.aab` — confirmed via a temporary, backed-up-and-restored diagnostic swap of `google-services.json`'s `package_name` field (never committed, real file restored immediately after) that let the build run all the way through instead of stopping at the (expected, unrelated) Firebase mismatch. **Still blocked on one manual, non-agent-executable step**: the *real* `google-services.json` for `in.uniscope.app` has to be issued from Firebase Console (Project `uniscope-83eeb` → Add app → Android → package `in.uniscope.app` → download the file → replace `android/app/google-services.json`, which is gitignored, so every machine needs this done locally) — the Firebase CLI on this dev machine is authenticated under an unrelated personal account (`krnkratoz@gmail.com`) with no access to the real project, so this genuinely cannot be done by an agent session here. New `mobile_flutter/scripts/build_android_release.sh` (mirrors `build_ios_release.sh` below) wraps `flutter build appbundle --release` with `API_URL`/`GIT_SHA`/`BUILD_TIME` always supplied and a post-build check that fails loudly if `localhost:3001` leaks into the compiled binary — use this instead of a bare `flutter build appbundle`/`apk` for any real build going forward.
8. **Unique ID on aspirant & mentor profiles — DONE, all 8 stream codes confirmed by the client and live in production.** Format (final, client-confirmed): `{A|M}{2-digit stream code}{2-digit enrolment year}{7-digit yearly sequence}`, e.g. `A44260000001` — no separators, sequence resets only on calendar-year rollover, never daily (see the doc comment in `unique-id.helper.ts` for why: without month/day in the visible ID, a daily reset would hand out duplicate IDs across different days in the same year). Final `STREAM_CODES`, no placeholders left: Medical=11, Dental=22, Engineering=33, Others=44, Arts & Humanities=55, Commerce & Business=66, Law=77, Design=88 (a plain +11 doubling pattern — supersedes an earlier client spec that had Others break the pattern at 26; any already-assigned row using that old code is left as-is, since a uniqueId never changes once given out). `UsersService.ensureUniqueId` assigns lazily the first time `profile.stream` is set, permanent once assigned. Mobile: `uniqueId` is in the `UserProfile` model and shown on the Profile screen for both roles. **Nothing pending on this item** — this format went through several revisions mid-project (an all-time-sequence format, then a YYMMDD-dated format that briefly shipped, then this final one); `unique-id.helper.ts`'s doc comment explains how to tell old-format rows apart from new ones if that ever matters.

## Calls migration status (2026-09-02 — production is still Agora)

Client cost/reliability comparison landed on **Stream Video** (not LiveKit — see below for why that path was abandoned), reusing the same Stream account already live for chat pre-migration (same `STREAM_API_KEY`/`STREAM_API_SECRET`, no new Railway variables needed). Confirmed before building: Stream Video has real India SFU presence (Mumbai **and** Delhi), and a dedicated audio-only pricing tier via the `audio_room` call type, which disables video at the platform level.

- **Built and live-tested** on `migration/agora-to-stream-calls`: backend `StreamVideoModule`/`StreamVideoService` (real call created, correctly call-scoped tokens issued for both parties, dual-confirm billing verified end-to-end against the real Stream account), mobile `stream_video` SDK swap with a rebuilt call screen. `tsc`/`flutter analyze` clean.
- **NOT merged, NOT deployed.** Production (`feature/flutter-migration`) is still plain Agora, healthy.
- **The one gate before merging**: real-device testing (join, dual-confirm billing, and specifically the speaker toggle — Stream Video's Flutter SDK has no plain on/off speakerphone switch, only device-label matching, which is the least certain piece of this integration).
- **LiveKit was tried first and abandoned**: fully built, briefly auto-deployed to production by accident (a shared-working-directory branch mix-up caused a LiveKit commit to land on `feature/flutter-migration` and Railway auto-deployed it with no real `LIVEKIT_*` credentials set — no live outage, since the previous successful build kept serving, but it was a real near-miss). Reverted cleanly via `git revert`. **Lesson that actually matters going forward**: this repo's working directory gets shared across concurrent sessions — always confirm `git branch --show-current` before committing, and use `git worktree add` for isolated testing of a migration branch rather than assuming the checked-out branch hasn't moved under you.

**Second instance of the same lesson (2026-09-05):** a concurrent session ran a working-directory-wide git reset/checkout while this session had a full day's uncommitted work in the tree (the university-review 12-question form + college-review gate, ~24 files). That session correctly `git stash`ed everything first (labelled "concurrent WIP: university-review structured form + college-review gate (not this session)") rather than discarding it, so nothing was lost — but ~19 unrelated commits had already landed on the branch in the meantime, so the stash could not be reapplied as a blind `git stash pop`. Recovery required diffing the stash against its own base commit (`git diff <base> stash@{0}` / `git stash show --stat`, **not** `git diff HEAD stash@{0}`, which conflates the stash's real changes with every intervening commit) to find which files were untouched by other work (safe direct restore) versus which had independently diverged (needed a manual re-merge onto the current file — `session-response.ts`/`session_list_screen.dart`/`sessions_api.dart`/`mentor_home_screen.dart` turned out to already have an equivalent `mentorIsAvailable` feature built independently by the concurrent session, so those were left alone rather than overwritten). **Takeaway: if you're about to run a tree-wide git reset/checkout/clean and the status shows modified files you didn't create, `git stash` them first with a descriptive message — never assume uncommitted work in a shared tree is yours to discard.** And if you're the one recovering from a stash later, always compare it against its own merge-base, not against current HEAD.

## Business model decisions (locked in, don't re-litigate without asking)

- **Roles:** collapsed to `ASPIRANT` / `MENTOR` / `ADMIN` (previously 4 granular roles — migrated, not reset).
- **Mentors are no longer medical-only — Uniscope expanded to all academic streams (Medical, Dental, Engineering, Arts & Humanities, Commerce & Business, Law, Design, Others).** This reverses the earlier "medical/dental alumni only" rule. `UserProfile.stream` (mentor-role usage — dual-purpose with the aspirant's school-level PCB/PCM stream, same pattern as `specialty`) is now the primary discovery/filter attribute (`kMentorStreams` in `profile_options.dart`); the old Areas-of-Guidance wizard step and its `specialty` picklist (`kGuidanceAreas`) were removed from the mentor onboarding wizard but the field/const still exists for backward compatibility with mentors who set it before this change, and is still used by `EditProfileScreen` (not yet updated for the multi-stream reality — worth revisiting). Mentor cards/detail screens now show `stream` when `specialty` is unset.
- **Mentor onboarding wizard collects a real name + self-chosen alias up front**, rather than relying on the auto-generated pseudonym. `UserProfile.realNameEncrypted` (AES-256-GCM, `PROFILE_ENCRYPTION_KEY` env var, `src/common/helpers/profile-encryption.helper.ts`) stores the real name — never returned in any response projection, write-only via `UpdateProfileDto.realName`. The alias *is* `User.displayName`, checked live for availability via `GET /users/check-display-name` (case-insensitive lookup, not a DB-level unique constraint — no existing collisions found when this was added). Mentor wizard structure, in order: Basic Information (full name, alias, gender) → Location (state, city) → College Details (**stream → degree → college → specialization**, in that order — each field appears once the one before it is answered, since degree options and the college/specialization data are all stream-specific) → Current Status (studying/graduated + year, `yearInfoPrivate`, preferred languages, preferred timing) → Choose Your Avatar (skippable) → identity verification (college ID upload; skippable). **Current Status ↔ College Details order fixed 2026-09-08** — the code had them the wrong way round (Current Status was step 3, before College Details), which put "are you studying or graduated" before the mentor had even said which college; this bullet's order is now what ships. `_next()` splits the old `_resolveCollegeThenSave` into `_resolveCollege()` (leaving College Details — find-or-create only) and `_confirmAgeThenSave()` (leaving Current Status — age gate + the single `updateProfile` that carries both the college and the status/languages/timing data). "Skip for now" appears on the avatar and verification steps. The wizard is reached directly from Role Selection (mentors skip the shared `ProfileSetupScreen` entirely, since Basic Information covers the same ground) and ends by going straight to Home.
- **Currency:** "Uniminutes" wallet. Internally still paise-denominated (`Wallet.balanceMinor`, `LedgerEntry.amountMinor` — architecture principle: money is always integer minor units, never floats), but 1 Uniminute = 1000 minor units = ₹10, matching the **flat mentor call payout rate** (all mentors currently earn the same ₹10/min for calls — this was a late-session decision that supersedes an earlier "per-mentor custom rate" discussion; worth double-checking with the user if extending this).
- **Recharge margin:** four fixed packages (`RECHARGE_PACKAGES` in `create-topup.dto.ts`) — ₹250→10, ₹400→20, ₹750→40, ₹1000→60 Uniminutes (1 Uniminute = ₹10 of spendable value, so the margin is ₹150 / ₹200 / ₹350 / ₹400 respectively — bigger packs are a *better* rate per Uniminute, opposite of the old flat 20%). The margin is baked into the recharge conversion only, **never** shown to the user as a line item, and **never** taken again at session-billing time (mentor gets paid the exact same amount debited from the aspirant). Any amount outside the four is rejected (`@IsIn` on the DTO + a throwing `WalletService.computeTopupCredit`) — recharge is a closed set of packages, not an open top-up-any-amount flow. See the "Pricing" section near the top for the full before/after.
- **Uniminutes are the only student-facing unit. Rupees appear at wallet top-up and nowhere else.** 1 Uniminute = 1 minute of call time = 1000 minor units, so a balance reads directly as "minutes I can talk". Call cost is never expressed in ₹ on any aspirant surface (mentor cards, mentor profile, slot picker, in-call, session list, ledger) — only the top-up sheet shows ₹, because that's the only moment real money changes hands. Mentor-side surfaces (earnings, payouts) stay in ₹ since mentors withdraw to a real bank account. Helpers: `minorToUniminutes` / `slotUniminutes` / `uniminutesLabel` in `mobile_flutter/lib/core/network/wallet_api.dart`.
- **Never advertise that chat is free.** Chat being free is the baseline assumption, not a feature to sell — no "Free chat" chips, no "Chat · Free" buttons, no "chatting is always free" footnotes. The Chat button just says "Chat". (This reverses earlier copy that labelled chat as free in several places.)
- **No dating-app / matchmaking framing in user-facing copy (2026-09-06 audit + sweep).** Uniscope *recommends / connects you with / helps you find* mentors — it does not "match" two people. Banned in shipped strings: "matching", "get matched", "strategic matching", "matchmaking", "your match", "perfect match", "swipe", "compatibility". Use "recommend", "suggest", "connect with", "find the right mentors". Fixed this pass: `welcome_screen.dart` carousel ("Strategic Matching" → "The Right Mentors"; "Get matched with mentors…" → "Find mentors…"), `aspirant_onboarding_screen.dart` + `mentor_onboarding_screen.dart` step subtitles ("For regional mentor/aspirant matching." / "Used for mentor matching." → "Helps us suggest mentors from your region." / "Used to recommend relevant mentors." / "Helps aspirants from your region find you."), `profile_home_screen.dart` completeness nudge ("helps mentors and matching find you" → "helps the right mentors find you"), `web/app/privacy/page.tsx` ("match students with the right mentors" → "connect students…"). Benign technical uses left alone: filter empty-states ("No mentors match these filters"), regex `.match()`, go_router `matchedLocation`, the data-import fuzzy college-name matcher, and code comments like "matches the reference".
- **Mentor availability is stated intent, NOT real-time presence — and the app must never imply otherwise.** `UserProfile.isMentorAvailable` is the mentor's own "Accepting call bookings" switch. It gates **only** AUDIO_CALL booking; it never hides a mentor from discovery (they stay listed and chat-reachable either way), and it says nothing about whether their app is open. Real presence was **deliberately not built**: a call is already asynchronous (request → mentor accepts → connect later), mentors are medical students who'd read as offline most of the day, and a green "online" dot the app can't back becomes a refund ticket when a paid call no-shows. Consequences to preserve: (1) never label this "online"/"offline" — the UI says "Accepting calls" / "Chat only", and the shared widget is `CallAvailabilityChip`, deliberately not a bare status dot; (2) availability **auto-expires after 24h** (`AVAILABILITY_TTL_HOURS`, `availabilitySetAt`) so a forgotten toggle can't advertise a stale promise; (3) every read path — mentor list, mentor detail, the booking gate, and the mentor's *own* profile switch — must go through `isCallAvailable()` in `modules/mentors/availability.ts`, because if the mentor and the student disagree about bookability that's a support ticket. **Turning the switch ON** has three gates in `UsersService.updateProfile`: MENTOR role, `verificationStatus === VERIFIED`, and — for mentors verified from 2026-09-03 on (`UserProfile.mustReviewCollege`) — having posted a `Review` of their own college (see the `university-reviews` module section for the full college-review-gate design). (Historical note: this used to also flag that Stream Chat's "last seen" was a separate signal from the chat vendor — no longer applicable now that chat has its own Postgres+Realtime backend with no presence feature at all, see "Chat architecture" below.) If the product ever moves to on-demand/instant calls, revisit — that's the case where real presence starts earning its cost.
- **Audio call billing:** fixed pre-paid slots only — 6/10/20 min (`CreateSessionDto.CALL_SLOT_MINUTES`; shortest slot was 5, changed to 6 2026-09-02 per explicit client request — the no-show grace period and overrun extension both derive from this constant, so both followed automatically, no separate change needed), not live per-minute metering. Student picks a slot at booking; balance is checked and a `WalletHold` placed **at booking time**, before the mentor ever sees the request.
  - **Call-request "When?" step (2026-09-06, client-approved via 4 mockups):** the call-request sheet now asks *when* to connect as well as *how long*. Four choices: **Instant connect** (the original flow — connect once the mentor accepts), one row per **mentor free-window** (the mentor's own `UserProfile.availableDays` / `kTimeSlots` picks, e.g. "Morning · 6 AM – 12 PM"), and **Custom time** (a day-and-slot picker — today + next 2 days, 30-min slots, the mentor's usual windows expanded + tagged "usually free", every other day-part collapsed to a "Show N times" header, past slots dropped on Today). **Scope decision — "hint only", per explicit client answers:** the chosen time is stored as `Session.requestedFor` (nullable `DateTime`, null = Instant) and shown to the mentor on the request; **nothing is reserved, no reminder job, and the hold / no-show / billing / connect logic is all completely unchanged** — the mentor still drives the connect. Duration (6/10/20) stays a separate pick in the same sheet; the hold still goes on at booking. Backend bounds `requestedFor` to now … now + 4 days (`SessionsService.create`, 400 otherwise). `SessionResponse` also gained `mentorAvailableDays` so the sheet's quick-picks render straight from a session row with no extra fetch. Mobile: `call_time_windows.dart` (day-part model + `kTimeSlots`→hour mapping + hand-rolled `friendlyCallTime`/`clockLabel` — no `intl` dep), `custom_call_time_screen.dart` (the picker, pushed as a `MaterialPageRoute` from inside the sheet, pops a `DateTime`), redesigned `_CallRequestSheet` in `call_request_sheet.dart` (`showCallRequestSheet` gained a `mentorWindows` param — passed from mentor detail / session row / chat header). Backend live-tested (valid future persists+echoes, Instant→null, past/>4-days/non-ISO all 400); mobile `dart analyze` clean, not click-tested (browser pane can't drive the Flutter canvas). **Not built:** real scheduled appointments / reminders / a reworked no-show sweep keyed off `requestedFor` — deliberately deferred (revisit if the product moves to on-demand/instant calls).
  - **Mentor free-time picklist stays 6 four-hour blocks; the call-request sheet is what splits into half-hours (2026-09-06, settled after two follow-up client requests).** `kTimeSlots` (`profile_options.dart`) — the mentor's own "Preferred Timing" onboarding picklist that both the aspirant's "preferred mentorship timing" step and the call-request quick-picks read — went through a same-day round-trip: uneven 4 windows → 6 uniform 4-hour blocks ("Late Night (12 AM - 4 AM)" … "Night (8 PM - 12 AM)") → briefly generated as 48 half-hour options → **reverted back to the 6-block `const` list**, per explicit client correction ("keep the existing available slots which the mentor gave during account creation… if we click on it it should show every four hour slot split into every 30 mins"). So the mentor's *stored* availability granularity is unchanged (still 6 blocks, still what onboarding collects), and the half-hour granularity lives entirely in the **call-request "When?" step** instead: `call_request_sheet.dart`'s mentor-free-window rows are now expandable — tapping one reveals a `Wrap` of half-hour chips for that block via two new `call_time_windows.dart` helpers, `windowAnchorDay` (today if the block hasn't fully passed, else tomorrow — same rule `nextOccurrenceOfWindow` already used) and `halfHourSlotsInWindow` (every 30-min slot in `[startHour, endHour)`, past ones dropped on today), and picking one sets the booking's `requestedFor` to that exact time. `startHourForTimeSlot`/`mentorFreeDayParts` are back to the original exact-string switch/equality match (no regex parsing needed now that `kTimeSlots` reverted to the block-label format). The old `_WindowChoice` variant of the sheet's internal `_WhenChoice` was removed — a picked half-hour slot is just a `_CustomChoice`, same as one picked from the full "Custom time" day picker, since booking doesn't care which UI path produced the `DateTime`. `custom_call_time_screen.dart` (the separate full "Custom time" picker) is untouched — it already had its own equivalent 4-hour→30-min expansion pattern, just not surfaced on the quick-pick rows until now. `web/lib/options.ts`'s `TIME_SLOTS` remains out of sync with mobile in shape (web still has the original 4 uneven windows) — not touched here. `flutter analyze` clean project-wide; not click-tested (browser pane can't drive the Flutter canvas). **Copy/layout tweaks (2026-09-06, per screenshot):** the collapsed window row's hint says "Mentor · tap to pick a time" (was "…a half-hour slot" — "half-hour" read as if it constrained call length). The "How long?" slot cards' "N Uniminutes" sub-label is now `fontSize: 10` inside a `FittedBox(scaleDown)` with tighter button padding, so "10 Uniminutes" no longer wraps to a "…utes" orphan in the narrow 3-across card.
  - **Two preferred times (2026-09-06, per request):** the aspirant can now offer the mentor **up to 2** times instead of one. Backend: `Session.requestedForAlt DateTime?` (migration `20260906180000_add_session_requested_for_alt`, applied to prod) — second option, same now…now+4d bound as `requestedFor`, ignored unless `requestedFor` is also set; `CreateSessionDto.requestedForAlt`; `SessionResponse.requestedForAlt`; validator factored into `boundRequestedTime(value, field)`. Live-tested 6/6 (both persist+echo, alt-without-primary → null, alt >4d → 400). Mobile: the sheet's `_when` sealed choice (`_Instant`/`_CustomChoice`) is gone — replaced by `final List<DateTime> _times` (empty = Instant, 1–2 = offered). Tapping a half-hour chip / Custom slot toggles it into `_times` (cap 2, a 3rd bumps the oldest); `_PickedTimesBar` shows the picks as removable chips under the "When?" section ("Offering 2 times — the mentor picks one"). `_CallRequestResult` carries `List<DateTime> times`; `showCallRequestSheet` sends `requestedFor`/`requestedForAlt` and the snackbar says "you offered 2 times". `Session.requestedForAlt` (`sessions_api.dart`) + `session_list_screen.dart` show it: aspirant row "Call requested · 2 times", the detailed card "Requested for X or Y". `dart analyze` clean (unrelated concurrent WIP error in `edit_profile_screen.dart` left alone). **Then (2026-09-07, per screenshots):** the sheet is now a `ConsumerStatefulWidget` and shows "N Uniminutes available" (`walletBalanceProvider`) next to the "How long?" label. The mentor's pending-call card in `session_list_screen.dart` distinguishes an **instant** request (`requestedFor == null`): a ⚡ "Instant request — Accept connects the call now. Make sure you can talk." line, and the Accept button reads "Accept & join" (was already `_acceptAndMaybeJoin` → `/call/:id`, just unlabelled). Report sheet reasons (`reports_api.dart`): "Spam" → "Spam/inappropriate content" (still `SPAM`); the `inappropriate` case renamed to `callDropped` = "Call ended before the booked slot" → `CALL_DROPPED` (the backend enum member that routes to the manual admin refund review).
  - **Mentor confirms a concrete 30-minute slot on accept (2026-09-08, client-approved via mockup — "the mentor is the scheduler; mentors are busy and must confirm the timing").** This is the piece the "hint only" note above deliberately deferred, now built because the client made it a hard requirement. The aspirant's `requestedFor` / `requestedForAlt` are now **soft anchors** — on accept the mentor picks any 30-min slot in the 4-hour block around one of them (busy at 4:30, takes 5:00). **New `Session.confirmedFor DateTime?`** (migration `20260908105911_add_session_confirmed_for`, applied to prod, plain additive `ADD COLUMN` — verified live). **Backend:** `AcceptSessionDto { confirmedFor?: string }` (new `dto/accept-session.dto.ts`); `SessionsService.accept(id, mentorId, dto)` → `resolveConfirmedSlot()` backstop-validates (30-min boundary, future, ≤ now+5d, within ~4h of an anchor; rejects a `confirmedFor` on an Instant request) then writes it; `SESSION_ACCEPTED` push body becomes "Your mentor confirmed {IST time} for your call. Join within that 30-minute window." (module-level `fmtIst` hoisted out of `create()` and shared); `SessionResponse.confirmedFor` added. **Sweep change (required):** `sweepCallNoShows` grace clock now runs from `max(respondedAt, confirmedFor)` — a call confirmed for a slot days ahead must survive untouched until its slot. **Mobile:** `Session.confirmedFor` + `SessionsApi.accept(id, {confirmedFor})`; new `confirm_call_time_sheet.dart` (`showConfirmCallTimeSheet` — per-anchor 30-min chip strips via `slotsAroundAnchor` in `call_time_windows.dart`, reusing `halfHourSlotsInWindow`; the anchor slot marked with a warning dot); wired into `_SessionActions._acceptAndMaybeJoin` + `_DockRow._acceptAndJoin` (Accept button → "Confirm a time" for a scheduled request; Instant still "Accept & join" and drops straight into the call, scheduled does NOT); `sessionStatusView` shows "Confirmed · {time}" for `ACCEPTED` with a `confirmedFor` (replaces the vague "Ready" / "join now"); `CallRequestWatcher` gained a `dueSoon` gate (`confirmedFor == null || within 15 min`) so a scheduled-accepted call doesn't yank the aspirant into the call overlay days early, and the 4s fast-poll only runs for due calls. Backend live-verified against the real DB: accept + valid slot (either anchor's block) persists `confirmed_for` and the notification carries the IST time; non-30-min-boundary / far-from-anchor / past / instant-with-confirmedFor all 400, and a 400 leaves the request PENDING; plain accept with no `confirmedFor` still works (legacy). `tsc` + `dart analyze` clean. Not click-tested on device (browser pane can't drive the Flutter canvas). **Mobile confirm-sheet UX simplified 2026-09-11, per explicit request ("mentor will accept the time booked by student, not pick between slots — give a custom time confirmation same as student has").** The per-anchor 30-min chip grid (`slotsAroundAnchor`, now deleted from `call_time_windows.dart` — it was only ever used here) is gone. `confirm_call_time_sheet.dart` now lists the aspirant's offered time(s) **exactly as requested** — accepting one confirms that precise moment, not an adjustable nearby slot — plus a "Suggest another time" row that opens `CustomCallTimeScreen`, the **same** day-and-slot picker the aspirant used to request the call in the first place (no mentor-specific `mentorWindows`/`mentorName` passed, so it renders as the plain generic picker). Backend `resolveConfirmedSlot`'s validation (30-min boundary, future, ≤ now+5d, within ~4h of an anchor, instant-request rejection) is **unchanged** — a time picked straight off an anchor row trivially satisfies "at/near the anchor", and a `CustomCallTimeScreen` pick is itself always on a 30-min boundary, so nothing server-side needed to change. New file-local `_TimeOption` widget (styled to match `call_request_sheet.dart`'s `_WhenOption`) replaces the deleted `_GroupLabel`/`_SlotChip`. `flutter analyze` clean project-wide (one pre-existing, unrelated `test/flow_test.dart` failure from the 2026-09-10 UPI-ID feature, out of scope). Not device-tested (browser pane can't drive the Flutter canvas). **Real bug found + fixed 2026-09-11: a scheduled/confirmed call connected immediately on "Join," not at the agreed slot.** Root cause: `SessionStatus.ACCEPTED` is join-eligible purely by status — neither the "Join Call" button (`session_list_screen.dart`'s `_SessionActions`, and `active_session_dock.dart`'s dock) nor the backend's `getCallCredentials` (token issuance) ever checked `Session.confirmedFor` against the current time, so tapping Join on a call scheduled hours/days out issued a real Agora token and connected right away — the scheduling only ever controlled *when the notification said* to join, not when joining actually worked. **Backend (defence-in-depth, the real fix):** new `CALL_EARLY_JOIN_WINDOW_MINUTES = 5` constant; `getCallCredentials` now 409s `"This call is scheduled for {IST time}. You can join starting 5 minutes before."` whenever `confirmedFor` is set, status isn't already `IN_PROGRESS`, and now is more than 5 min before it. An instant request (`confirmedFor == null`) is unaffected. **Mobile (UX — stops the dead-end tap before it reaches the backend):** new `isScheduledCallJoinableNow(confirmedFor, {alreadyLive})` + `kCallEarlyJoinWindowMinutes` in `call_time_windows.dart` (mirrors the backend constant). Both Join surfaces now check it: not-yet-joinable renders as a muted "{scheduled time}" button/chip (schedule icon, `AppColors.textMuted`) instead of an enabled "Join"/"Join Call," and tapping it shows a snackbar with the scheduled time and the 5-minute window rather than opening the call screen. `tsc` + `flutter analyze` clean project-wide (the one pre-existing `test/flow_test.dart` failure is the unrelated 2026-09-10 UPI-ID gap, untouched). Not live-tested against the real DB/device this pass — logic mirrors the already-verified `resolveConfirmedSlot` time math, but worth a real confirm-then-early-tap run next time this path is touched.
    - **Two follow-up bugs the client hit on device, both fixed the same day (2026-09-08):** (1) **the student's app auto-dialled the moment the mentor confirmed a slot** (should wait for the slot). Root cause: `push_service.dart`'s foreground `onMessage` runs the deep-link handler *on receipt*, and a scheduled `SESSION_ACCEPTED` carried the same `metadata.sessionType: 'AUDIO_CALL'` as an instant one → auto-routed into `/call/`. Fix: `accept()` now sends `sessionType` ONLY for an instant accept; a scheduled accept sends `{ sessionId, confirmedFor }` instead (the student just gets the "confirmed for {time}" notice). Mobile handler also bails on `SESSION_ACCEPTED`+`AUDIO_CALL` when `confirmedFor` is > ~2 min away (belt-and-suspenders). (2) **the mentor got no alert when the call actually started** — nothing pinged the other party when the first party joined. Fix: **`SESSION_STARTING` is now live**, two triggers: (a) `confirmJoined`'s first-join branch pushes `SESSION_STARTING` (`sessionType: 'AUDIO_CALL'` → routes into the call) to whichever party isn't on the call yet; (b) new `@Interval` `remindScheduledCallsStarting` (shares the 30 s no-show-sweep cadence) pushes "Call starting soon" to **both** parties ~2 min before `confirmedFor`, exactly once (`Session.startingNotifiedAt` column, migration `20260908174834_add_session_starting_notified_at`, applied to prod; claim-then-notify guard so the two dev processes / any redeploy can't double-fire). `push_service.dart` gained a `SESSION_STARTING` case (→ `/call/:id`). Live-verified against the real DB: scheduled accept → metadata has `confirmedFor`, no `sessionType`; instant accept → still `sessionType: 'AUDIO_CALL'`; first-join → other party gets `SESSION_STARTING` + `sessionType`; the sweep → both parties get "Call starting soon" once and `starting_notified_at` is set. Both `sessions.service.ts` hunks staged surgically around concurrent `startChatWithStudent` WIP.
    - **Still deferred:** a "Suggest another day/time" free picker that counter-proposes outside the aspirant's windows (needs a student re-confirm round-trip). Also worth revisiting: `CALL_GRACE_FRACTION` (50 % of slot → 3 min for a 6-min slot) may be too tight for a *scheduled* call where people join a few minutes around the agreed time, vs the instant model it was tuned for.
    - **Double-booking guard (2026-09-08, per client question "can the mentor see that a slot is already taken?").** `SessionsService.accept` now rejects a `confirmedFor` whose `[start, start + thisSlotMin)` interval overlaps another of the mentor's still-live confirmed calls (`ACTIVE_STATUSES`, `type=AUDIO_CALL`, `confirmedFor` within ±20 min then exact interval-overlap check in JS using each call's own `callSlotMinutes`). 409 `"You already have a call confirmed around {IST time}. Pick a slot that doesn't overlap it."` This is the authority — a stale sheet or a second device can't get past it. **Mobile:** `confirm_call_time_sheet.dart` greys the clashing 30-min chips (`_slotBusy` vs a `List<BusyInterval>` = `(start,end)` records) and can't select them, with an "Greyed slots overlap a call you've already confirmed." hint; `mentorBusyIntervals(ref.read(sessionsListProvider)…, excludeSessionId:)` (top-level in that file) builds the list from the sessions already in memory — no extra fetch — and both `_acceptAndMaybeJoin` (`session_list_screen.dart`) and `_DockRow._acceptAndJoin` (`active_session_dock.dart`) pass it. **Not built:** aspirant-side greying in the *request* picker (`custom_call_time_screen.dart`) — would need a `GET /mentors/:id/busy-slots` endpoint; deferred, the backend reject + the mentor-sheet grey are the real protection. Live-verified against the real DB: mentor confirms call A @ 13:00 (6-min slot) → 200; a second student's call confirmed @ 13:00 → 409 with the IST-time message; @ 13:30 (clear) → 200. **`_slotBusy`/`mentorBusyIntervals` survived the 2026-09-11 confirm-sheet rewrite above** — greying now applies per-exact-offered-time (a `_TimeOption` row shown struck-through + "Busy — you've already confirmed a call then", not tappable) instead of per-30-min-chip-in-a-grid, same underlying check. A time picked via the new "Suggest another time" (`CustomCallTimeScreen`) is **not** busy-checked client-side (that screen has never had busy awareness — same pre-existing gap noted above) — the backend 409 is still the real protection there, surfaced through the same accept-error path.
  - **Sessions tab reads like a chat list — Phase 1: last-message preview (2026-09-08, client-approved via mockup — WhatsApp/Instagram style).** The grouped Sessions rows (`_AspirantMentorRow` / `_MentorStudentRow`) used to lead with a *status string* (`sessionStatusView` label + a state dot). Now, when the latest thing that happened on a relationship is a chat message, the subtitle is the **message text** — prefixed `"You: "` when the viewer sent it — with a compact relative time (`chatTimeLabel`: `now` / `8m` / `2:30 PM` / `Yesterday` / `Mon` / `Sep 3`) pinned right, and no dot. **The status label + dot fallback is gone for both roles (2026-09-09, in two steps — "remove the status showing in the sessions for mentor … we can go with the message shown there", then "Remove it everywhere — both roles name-only unless there's a message").** `_rowSubtitle` now returns an empty `text` whenever `!hasMsg`; `_AspirantMentorRow` and `_MentorStudentRow` both wrap the `SizedBox(height: 2)` + `_SubtitleRow` in `if (sub.text.isNotEmpty)`, and `_SubtitleRow` also early-returns `SizedBox.shrink()` for an empty result. So a row is **name-only** unless there's a chat-message preview — a pending call already shows Accept/Reject inline and a bland "Ready"/"Chat" told nobody anything. `sessionStatusView` is untouched and still drives the `_SessionActions` in-card chip. New shared `_rowSubtitle(latest, viewerIsMentor:)` + `_SubtitleRow` widget in `session_list_screen.dart`; the "me" check is `lastMessageSenderId == (viewerIsMentor ? mentorId : aspirantId)` — no `ref`/userId threading, since each row type is only ever shown to its own role. **Backend:** `SessionResponse` gained `lastMessageText` / `lastMessageAt` / `lastMessageSenderId` (`LastChatMessageLite`), filled only by the list path (`SessionsService.findAll` — one query for the CHAT sessions' channels on the page, one `distinct: ['channelId']` newest-message query, `O(1)` extra queries, no N+1, no migration); `GET /sessions/:id` leaves them null. Mobile `Session` model mirrors them. **Known gap:** the list is still cursor-paginated by `requestedAt` and rows aren't re-sorted by `lastMessageAt` client-side yet — a mentor with an old session but a fresh message stays where recency-by-request put them (fine on the default single ~20-row page; a proper server sort is Phase 2 territory). **Phase 2 (not built): unread counts** — needs a `ChatChannelRead {channelId, userId, lastReadAt}` model + a mark-read call; the count would ride as a badge on the existing chat icon (call icon stays), unread rows bold with a green time. Live-verified against the real DB: aspirant msg → row carries the text + aspirant senderId + a timestamp; mentor reply → updates to the mentor's text/senderId; an AUDIO_CALL row → `lastMessageText: null`.
  - **Request type + time made explicit on every mentor-facing surface (2026-09-08, per request).** A call request now says *what kind* it is (instant vs. scheduled) and *when* on all three "incoming" surfaces, not just the full history card:
    - **Push** (`SessionsService.create`): instant → title `New instant call request`, body `A student wants a {n}-min call now — accept to connect.`; scheduled 1 time → `A student booked a {n}-min call for {IST time}.`; scheduled 2 times → `A student booked a {n}-min call — {t1} or {t2}.` Times formatted via an inline `fmtIst` (`Intl.DateTimeFormat('en-US', … timeZone: 'Asia/Kolkata')` → e.g. `Tue, Sep 8, 4:17 AM`, matching the app's own `friendlyCallTime` style). Live-tested against the real backend + DB on a throwaway aspirant/mentor pair — all three `Notification.body`/`title` rows verified, test data cleaned up.
    - **`ActiveSessionDock`** mentor pending `_statusText` (was flat `New call request`): instant → `Instant call request · connect now`; 1 time → `Call request · {friendlyCallTime}`; 2 times → `Call request · {clockLabel a} or {clockLabel b}`. (Added `call_time_windows.dart` import.)
    - **`_lastActivityLabel`** (mentor + aspirant Sessions row subtitle): instant → `Instant call requested · connect now` (was bare `Call requested`); 2 times → `Call requested · {clockLabel a} or {clockLabel b}` (was `· 2 times` — now shows the actual times).
    The full request card (`_SessionActions` `showLabel` block — ⚡ "Instant request — Accept connects the call now…" / 🕐 "Requested for X or Y") already carried this and is unchanged. `tsc` + `dart analyze` clean on touched files.
  - **Reserve-and-surface (2026-09-06):** the hold is a *reservation*, not a debit — `balanceMinor` is untouched at booking; the hold only reduces *available* balance so the same Uniminutes can't be double-booked, and converts to a real `SESSION_DEBIT`/`SESSION_CREDIT` pair only when **both** parties confirm join. That was invisible to users before, so `GET /wallet`'s `WalletResponse` now also returns `reservedMinor` (Σ of every ACTIVE `WalletHold` on the wallet — one aggregate in `WalletService.getBalance`, same query shape as the pre-existing `getAvailableBalanceMinor`) and `availableMinor` (`balanceMinor − reservedMinor`). Mobile `Wallet` model mirrors it (`reservedMinor`, plus `reservedUniminutes`/`availableUniminutes` getters, parses `reservedMinor` defaulting 0). Surfaced: (1) wallet screen shows "N Uniminutes reserved for a pending call · M available" under the balance when `reservedMinor > 0`; (2) the "Request a call" sheet copy is now "N Uniminutes reserved now — charged only when the call connects, released if it doesn't"; (3) the pre-booking balance gate in `session_chat_screen.dart` + `session_list_screen.dart` now checks `availableUniminutes` (matching the backend's `placeHold` → `getAvailableBalanceMinor` check exactly) instead of raw `balanceUniminutes`, so a user with a pending hold gets the low-balance sheet client-side instead of a server 409; (4) `showLowBalanceSheet` explains the held portion when present. Live-verified against the running backend + real DB: no hold → `reserved 0, available == balance`; with a real 10-Uniminute ACTIVE hold → `reserved 10000, available balance−10000`; released → back to `0`.
- **Free tier — REMOVED for calls (2026-09-11, explicit client instruction: "there is no free call feature — only users with Uniminutes can call").** Chat is still unconditionally free, always, with no limit or counter — `SessionsService.create` sets `ratePerMinuteMinor: 0` for every CHAT session (see the "Never advertise that chat is free" rule above). But the free-call-minutes tier that let a call through with **0 Uniminutes** — root-caused 2026-09-11 as `SessionsService.create` computing `isFreeSlot = profile.freeCallSecondsRemaining >= slotSeconds` and, when true, skipping `WalletService.placeHold` entirely — is now **gone**: `create()` no longer checks `freeCallSecondsRemaining` at all, so every AUDIO_CALL, no exceptions, must place a real `WalletHold` for the full slot cost before the mentor ever sees the request; `placeHold` throwing on insufficient balance (and the orphaned session row being deleted) is the enforcement, same as it already was for a paid slot. `confirmJoined`'s billing-settle and `resolveNoShow`'s aspirant-no-show branches both kept their now-unreachable free-tier "no hold" paths (with a warning log added) purely to close out any legacy session that was already mid-flight when this landed — nothing new can ever reach them. `UserProfile.freeCallSecondsRemaining`/`freeChatsRemaining` columns are left in the schema (harmless, admin-editable via `PATCH /users/:id`) but are now dead data for both chat and calls — no code path reads either to gate anything. No mobile change needed — the client-side balance gates (`availableUniminutes` checks before showing the call-request sheet, `showLowBalanceSheet`) were already correct; they just used to be bypassable server-side by the free-tier shortcut, which is what this closes. Backend `tsc` clean; not yet live-tested against the real DB in this pass — the change is a straightforward removal of a bypass branch (same `placeHold` code path already exercised for every paid call).
- **Call connect confirmation:** billing only fires once **both** the aspirant's and mentor's clients independently report "I joined" (`POST /sessions/:id/call/joined`) — an interim fraud-resistance measure until a real Agora server-side "user joined channel" webhook is configured (needs additional Agora Console setup the user hasn't done yet). Neither party alone can trigger a charge.
- **Call overrun:** hard cutoff at slot end, with a "continue?" popup offering another fixed +5 min block (`POST /sessions/:id/call/extend`), billed the same way as the original booking.
- **Call no-show / grace period:** a call still in `ACCEPTED`/`RINGING` (mentor accepted, but the two sides haven't both confirmed "joined" yet) is auto-resolved once **half the booked slot's minutes** have passed since the mentor's accept (`respondedAt`) — 5-min slot → 2.5 min grace, 10-min → 5 min, 20-min → 10 min, always exactly 50%, no special-casing per slot size (money is minor-unit integers throughout, so the fractional 2.5 min on a 5-min slot is still an exact 2500-minor fee, never a rounding problem). `SessionsService.sweepCallNoShows` (`@Interval`, every 30s) does the sweeping; `resolveNoShow` does the actual settling, status → `FAILED`. Money only moves one way: **aspirant no-show** (mentor showed, aspirant didn't) charges the aspirant exactly the grace-period-equivalent fee and credits the mentor the same amount in full (same zero-margin same-amount pattern as every other session billing path) — `endReason: 'ASPIRANT_NO_SHOW'`. A free-tier booking (no `WalletHold`) has no mentor payout to fund on a no-show either, same as a normal free-tier connect — the aspirant's free-call-seconds are decremented by the grace period instead, so it's still not consequence-free for them. **Mentor no-show** (aspirant showed, mentor didn't) and **mutual no-show** (neither showed) both fully release the hold — nobody is charged, mentor gets nothing — `endReason: 'MENTOR_NO_SHOW'` / `'NO_ANSWER'`. Mobile: the shared `sessionStatusView` (see the "Session-status labels" bullet below) shows a role-specific label for each no-show `endReason` (aspirant-no-show → aspirant sees "Missed" / "You didn't join in time", mentor sees "Aspirant no-show"; mentor-no-show → the mirror) instead of a generic "Failed" — **no "charged"/"paid" wording in any status label**, that's the wallet/ledger/notifications' job. Verified live against the real DB for both the aspirant-no-show (fee+credit landed correctly) and mentor-no-show (hold released, zero charged) cases.
- **Mentor-side call drop:** still bills the aspirant in full — **never** an automatic refund. The aspirant gets a Report action; an admin manually reviews and can issue a `REFUND` ledger entry via `PATCH /reports/:id/resolve`. See `ReportsService.resolve`.
- **Support chat:** a persistent, session-independent chat channel per user with a fixed "UniScope Support" identity (`SUPPORT_ACCOUNT_ID` in `chat.service.ts`), lazily provisioned via `GET /chat/support/messages`. Reached from the "Need Help?" entry points via the new `/help` landing screen (see Help & Support below). **Staff reply path now exists (2026-09-06):** ADMIN-guarded `AdminSupportController` (`backend/src/modules/chat/admin-support.controller.ts`, `/admin/support`) — `GET /admin/support` lists every SUPPORT channel with ≥1 message (user name/uniqueId/role, last-message preview, `awaitingReply` flag), `GET /admin/support/:userId/messages` reads a thread (cursor-paged, same `before` param as the user's own GET), `POST /admin/support/:userId/messages` posts a reply as the `SUPPORT_ACCOUNT_ID` sentinel through the normal `ChatService.sendMessage` path. `ChatService` gained `listSupportChannels()` / `getSupportThreadForUser()` / `sendSupportReply()`. Also fixed a latent bug in `ChatService.notifyOthers`: the SUPPORT-channel recipient ternary was inverted (a real user's message self-notified them; a sentinel reply notified nobody) — now a staff reply correctly notifies `channel.supportUserId` (in-app + push) and a user→support message notifies no one. Admin UI: `/dashboard/support` ("Support" nav item, `lifebuoy` icon) — two-pane inbox (`SupportInbox.tsx`): conversation list with awaiting-reply dots + a Refresh button, thread view with bubble log + "Load older messages" + a reply box (⌘/Ctrl+Enter to send). Backend live-tested end to end against the real DB (user send → admin list shows `awaitingReply` → admin reply as sentinel → user sees it + gets a notification → idempotent re-POST returns the original → non-admin 403); admin page verified rendering the real thread. **Still no auto-close/assignment/canned-replies** — it's a plain shared inbox, every admin sees every thread.
- **Aspirant Sessions tab shows one row per mentor, not one per session (2026-09-02, explicit product decision).** `session_list_screen.dart`'s `_groupAllSessionsByCounterpart` collapses every chat/call session with the same mentor into one relationship — a long-running mentor relationship no longer reads as a scrolling wall of near-duplicate cards. **Mentor accounts were deliberately unaffected here originally** — a mentor serves many different students and still needs each one's individual request visible separately (`_groupActiveSessions` → `_SessionCard`/`_MergedSessionCard`, the pre-existing behavior). **Reversed 2026-09-07 per explicit follow-up** — see the "UX-fix batch" bullet near the end of the "What's built" section: the mentor tab now groups by student too (`_groupAllSessionsByStudent` → `_MentorStudentRow`), `_groupActiveSessions`/`_MergedSessionCard` are deleted, and per-session Accept/Reject/Join still work exactly as before via `_SessionActions`. Paired with this: `ActiveSessionDock` (the pending-call banner) is no longer global for aspirants — `MainShell` only mounts it for mentor accounts now; an aspirant's own pending-call status shows scoped inline inside that specific mentor's `SessionChatScreen` (`ActiveSessionDock(mentorId: ...)`) instead of floating over every tab regardless of which mentor it's for. Mentors keep the global one, same reasoning as above. **Reversed again 2026-09-11, per explicit request ("remove the dock for the mentor — all the call details are going to work same as student screen same as inside the chat"): the mentor's global dock is gone too, both roles now work identically.** `MainShell` no longer mounts `ActiveSessionDock` at all (the `Consumer`/`isMentor` block is deleted, along with the now-unused `flutter_riverpod`/`auth_controller`/`active_session_dock` imports) — `CallRequestWatcher` (polling + the aspirant auto-navigate-into-call) is untouched, it renders nothing and isn't the dock. `ActiveSessionDock` gained an `aspirantId` param alongside `mentorId` (filters on whichever is passed; passing neither shows every counterpart's active session, which nothing does anymore but is kept as a graceful default rather than throwing). `SessionChatScreen` now always renders the scoped dock, not just `if (isAspirant)`: `ActiveSessionDock(mentorId: isAspirant ? _session!.mentorId : null, aspirantId: isAspirant ? null : _session!.aspirantId)` — so a mentor's pending/live call with a specific student now shows exactly the same way a student's always has, inline inside that one chat, and nowhere else. A mentor still sees every pending request at a glance on the Sessions tab itself (`_MentorStudentRow`'s inline Accept/Reject via `_pickPrimaryCall`) — unaffected, that was never the dock. `flutter analyze` clean project-wide (one pre-existing, unrelated `test/flow_test.dart` failure — `_FakeUsersApi.updateProfile` missing the `upiId` param the 2026-09-10 UPI-ID feature added — left as-is, out of this task's scope). Not device-tested (browser pane can't drive the Flutter canvas).
  - **Row redesign (2026-09-03, explicit client request): the aspirant row is now a flat WhatsApp-style list tile, not a card with stacked action buttons.** `_AspirantMentorRow` renders avatar · mentor name · one status subtitle (`_lastActivityLabel`) · a trailing **call icon then chat icon** (`_RowIconButton`), rows split by a hairline `AppColors.border` divider, no `AppCard` chrome. Gone from the row: the inline `_SessionActions` (per-session `Cancel`/`Join Call`/`Open Chat` pills) **and** the "+N earlier" history tap target — per the client, history belongs *inside* the chat, not piled on the Sessions tab. It's still fully reachable: `SessionChatScreen` keeps its history action (`showMentorSessionHistory`, aspirant-only) and the scoped `ActiveSessionDock` covers a live/pending call's Join/Cancel. **Tap targets:** avatar → `/mentors/:id` (mentor detail); anywhere else on the row, or the chat icon → the chat thread (`startChatWithMentor`, find-or-create); call icon → the balance-gated "Request a call" sheet (shares `_minCallSlotUniminutes` with `SessionChatScreen`). **Call-icon colour reflects bookability:** green (`AppColors.primary`) when the mentor can be booked for a call right now, grey (`AppColors.textMuted`) — same glyph — otherwise. Backed by a new `SessionResponse.mentorIsAvailable` (backend `session-response.ts`) computed via the canonical `isCallAvailable(session.mentor.profile)` — the same single-source-of-truth gate the mentor list / detail / booking endpoint use, staleness-expiry included (`SESSION_WITH_NAMES_INCLUDE` now also selects the mentor profile's `isMentorAvailable`/`availabilitySetAt`). Mobile `Session.mentorIsAvailable` mirrors it. This is a bookability tint, **not** presence — no "online/offline" label, consistent with the mentor-availability decision above. `showMentorSessionHistory`, `_SessionCard`, `_SessionActions`, `_MergedSessionCard` all still exist and are still used by the mentor tab and the history sheet — only `_AspirantMentorRow` changed. **Note (2026-09-05): a concurrent session independently built an equivalent `mentorIsAvailable` implementation** (see `SessionResponse.mentorIsAvailable` further down this file) which is what's actually live — this bullet documents the row-redesign UI, which is this session's own work and additive on top.

## What's built (backend modules, all tested live against the real Supabase DB + real Razorpay/Agora APIs — not just unit-style tests)

- `auth` — Phone OTP (Twilio/mock) + JWT
- `users` — profile CRUD, role transitions
- `universities` — listing/search (mobile Colleges tab wired to real data, not hardcoded placeholders). Two real bugs found and fixed 2026-09-02 while chasing a user report that Discover's State filter was missing colleges: (1) `UniversitiesApi.list()` (mobile) looped cursor-paginated `GET /universities` up to a 40-page/2,000-row safety cap — a leftover from when the catalogue was ~830 medical colleges, since grown to ~10,500 across all streams, so Discover silently loaded only the alphabetically-first ~19% of the catalogue and every filter (state included) missed everything past that. Fixed by switching to the backend's existing `browse=true` mode (same one the web app's college search already uses) — one uncapped request instead of looping paginated ones; live-verified 10,266 rows in ~3s/~5.5MB vs. the old cap. (2) 81 of ~10,500 `University.state` values were typos/import-junk (`"Orissa"` instead of `"Odisha"`, `"Pondicherry"` instead of `"Puducherry"`, stray PIN-code-looking suffixes like `"Andhra Pradesh-51800"`, etc.) that didn't exactly match any option in mobile's `kIndianStates` picklist, so those colleges were invisible under every specific state selection — see `backend/scripts/fix-university-state-typos.mjs` (already run against production data; safe to re-run, every UPDATE is scoped to an exact old-value match). 38 more rows are `state: "nan"` (bad import artifact) with no city either and generic names — deliberately left alone rather than guessed; needs either better source data or a decision to allow a genuinely-unknown state. **Discover filter rework (2026-09-03, `mobile_flutter`):** the Colleges/Discover tab's filter bottom sheets (`_pickOption` in `university_list_screen.dart`) were a non-scrolling `Column` with no height cap — the full State list (37 entries) and the specialization lists overflowed off-screen with no way to reach them. Rewrote as `isScrollControlled`, 75%-height-capped, internally scrollable, with an optional search box (State + Specialization). Also made Degree/Specialization cascade like the web enrollment forms: Degree options are now `degreesForStream(stream)` (was a fixed `['All','UG','PG']` filtering `University.levels`), shown only once a Stream is picked; Specialization options come from `GET /universities/curated` for that stream+degree (new `UniversitiesApi.curated()` + `curatedCollegesProvider` family + `kCuratedDegreeMapByStream`/`curatedDegreeKey()` in `profile_options.dart`, ported from web `CURATED_DEGREE_MAP_BY_STREAM`), falling back to the flat `kMedicalSpecializations` for Medical degrees with no curated dataset, and the pill hides when a combination has neither. A curated Degree filter also narrows the college list by intersecting with that dataset's college ids (the flat `browse=true` catalogue can't express a postgrad degree like MD/MS or B.Tech). Also ported the web `0900f98` "Rename Medical UG degree to MBBS" to mobile: `kMedicalDegrees[0]` is now `'MBBS'` (not `'UG'`) — flows through `degreesForStream` to the Discover Degree pill, the mentor onboarding wizard's Degree step, and `EditProfileScreen`; `University.levels` still stores the plain `'UG'` tag so MBBS→UG translation happens in the level-scoped college lookup, and MBBS (like web) shows no Specialization pill. `mentor_onboarding_screen.dart`'s `_needsSpecialization` check updated `'UG'`→`'MBBS'` to match. `flutter analyze` clean. Not yet click-tested on a real aspirant account (the Colleges/Discover tab is aspirant-only and the browser-pane can't drive the Flutter canvas).
  - **Removed `type` / `nirfRank` / `mbbsSeats` from `University` entirely (2026-09-05, client decision).** Migration `20260905143000_drop_university_type_nirf_seats` (applied to prod) drops the three columns + their indexes + the `UniversityType` enum. Gone from: the create/update/list DTOs and the `type` query filter; `findOrCreateByName`'s default type; the data-import pipeline's diff/detail/create/update (parser left emitting the now-ignored keys); `seed.ts`. Admin: universities list lost the Government/Private/Deemed/Central filter tabs + the type badge / NIRF·seats line; the row/add forms lost those inputs and gained a Stream field. Mobile: `University` model dropped the fields; college detail's "Type"/"Seats" rows → a single "Stream" row (`_typeLabel` deleted); the Home "Colleges for you" rail lost its NIRF badge/ranking (it's stream-filtered alphabetical now — the list endpoint has no quality signal). `web/` had no references. The card never showed a Private/Govt badge from `type` anyway — the "MEDICAL" chip is `stream`.
  - **Name cleanup (2026-09-05, migration `20260905201500_clean_university_name_import_junk`, applied to prod):** stripped leading admission/institute codes (`100005-KCES'S …`, `3143 …` — 20 rows), collapsed 2+ spaces / trimmed (~194 rows); the single `(Private ) Institute of Opthalmology …` row was fixed by a direct one-row UPDATE (not in the migration). **Still there: ~3,766 ALL-CAPS names** — safe case-folding needs an acronym/roman-numeral allowlist, a separate pass. (The bulk `UPDATE` via the Supabase MCP tool is blocked by a safety classifier — do cleanups as a Prisma migration + `migrate deploy`.)
  - **Three data-cleanup items relayed earlier (2026-09-06) confirmed 2026-09-07 — 2 of 3 were already done, 1 wasn't:** (1) the 5 corrupted `"/Deem ed …"` duplicate university rows are all `is_active: false` — done, confirmed live. (2) The single MD/MS "Respiratory Medicine"/"Tuberculosis & Respiratory Diseases" merge — the exact old name no longer appears anywhere in `Program.specializations`, and `"MD - Respiratory Medicine"` now has 327 rows (a number consistent with a real consolidation) — treated as done; a tiny unrelated residual (`"Respiratory Medicine"` bare ×3, `"MD - Pulmonary/Respiratory Medicine"` ×6) is a minor naming variant, not this item, left alone. (3) The 4 DNB **"(Direct 6 Years Course)"** duplicate specialization pairs were genuinely **not done** — `"Neuro Surgery (Direct 6 Years Course)"` (2 rows) sat alongside `"Neuro Surgery"` (128 rows) as a fully separate, selectable duplicate, same for Cardio Vascular & Thoracic Surgery / Paediatric Surgery / Plastic & Reconstructive Surgery. **Fixed live 2026-09-07** — `array_replace()` on the 6 affected `Program.specializations` rows (verified none already held both variants, so this was a clean rename, not a merge-with-dedup); read back after: zero `"(Direct 6 Years Course)"` strings remain, and every affected row's array element count still equals its distinct-element count (no duplicate entries introduced). No migration file needed — this is a data correction, not a schema change, same class of fix as the earlier state-typo/name-cleanup scripts above.
- `mentors` — discovery/search, eligibility filtering, `{rating, reviewCount}` attached from `reviews`. `MentorResponse.isVerified` (added 2026-09-02) is what the mobile "Verified" badge must gate on — a real bug shipped before this: both mentor-facing screens rendered the badge unconditionally on every mentor, and `MentorsService` never even exposed `verificationStatus`, so every mentor read as "Verified" regardless of actual state. Fixed; discovery/chat-reachability for unverified mentors is unchanged (that's a deliberate, separate decision — see `MentorsService.findAll`'s own comment). Confirmed while fixing it: `PATCH /users/me` already correctly required `VERIFIED` before a mentor could turn ON call bookings (`isMentorAvailable`) — that gate was already real, only the badge was lying.
- `sessions` — full booking lifecycle: request → accept/reject/cancel → (for calls) token issuance → dual-confirm connect → billing → extend → end. `SessionResponse.mentorIsAvailable` (added 2026-09-04) carries the expiry-aware `isCallAvailable(mentor.profile)` on every session row so the aspirant's Sessions list can show a live per-mentor call-availability state without a second request — `SESSION_WITH_NAMES_INCLUDE` pulls the mentor's `isMentorAvailable`/`availabilitySetAt` for this. Mobile `Session.mentorIsAvailable` reads it (defaults false); the "Request a call" icon on `session_list_screen.dart`'s per-mentor row greens on true. (Mobile commit `4df7f71` referenced this field before either side had it — `56a3594` added the mobile model field as a stopgap, this wired the backend.) `Session.requestedFor` (nullable, added 2026-09-06, migration `20260906000000_add_session_requested_for`) + `SessionResponse.requestedFor`/`mentorAvailableDays` back the call-request "When?" step — see the "Call-request "When?" step" bullet under Business model decisions for the full design; `SESSION_WITH_NAMES_INCLUDE` also selects the mentor profile's `availableDays` for the quick-picks.
- `wallet` — balance, ledger, Razorpay topup (order + webhook + direct-confirm fallback), Uniminute conversion, holds. `GET /wallet` returns `balanceMinor` + `reservedMinor` (Σ ACTIVE holds) + `availableMinor` — see the reserve-and-surface note under "Audio call billing".
- `chat` — Postgres + Supabase Realtime Broadcast: session-scoped channels (CHAT-type sessions) + persistent support channel. See "Chat architecture" below for the full design and its known gaps.
- `agora` — RTC token generation (`RtcTokenBuilder.buildTokenWithUserAccount`)
- `reports` — user-submitted reports + admin resolution/manual refund
- `technical-reports` — "Report a technical issue" submissions from the Help Centre (`backend/src/modules/technical-reports/`). Flat bug queue, deliberately **not** part of `reports` (no target, no wallet). `TechnicalReport` model (migration `20260906120000_add_technical_reports`, applied to prod) + `TechnicalReportStatus` enum (OPEN/RESOLVED). `POST /support/technical-reports` (any authed user — `{ message ≤2000, platform?: android|ios|web, appVersion? }`); ADMIN `GET /admin/technical-reports?status=&cursor=&limit=` (cursor-paged, reporter projected as `{displayName, uniqueId, role}` only — never the user id/PII) and `PATCH /admin/technical-reports/:id` (`{ status, adminNote? }` — sets/clears `resolvedBy`/`resolvedAt`). Live-tested end to end against the real DB (create/validation-400/list/status-filter/resolve/reopen/non-admin-403).
- `verification` — mentor ID verification: Supabase Storage upload, admin FIFO queue, signed document URLs, approve/reject
- `reviews` — mentor ratings (`MentorReview`, distinct from the university-scoped `Review` model)
- `notifications` — in-app notifications (durable) + best-effort Firebase push. **Firebase is now genuinely configured** (a real project `uniscope-83eeb` + a real service-account credential, confirmed locally this session via a forced live auth round-trip against Google's servers — not a placeholder anymore, contra the old note below). Whether Railway's production copy of the same credential is set couldn't be confirmed (values are redacted through this integration) — see "What's NOT built yet".
  - **Enum-gap wiring pass (2026-09-08).** Three `NotificationType` values that were declared but never sent are now live, and a few existing types got wired into paths that were silently skipping them. All are **additive `NotificationsService.send()` calls at points the code already executed** — no schema change, no new cron/interval. New shared helper `backend/src/common/helpers/notification-format.helper.ts` (`uniminutesLabel` / `rupeesLabel`) keeps every money-moving body on the locked currency rule (aspirant copy in Uniminutes, mentor copy in ₹). What now fires: **`PAYMENT`** — top-up credited (`WalletService.verifyAndCreditTopup` + `handleTopupCaptured`, guarded on `applied === true` so webhook+direct-confirm don't double-notify; `WalletService` gained a `NotificationsService` dep + `WalletModule` imports `NotificationsModule`); call billed → aspirant + "you earned" → mentor on dual-confirm settle (`SessionsService.confirmJoined`, paid-hold branch only — a free-tier call isn't "charged") and on `extendCall`; payout COMPLETED / FAILED → mentor (`PayoutsService.process`). **`LOW_BALANCE`** — after a paid call settles and leaves the aspirant's available balance below the shortest slot (`CALL_SLOT_MINUTES[0] * MENTOR_RATE_PER_MINUTE_MINOR`). **`SYSTEM`** — report resolved/dismissed → the reporter, with the refund amount in the body when one was issued and `metadata.sessionId` for a SESSION-target deep link (`ReportsService.resolve`; `ReportsService` gained a `NotificationsService` dep + `ReportsModule` imports `NotificationsModule`). **`VERIFICATION`** — now also sent on *submission* (was only on the admin review outcome): the in-app `VerificationService.submit` path, and the web-lead auto-created request in `EnrollmentsService.provisionAccountFromLead` (`EnrollmentsModule` imports `NotificationsModule`). **`SESSION_REJECTED` / `SESSION_ENDED`** copy — the mentor-reject and mentor/mutual-no-show bodies now tell the aspirant their held Uniminutes were released (the release already happened; only the notification text changed). Every new send is wrapped so a notification failure can never bubble a 500 out of a settled billing path (`SessionsService.notifySafe`, or an inline `.catch`). Not touched (out of the chosen scope): the P1 scheduled-call / join-now / still-waiting / availability-expired reminders (need new cron/interval — the scheduled one also reverses a deliberate hint-only deferral), notification preferences / quiet hours, deep-link routing for `SYSTEM`, Android push grouping, mark-read-on-tap. `tsc` clean; app boots clean with the new module wiring (no circular dep). Live-verified against the real Supabase DB + running backend: `POST /verification` → a `VERIFICATION` "Verification received" row; a full 6-min paid AUDIO_CALL (free seconds zeroed so a real hold is placed) → aspirant gets `PAYMENT` "Call charged · 6 Uniminutes used…" + `LOW_BALANCE` "You have 0 Uniminutes left…", mentor gets `PAYMENT` "You earned · ₹60…". All ~15 throwaway test users + their sessions/holds/ledger/notifications were deleted afterward.
- `payouts` — mentor payout requests: amount is always server-derived (never mentor-chosen). **Model (confirmed 2026-09-09): once every 7 days a mentor may request a payout of their FULL wallet balance.** The 7-day cadence (`PAYOUT_REQUEST_COOLDOWN_DAYS`) is unchanged; only the *amount* stopped being a per-week time-slice. **The withdrawable amount is the mentor's current wallet `balanceMinor` (2026-09-09 fix).** A mentor's balance only ever moves two ways — `+SESSION_CREDIT` (earnings), `-PAYOUT` (a COMPLETED payout) — so it *is* "earned but not yet paid out"; if a mentor skips a week the balance just accumulates and comes out in the next weekly request. The previous calc summed `SESSION_CREDIT` entries created after the last COMPLETED payout's `periodEnd`, which **permanently stranded** earnings whenever a COMPLETED payout's amount was less than the credits in its time-window (e.g. a hand-entered admin payout row): the next `periodStart` jumped past those credits and they could never be claimed. Client hit this — ₹680 balance, could only ever withdraw ~₹0 because a ₹200 COMPLETED payout's `periodEnd` had "consumed" the whole period. Now `requestPayout` and `isReminderDue` both read `wallet.balanceMinor`; `periodStart`/`periodEnd` stay on the `PayoutRequest` row as informational bookkeeping only. Self-healing — a mentor with previously-stranded earnings just gets the full balance on their next request. **No minimum amount (2026-09-07, replaces the old ₹200 floor / `MIN_PAYOUT_MINOR`)** — instead capped to once every `PAYOUT_REQUEST_COOLDOWN_DAYS` (7), checked against the mentor's own most recent `PayoutRequest` row regardless of its outcome (a rejected/failed one still used up that week's request); `requestPayout` 400s with the next-eligible timestamp if called early, and only rejects a genuinely-zero-balance request otherwise (still guards against a 2nd open request → 409). Admin-only PROCESSING/COMPLETED/FAILED transitions, wallet only debited on COMPLETED (`LedgerEntryType.PAYOUT`; `process()` still checks `balanceMinor >= payout.amountMinor` before debiting). Still no auto-disbursement — that's an intentional architecture decision, not a gap. Live-verified 2026-09-09: mentor with ₹680 balance + a COMPLETED ₹200 payout whose `periodEnd` is 30 min old → `POST /payouts` now returns `amountMinor: 68000` (was "No unpaid earnings" under the old logic); zero-balance → 400; 2nd request while PENDING → 409.
  - **UPI ID capture (2026-09-10) — the payout is transferred by an admin, by hand, to the mentor's UPI ID. There is no RazorpayX / auto-disbursement** (Razorpay is top-ups only). Before this the app stored nothing an admin could pay to; now: **`UserProfile.upiIdEncrypted`** (migration `20260910091749_add_user_profile_upi_id`, additive nullable `ADD COLUMN`, applied to prod via `prisma migrate deploy`) — AES-256-GCM at rest, the `profile-encryption.helper.ts` helper generalized `encryptRealName`/`decryptRealName` → `encryptField`/`decryptField` (+ `decryptFieldSafe`, old names kept as aliases). `UpdateProfileDto.upiId` — `@Transform` trims + lowercases, `@ValidateIf` skips validation on `''` (so `""` clears), `@Matches(/^[\w.\-]{2,256}@[a-zA-Z][\w.\-]{1,63}$/)`. `UsersService.updateProfile` rejects it for a non-MENTOR (400 "Only mentors can set a payout UPI ID"); `adminUpdateUser` also accepts it (no role gate). `GET /users/me` returns the decrypted `upiId` (only the caller's own — `toPublicUser` under the profile-loaded guard); `AdminUserDetail.profile.upiId` too. **`requestPayout` now 400s "Add a UPI ID in your profile before requesting a payout." when `upiIdEncrypted` is null** (checked before the balance check), so no un-fulfillable PENDING rows. `PayoutRequestResponse.mentorUpiId` (decrypted) is populated on both `findAll` (admin list) and `findMine`. Mobile: `UserProfile.upiId` + `UsersApi.updateProfile(upiId:)` (pass `""` to clear); a "Payout UPI ID" `TextField` in the mentor block of `EditProfileScreen` (client regex guard, mirrors the DTO); `wallet_screen.dart`'s Withdraw pre-checks `myProfileProvider.upiId` — no UPI → an "Add a UPI ID first" dialog instead of the payout confirm; when set, the confirm dialog names the UPI. Admin: `PayoutRow` shows the UPI under the mentor name + a bold "Pay to UPI:" line in the expanded panel + in the confirm-paid prompt; user-detail page adds a "Payout UPI ID" field. `tsc` + `dart analyze` + admin `tsc` + `user-response` spec all clean. **Live-verified 2026-09-10** against the real DB (throwaway mentor): valid UPI with surrounding spaces/caps → stored `iv:tag:ct` ciphertext, round-trips as `gentle.wolf@okhdfcbank`; invalid → 400; `""` → cleared; payout with no UPI → 400 "Add a UPI ID", with UPI + zero balance → 400 "No unpaid earnings" (gate passed); admin `/payouts?status=PENDING` + mentor `/payouts/mine` both carry the decrypted `mentorUpiId`; aspirant → 400 "Only mentors"; 2nd request while PENDING → 409. All test users/wallets/ledger/payouts deleted after. **Not built:** in-app bank-account (acct + IFSC) capture, penny-drop validation, RazorpayX payout automation + its `processed`/`reversed`/`failed` webhooks — revisit once manual transfers become the bottleneck.
- `wishlist` — aspirant saved/favorited mentors (`SavedMentor`), idempotent save/unsave, hydrated mentor cards via `MentorsService.findById` (a saved mentor who's since become ineligible silently drops off the list)
- `university-reviews` — the university-scoped `Review` model (distinct from `MentorReview`), gated to `VERIFIED` users only, one review per (author, university) enforced at the DB level, anonymized (only `authorRole`, never a name) in every response. `{rating, reviewCount}` attached to `GET /universities/:slug` the same way mentor ratings attach to mentor responses. **A review is write-once (2026-09-09, per request "if the user saves the review they cannot edit again — remove the edit review option").** The `PATCH /universities/:universityId/reviews` route + `UniversityReviewsService.update` + the client `update()` method + `myUniversityReviewProvider` are **removed**. `POST` still 409s a second attempt (`ConflictException`). Client: `openUniversityReview` now checks `hasReviewed` first and, if true, shows a "you've already reviewed — can't be changed" snackbar and does **not** open the form (`existingReview` param + `_isEditing` + prefill all gone from `UniversityReviewScreen`); the college-detail Reviews-tab button hides once reviewed (replaced by a muted line), and the Profile "Rate Your College" row becomes a locked "College Review / you've reviewed your college" row. `GET .../reviews/mine/detail` is kept (harmless, backs a possible "your review" display). Only mentors post a review today (mobile-side `canReview` gate, tighter than the backend rule, which only restricts a mentor to their own verified college and doesn't block a verified aspirant — a real mobile/backend asymmetry, untouched).
    - **Stale-`universityId` submit failure fixed (2026-09-10, device report: a mentor submits their college review, gets a generic "Could not submit your review", the Home/Profile "review your college" prompt won't clear, and the "Accepting calls" switch stays gated).** Root cause: `myProfileProvider` (`FutureProvider.autoDispose`) is a long-lived client cache; the 2026-09 university-dedup passes repointed many `profile.universityId`s server-side, so an app open since before that was POSTing a **stale** id. Backend `UniversityReviewsService.create` then 403s `"You can only review your own college"` (`user.profile.universityId !== universityId`), no `Review` row is written → `hasReviewedOwnCollege` stays false → gate stays shut → banner never hides → tapping it opens a **blank** form (prefill was removed with the write-once change) so it looks like the old review was "cleared". Two fixes, mobile-only: (1) `openUniversityReview` gained `asMentorOwnCollege` — the three mentor-gate entry points (Home/Profile `CollegeReviewPromptBanner`, the "Rate Your College" menu row, `MentorAvailabilityCard.openReview`) pass `true`, which does `ref.refresh(myProfileProvider.future)` and reviews against the **currently linked** college id/name, self-healing the stale cache. (2) `UniversityReviewsApi.create` now catches `DioException` and rethrows the backend's own `message` (string **or** validation list) as `Exception(msg)`; `_friendlyError` unwraps the `Exception: ` prefix — so a real failure ("You can only review your own college", "Only verified students and alumni…", a 400 field list) is now shown instead of the catch-all. `dart analyze` clean; not device-tested (browser pane can't drive the Flutter canvas).
  - **12-question structured form (2026-09-03, client-confirmed via two design artifacts — questions + display card). Superseded 2026-09-07 — it's 13 questions now (Restroom Facilities added as Q5); see the "UX-fix batch" bullet near the end of this section for the full change.** The old free-form `{overallRating, comment, boolean wouldRecommend}` shape is gone. `Review` now carries: Q1–Q4 sliders on the pre-existing category columns (`clinicalExposureRating`=Academic Exposure, `campusLifeRating`=Campus Culture, `workloadRating`=Workload & Stress, `placementsRating`=Future Value — all now **required** in the create DTO), Q5–Q11 single-choice codes in six **new** `String?` columns (`raggingCulture`, `facultyApproachability`, `stipendStatus`, `hostelAvailability`, `hostelSafety`, `valueForMoney`) plus `wouldRecommend` **changed `Boolean?`→`String?`** (4-way: `ABSOLUTELY`/`RIGHT_PERSON`/`DEPENDS`/`PROBABLY_NOT`; the first two count toward the aggregate "% would recommend" via `WOULD_RECOMMEND_POSITIVE`), Q12 star = `overallRating`, plus the optional `tags` (picklist widened 7→12) and `body` (the "in your own words" field, DTO-capped at 300). Choice value sets + codes live in `dto/review-choices.ts`, mirrored 1:1 in `mobile_flutter/lib/features/universities/review_choices.dart` (which also holds the verbatim question wording / option labels / slider captions — the mockups are the content + interaction spec only; the screen is Uniscope's **light-green** system, NOT the artifacts' dark indigo). Migration `20260903120000_...` **deleted the single legacy `Review` row** (per explicit decision — not backfillable) and added the columns. Legacy columns `facultyRating`/`infrastructureRating`/`pros`/`cons` are left in place, no longer collected. `reviewSummary()` now also returns `choiceDistributions` (per-question code→count, backs Phase 2's breakdown bars). Mobile: `WriteReviewSheet` (bottom sheet) **replaced** by `UniversityReviewScreen` (route `/college-review`, `extra`-driven) — a full scrolling form with a progress bar, reached everywhere via `openUniversityReview(context, ref, universityId:, universityName:)` (does the `findMine` prefill then pushes); old callers in `profile_home_screen.dart` / `university_detail_screen.dart` / `review_breakdown_screen.dart` all updated. Backend live-tested end to end (12-field create → 201, bad choice code → 400, summary aggregation incl. recommend%/distributions, edit path).
  - **Aspirant display (Phase 2, 2026-09-03):** `ReviewBreakdownScreen` gained a **"Student Experience Breakdown"** section — one `ChoiceDistributionCard` (`review_widgets.dart`) per Q5–Q11 that has any answers: a headline (`N% <positivePhrase>` from `ReviewChoiceSpec.positiveCodes`/`positivePhrase` in `review_choices.dart`), a stacked severity bar (`reviewChoiceSeverityColor`, green→red by option index, `NA` grey), and a tap-to-expand per-option legend with real %s — all from `summary.choiceDistributions`. The "Rated by Category" bars now carry one-line subtitles (`CategoryRatingBar.subtitle`). The summary card (`ReviewSummaryBody` / `UniversityCard` embed) was already wired for `recommendPercent` + category bars + top tags and needed no change — `recommendPercent` now derives from the `wouldRecommend` string codes server-side. No backend change in Phase 2. `flutter analyze` clean.
  - **Mentors verified from 2026-09-03 on must review their own college before accepting call bookings.** New `UserProfile.mustReviewCollege Boolean @default(false)`, set `true` in `VerificationService.review` on approve (existing verified mentors stay `false` → exempt). `UsersService.updateProfile` blocks flipping `isMentorAvailable → true` (400 "Add a review of your college…") while `mustReviewCollege && !hasReviewedOwnCollege` (`UsersService.hasReviewedOwnCollege`). `GET /users/me` exposes `mustReviewCollege`; the client pairs it with `GET /universities/:id/reviews/mine` to know if the gate is met. Mobile: `CollegeReviewPromptBanner` (a nudge card, self-hides for anyone not subject — i.e. shows only for a MENTOR with `mustReviewCollege && universityId != null && !hasReviewed`; an *un*verified mentor sees the separate `MentorAvailabilityCard` prompt, not this) on the mentor Home + Profile tabs, and `MentorAvailabilityCard`'s switch becomes a tap-to-open-review target with helper copy while the gate is unmet — same pattern as the existing unverified-mentor case. Banner copy (2026-09-06, matched to the approved mockup): title "Review your own college to accept call bookings", subtitle "A quick honest review of {collegeName} unlocks the \"Accepting calls\" switch." Client's chosen strictness: persistent banner **+** hard-gated switch (not a forced full-screen step). Live-verified: gate rejects, then passes once a review exists. **Confirmation copy (2026-09-06):** `_SuccessView` in `university_review_screen.dart` reads the caller's role — a MENTOR sees "You're all set / Your review of X is in. You can now accept call bookings and start earning as a mentor." (the review is the gate they just cleared); anyone else keeps the "helps the next batch decide" framing. Deliberately says nothing about editing the review (there is no edit — it's write-once, see the `university-reviews` module note above). **Gate stayed stuck open for up to two days after a genuine review, fixed 2026-09-12 (device report + screenshots — the mentor had reviewed, the backend confirmed it, the app kept showing the banner and a locked switch regardless).** Root-caused live against the real DB for the reporting mentor: `GET /universities/:id/reviews/mine` correctly returned `true` and `mustReviewCollege` was satisfied — the gate was genuinely cleared server-side; the client just never found out. Two compounding client bugs, both fixed: (1) `myProfileProvider` (`FutureProvider.autoDispose`) and `hasReviewedUniversityProvider` (a *separate* `autoDispose.family<bool, String>` cache) only refetch when their last watcher unmounts and remounts — which never happens for a screen the bottom-nav shell keeps alive, so a mentor who wrote their review and then simply backgrounded the app (never force-quit) could see the stale "not reviewed" state for days; fixed with a new app-resume hook in `main.dart` (`_UniscopeAppState` + `WidgetsBindingObserver`, `didChangeAppLifecycleState` → on `resumed` while authenticated, invalidates both providers — same pattern `ChatThreadView` already uses for its own staleness). (2) Even within one session, submitting via `CollegeReviewPromptBanner`'s own tap had **no follow-up invalidation at all** (unlike `MentorAvailabilityCard.openReview()`, which had one but only for `myProfileProvider` — missing `hasReviewedUniversityProvider`, the actual provider the gate reads) — both entry points now invalidate **both** providers after the review flow returns, so "review submitted" can't read back as "not reviewed" in the same session that just cleared it either. (An unrelated, correctly-working mechanism the same report also touched on: `isMentorAvailable` auto-expires 24h after it's switched on regardless of the review gate — see the mentor-availability decision above — so a mentor back after a few days may still need to re-flip the switch once; that's by design, not part of this fix.) `dart analyze` clean project-wide; not device-tested (the exact repro needs a real backgrounded-for-days session, which a browser preview or a fresh debug run can't reproduce).
- `university-wishlist` — same idempotent save/unsave pattern as mentor `wishlist`, for `SavedUniversity`. Mobile: heart toggle on college cards + detail screen, Saved Colleges screen at `/colleges/saved`.
- Mentors are now fully self-service end to end: `PATCH /users/me` lets a MENTOR-role user set `specialty`, `bio`, `languages`, and toggle `isMentorAvailable` themselves (guarded to MENTOR role) — no admin/DB script needed anymore to become discoverable. Combined with self-serve verification (already existed) and the verification-approval → `userProfile.universityId` link (see `VerificationService.review`), a mentor can go from signup to fully bookable using only the app. Mobile: "Available for mentoring" switch on the Profile screen, plus `EditProfileScreen` is now role-aware — a MENTOR sees specialty/bio/languages fields there instead of the aspirant fields (gender/qualification/stream/state/city/goals). The specialty/languages inputs didn't exist in the UI until now even though the backend DTO always supported them — a mentor had no way to actually set their own expertise tags before this.
- **Mentors-tab filters (2026-09-04)**: Stream now defaults to — and actively filters by — the viewer's own `profile.stream` from onboarding until they explicitly touch the pill (same deferred-default pattern the Colleges tab's Stream pill already used), rather than sitting inert as just a hint. Degree and Specialization filter chips were added, then explicitly removed again per client follow-up the same day — Mentors-tab filters are just Available now / Rating / Stream / Language. `MentorResponse.qualification`/`.specialization` (added on the backend for those chips — the mentor wizard's "Degree" step actually writes to `UserProfile.qualification`, which no API response had exposed before) were left in place since they're harmless and may be useful elsewhere later, but nothing on mobile reads them today.
  - **Reversed again, 2026-09-05, per a new written requirements brief that explicitly asked to re-add Degree + Specialization filters** (a real conflict with the note directly above, on record rather than silently overwritten): `ListMentorsDto` gained `qualification`/`specialization` optional filters; `MentorsService.findAll`'s `where.profile` now applies equals-insensitive filters for stream/qualification/specialization alongside the pre-existing `language`/`specialty` filters — all plain indexed `UserProfile` columns, so this stayed server-side rather than downloading every mentor and filtering in Flutter (the brief's explicit preference). Mobile: `mentorsListProvider` converted from a bare `FutureProvider` to a `.family<List<Mentor>, MentorListFilters>` keyed on `(stream, qualification, specialization, language)` (`MentorListFilters` typedef + `kNoMentorFilters` constant in `mentor_list_screen.dart`); `MentorsApi.list()` now sends all four as query params. `mentor_list_screen.dart` gained a Degree pill (disabled until a Stream is picked, options via `degreesForStream(stream)`) and a Specialization pill (Medical stream + non-MBBS degree only, via `kMedicalSpecializations`), plus a "Clear filters" action and a distinct "no mentors match your filters" empty state (vs. "no mentors yet"). `home_screen.dart`'s "Top Mentors" strip was the only other consumer of the old no-arg provider — updated to pass `kNoMentorFilters`, as was `test/flow_test.dart`'s provider override (`overrideWith((ref, filters) async => ...)`), both caught via a full-project `dart analyze` pass before considering this done. "Available now"/"4★+ rating" stayed client-side deliberately — they're a computed/joined aggregate (rating) and a time-decayed derived flag centralized in `isCallAvailable()` (availability), not raw columns, so pushing them server-side would be a separate, larger change than what was asked. Known pre-existing gap, not touched here (out of this task's scope): `MentorsApi.list()` still never sends `limit`/`cursor`, so the Mentors tab has always only fetched the backend's default first page (~20) — worth a dedicated pass. **FIXED 2026-09-10 — see the "`MentorsApi.list()` now walks all pages" bullet below.**
- **Mentor Homepage — "Today's Overview" gap (2026-09-05)**: `MentorLandingScreen` (mentor Home tab) and its backing `GET /mentors/me/dashboard-stats` already existed with real (non-fake) data for today's-session-count/minutes-consulted/weekly-earnings/monthly-earnings/rating/recent-sessions — this bullet documents that pre-existing feature for the first time (found undocumented while auditing a requirements brief) and the two real gaps found+fixed in it: (1) no today's-earnings figure existed at all (only weekly/monthly) — `MentorsService.getDashboardStats` now runs a `todaysEarnings` ledger aggregate parallel to the existing weekly/monthly ones, exposed as `todaysEarningsMinor` on `MentorDashboardStatsResponse`; (2) each Recent Session row was missing session type and status — `MentorDashboardRecentSession` gained real `type`/`status` fields sourced straight from the `Session` row (not fabricated). Mobile: `MentorDashboardStats`/`MentorDashboardRecentSession` (`mentors_api.dart`) gained the matching fields + a `todaysEarningsRupees` getter; `mentor_landing_screen.dart`'s "Today's Overview" grid gained Today's Earnings + Sessions Completed (`totalSessionsCount`, already fetched but previously unused on this screen) tiles, and `_RecentSessionCard` now shows a call/chat type icon + a manually-formatted date (no `intl` dependency in this project, so a small hand-rolled formatter) plus a `StatusChip` (existing shared widget) instead of just name + amount + minutes. **"Top Colleges For You" NOW BUILT (2026-09-06, client-approved via mockup — supersedes the "explicitly NOT built" note that was here).** Client picked "real ranking, backend endpoint" over an alphabetical no-backend list. `GET /universities/top-for-mentor` (MENTOR-only, `RolesGuard`): `UniversitiesService.topForMentor(userId)` takes active colleges in the mentor's **own** `profile.stream`, drops their own `profile.universityId`, ranks by average rating then review count — computed fresh via the pre-existing `UniversityReviewsService.ratingSummaries(ids)` batch aggregate over ACTIVE reviews. Returned reviewed colleges are capped at `TOP_FOR_MENTOR_LIMIT = 8`, each row carrying `rating`/`reviewCount` (same shape as `findBySlug`). **(Superseded 2026-09-08 — see the "`top-for-mentor` now always populates the rail" sub-bullet further down: it no longer returns `[]` when nothing is reviewed / no stream is set; it falls back to an alphabetical slice with `rating: null`. The original behaviour described here — reviewed-colleges-only, rail hides when empty — held only from 2026-09-06 to 2026-09-08.)** Route declared **before** `@Get(':slug')` so the literal path isn't swallowed by the slug param. Mobile: `UniversitiesApi.topForMentor()` + `topCollegesForMentorProvider` (`universities_api.dart`); `mentor_landing_screen.dart` renders a horizontal rail of `_TopCollegeCard` (fixed 150px, tinted thumb + name + stream + **rating shown** — unlike the aspirant's `_CollegeSpotlightCard` which hides it) between Today's Overview and Recent Sessions, whole section wrapped in `maybeWhen(data:...)` so header+rail vanish together when empty. Card taps → `/colleges/detail` (registered in the mentor shell too). Backend live-tested: 9/9 — 200, array, exactly the reviewed colleges (own + unreviewed excluded), ranked rating-desc, `reviewCount` carried, no-stream mentor → `[]`, aspirant → 403. **"Your Starboard"** — the "Reviews" section on this same screen (`myMentorReviewsProvider`, real `MentorReview` rows) was **renamed `Reviews` → `Your Starboard`** in the visible `SectionHeader` in the same change. `tsc` + `dart analyze` clean.
- **Call screen back navigation (2026-09-04)**: `CallScreen` used to block back (hardware/gesture) on every phase except the terminal ones (`ended`/`error`/`permissionDenied`), with no back arrow of its own — a user stuck mid-connect or mid-ring had no way off the screen at all. Fixed via `PopScope.onPopInvokedWithResult`: terminal phases still pop straight away; a not-yet-live call (permission/connecting/ringing) cancels quietly since nothing's been billed yet; a genuinely active call shows a confirm dialog first so a stray swipe-back can't silently drop a live, billed call. **On-screen back button added (2026-09-12, user report "where is the back button — which should take inside the app").** The doc comment above was literal — this screen genuinely had no visible way off it, only the invisible OS back gesture (no affordance on Flutter web at all, and only a hardware button/edge-swipe on native). `_CallStage`'s old `onMinimize` chevron — a down-arrow shown ONLY in the now-dead overlay mode (`inOverlay`, reverted `1e87a3b`) — is renamed `onBack`, made an always-required (not conditional) `VoidCallback`, drawn as a proper `Icons.arrow_back_rounded`, and threaded into all three live-phase views that render through `_CallStage` (`_ConnectingView`, `_WaitingView`, `_ActiveCallView` — `_ConnectingView` previously had zero controls of any kind, the worst instance of the gap). Every call site wires it to the same `_handleBack()` the hardware back button already used, so the semantics are unchanged: quiet-cancel pre-connect, confirm dialog once `active`, straight pop once already `ended`/`error`/`permissionDenied`. `dart analyze` clean (0 errors project-wide); not device-tested.
- **Profile screen (2026-09-04)**: avatar + edit button now sit outside the scroll view (pinned above it) instead of scrolling away inside the top profile card with everything else — always reachable for editing. The profile-completeness nudge no longer prompts for "your date of birth".
- **Edit Profile "Others" language (2026-09-04)**: the mentor onboarding wizard's Languages step always let you type a custom value when "Others" was picked (`_languagesOtherController`); `EditProfileScreen`'s own (mentor-only) Languages section offered the same "Others" chip but never showed a text field to type into — selecting it just silently added the literal string "Others" as a spoken language. Fixed to mirror the onboarding wizard's exact pattern, plus hydration: since `_save` maps `"Others"` to whatever was typed (the literal word "Others" itself is never stored), an existing profile's already-saved custom language (anything not in `kLanguageOptions`) is now detected on load and shown pre-filled with "Others" checked, instead of silently vanishing from the chip group when reopening the screen.
- **Edit Profile sticky avatar (2026-09-05)**: distinct from the "Profile screen" pinning above — that one fixed `ProfileHomeScreen` (the read-only profile tab); `EditProfileScreen` (the actual edit *form*, reached via its edit button) had no avatar shown at all before this, and its whole body was a single `SingleChildScrollView`. Restructured to `Column([pinnedAvatarBlock, Expanded(SingleChildScrollView(...))])` — avatar + edit-avatar action now stay fixed above the fold while the form fields scroll underneath, and since only the `Expanded` half resizes for the keyboard, focusing a text field no longer pushes the avatar off-screen or squeezes the layout. `dart format` + `dart analyze` clean.
- **Onboarding avatar step now has the same sticky preview (2026-09-06)**: the pinned-preview + scrolling-options pattern (previously hand-rolled only in `avatar_customizer_screen.dart`) is extracted into `StickyPreviewAvatarPicker` in `avatar_picker_panel.dart` — forwards a `GlobalKey<AvatarPickerPanelState>` to the inner panel (turns its inline preview off, pins an 80px copy above an `Expanded(SingleChildScrollView(...))`). `OnboardingStepScaffold` gained an `expandedChild` param (fixed header + `Expanded(child)` instead of the page scroll) for steps whose body scrolls itself. Both onboarding wizards' "Choose Your Avatar" step and the standalone Customize Avatar screen now use `StickyPreviewAvatarPicker`, so the three can't drift. The preview avatar sits on a light-green (`AppColors.primaryLight`) circular disc — the avataaars SVG is transparent-backed (`backgroundColor: ['transparent']` in `avatar.service.ts`), so without it the preview read as a floating cutout. `dart analyze` clean (`dart format` deliberately NOT run — it reflows large unrelated regions of these files, which aren't kept canonically formatted).
- **10-minute logout root-caused and fixed (2026-09-05)**: users were being logged out after roughly 10 minutes of use despite `JWT_ACCESS_TTL=900`s (15 min) and `JWT_REFRESH_TTL=604800`s (7 days) — nowhere near 10 minutes on their own, and confirmed via full trace that **no inactivity timer or app-lifecycle-based logout exists anywhere** in `mobile_flutter/lib/` (grepped clean) and `auth_controller.dart`'s `logout()` is only ever called from two places: the explicit user-initiated logout button, and the Dio interceptor's error handler. The actual root cause: `backend/src/auth/token.service.ts`'s `rotateRefreshToken()` implements **single-use, rotating** refresh tokens — each successful `/auth/token/refresh` call hash-compares the incoming token against the one stored `user.refreshTokenHash` and *overwrites* it with a freshly-issued pair; a second call presenting the now-superseded token throws `UnauthorizedException('Token reuse detected')`. The old `dio_client.dart` interceptor had no concurrency guard around this — if two API calls both 401'd around the same moment (e.g. two widgets refreshing data on app-resume), each independently fired its own `/auth/token/refresh` call with the *same* stored refresh token; the first succeeded and rotated the hash, the second then failed the token-reuse check and the interceptor's over-broad `catch (_) { logout(); }` silently logged the user out — no timer involved, just an ordinary concurrent-refresh race that got more likely the longer/heavier a session ran (hence "roughly 10 minutes", not a fixed threshold). **Fix**: `dio_client.dart`'s interceptor now shares one in-flight refresh across concurrent 401s via a single-flight guard (`Future<(String,String)>? refreshInFlight`, a `refreshTokens()` helper that does `refreshInFlight ??= _doRefresh()` so a second caller awaits the same pending call instead of firing its own), and only calls `logout()` on a **confirmed** 401/403 from the refresh endpoint itself — a generic `DioException` (network blip, timeout) during refresh no longer logs the user out. `dart analyze` clean. Not live-tested against two genuinely concurrent requests in a running app in this session (only statically verified) — worth a real two-tab/two-widget race test next time this path is touched.
- **"Need Help?" added to Profile (2026-09-05)**: `ProfileHomeScreen`'s menu list had a Settings row and nothing after it — no support entry point existed on the Profile tab for either role (the "Need Help?" card that exists on the mentor Home screen and the Support chat itself were the only ways in). Added a final menu row (`Icons.support_agent_rounded`, unconditional for both ASPIRANT and MENTOR) that pushes the Help & Support screen (below).
- **Help Centre screen (2026-09-06)**: `HelpScreen` (`mobile_flutter/lib/features/support/help_screen.dart`, top-level route `/help`, app-bar title "Help Centre") — a real landing screen in front of the raw support thread. Canopy `GradientAppBar`, a "How can we help, {firstName}?" lede (name in `AppColors.logoNavy`), a live search box that filters the FAQ, a "Chat with Support" card whose "Chat with us" button opens the support thread (the only path that touches the backend), 6 "Browse topics" rows (each opens `_HelpTopicScreen` — a filtered article list), and a "Popular questions" expandable-FAQ block. All topic/FAQ content is a static Dart list in the same file (`_kTopics` / `_kArticles`, `{category, question, answer, popular, mentorOnly}`) — no CMS, works offline. Role-aware: the "Verification & payouts" topic and the mentor-only articles (verification time, "why can't I turn on Accepting calls", payouts, "does Accepting calls mean online") render only for `role == MENTOR`. FAQ copy is written to the real billing/availability model (holds vs. charges, 6/10/20 slots, anonymous reviews, reversible account deletion) and deliberately never says "chat is free". Footer: a "Facing technical difficulties with the application?" block with a "Report a technical issue" button → route `/report-issue`, then Terms/Privacy/version. (The grey "Still can't find…" Chat-with-us/Call-us band lives on the `/report-issue` screen, not here.) The three "Need Help?" entry points (`mentor_landing_screen.dart`, `session_list_screen.dart`, `profile_home_screen.dart`) now `context.push('/help')` instead of `/chats/support`; the aspirant `HomeScreen` also gained a "Need Help?" `AppCard` at the bottom (after "Keep Exploring") — the mentor `MentorLandingScreen` already had one there. **`SupportChatScreen` is also registered as its own top-level route `/support`** (sibling of `/help`) and that's what `HelpScreen` pushes — the pre-existing `/chats/support` lives inside a `StatefulShellBranch`, and pushing a shell-nested location from a non-shell route (`/help`) renders blank, so the standalone `/support` route is required. The two orphaned nested `chats/support` routes are left in place (harmless, no caller). `flutter analyze` clean.
- **"Report a technical issue" (2026-09-06)**: `TechnicalReportScreen` (`mobile_flutter/lib/features/support/technical_report_screen.dart`, top-level route `/report-issue`) — heading "Report a technical issue", a "Describe your problem" textarea, Submit → `SupportApi.submitTechnicalReport` (`lib/core/network/support_api.dart`) → `POST /support/technical-reports` (sends `platform` from `defaultTargetPlatform`/`kIsWeb`), then an inline "Thanks — we've logged your report" success state. A grey "Still can't find what you're looking for? Don't worry we're here to help" band with **Chat with us** + **Call us** is pinned at the bottom of this screen. Lands in the admin panel's Support → Technical reports tab (see backend `technical-reports` module). **Call us**: `lib/features/support/support_contact.dart` — `kSupportPhoneNumber = '+917010441518'` (client-provided), `callSupport(context)` opens the dialer via `url_launcher` (added as a dep; Android manifest gained a `<queries>` DIAL/`tel` intent), SnackBar fallback with the number if no dialer. `dart analyze` clean.
- **Session chat header: unique ID instead of a profile-navigation tap (2026-09-05)**: `SessionChatScreen`'s header previously had no tap handler on the counterparty's name at all (the brief that requested this fix described a "clicking navigates to profile" bug that did not actually reproduce in code — noted here rather than silently accepted). Built the requested end-state regardless: the header now shows `@<uniqueId>` (the platform's existing public registration number, e.g. `A1134500001` — never the internal DB id) beneath the counterparty's display name, for both directions (mentor viewing an aspirant's header, and an aspirant viewing a mentor's), and the name is not a tap target. Backend: `SESSION_WITH_NAMES_INCLUDE` (`session-response.ts`) now also selects `uniqueId` for both parties; `SessionResponse` gained `aspirantUniqueId`/`mentorUniqueId: string | null`, populated in `toSessionResponse`. Mobile: `Session` (`sessions_api.dart`) gained the matching fields/parsing. `tsc` + `dart analyze` clean.
- **Gender is set once at onboarding, not user-editable (2026-09-06)** — aspirants and mentors both. Mobile: `EditProfileScreen` drops the Gender dropdown from the aspirant section and no longer sends `gender`. Backend: `UsersService.updateProfile` only writes `gender` when the profile doesn't already have one, so onboarding's first write lands and any later change (any client, or a crafted `PATCH /users/me`) silently no-ops. Admin `PATCH /users/:id` is unaffected. Live-verified: fresh `gender=Female` applies; a later `gender=Male` is ignored while `state=…` in the same request still saves.
- **Home "for you" rails, actually personalized (2026-09-06, aspirant Home)** — "Top mentors for you" now server-filters by the aspirant's own `profile.stream` (same deferred-default pattern as the Mentors/Discover tabs) and client-sorts the page by rating (the backend orders `createdAt desc`); falls back to the unfiltered list when the stream is unknown or has no mentors. New **"Top colleges for you"** rail (header renamed from "Colleges for you" 2026-09-06 per client): the aspirant's stream filtered out of the catalogue already fetched for "Keep Exploring" (no extra request), alphabetical (no ranking signal on the list endpoint — the "Top" is aspirational copy, not a real ranking). New `_CollegeSpotlightCard`; the card fills the rail's fixed height so every card is the same size regardless of name length, and its hero shows the same per-stream glyph as Discover (see "Stream glyphs on college cards" below). Home section order (2026-09-08, per request — "Top mentors for you" moved above "Pick up where you left off"): Quick actions → Top colleges for you → Top mentors for you → Pick up where you left off → Keep Exploring → Need Help.
- **Home canopy greeting + seam polish (2026-09-07, per screenshot)** — the "Good afternoon, {name}" line was a white prefix + `AppColors.logoNavy` name split at `AppFont.lg`, inconsistent and prone to wrapping with a long display name. Now one solid-green (`AppColors.primary`) run at the smaller `AppFont.md`, with `overflow: ellipsis, maxLines: 1` as a safety net. The canopy→sheet boundary (where the rounded white sheet is pulled up over the canopy's foot) gained a small glowing handle bar — a white pill with a soft green `BoxShadow` (`Positioned` inside a `Stack` wrapping the sheet `Container`) — as a clearer seam than the bare rounded corner gave on its own.
  - **Reworked 2026-09-08 to a mockup, then FULLY REVERTED the same day (per "can we go one step back … undo the header mockup rework").** The aspirant Home header is back to its pre-rework state: green `AppGradients.canopy` gradient wash, `NotificationBell(color: Colors.white)` beside the greeting (not in the top row), the logo mark on a white chip, greeting as a single `AppColors.primary` green `AppFont.md` run, subtitle white-alpha, `"Search universities..."` placeholder, the small glowing white handle-bar pill at the canopy/sheet seam, and **no** `home_header.svg` building watermark (`flutter_svg` import dropped from `home_screen.dart`). What the reworked round did — kept here only as history: ~~ The saturated `AppGradients.canopy` green is replaced by a soft `LinearGradient(primaryLight → background)` mint→white wash, so the canopy fades **seamlessly** into the content sheet — the glowing handle bar from the 2026-09-07 round is **removed** (nothing to mark once the seam is invisible). Layout changes: the logo mark loses its white chip (dark navy reads fine on the pale wash); `NotificationBell` moves from beside the greeting into the top row, between the "Uniscope" wordmark and the avatar, tinted `AppColors.textPrimary` (was white); the avatar sits on a white circle with `AppShadows.card` instead of a white ring. The greeting goes back to a **split** style at `AppFont.xxl` extraBold — `"$greeting, "` in `AppColors.textPrimary` + the first name in `authBrandTeal` (reverting the 2026-09-07 all-green `AppFont.md` run). Subtitle "What are you looking for today?" is now `AppColors.textSecondary` grey (was white-alpha). Search pill placeholder "Search universities..." → **"Search colleges, courses, or mentors..."**, and the pill gained `AppShadows.card` to lift off the pale ground. New decorative asset wired in: `assets/illustrations/home_header.svg` (a columned-building watermark, already in the bundle, previously unused) — `SvgPicture.asset` at `width: 132`, `opacity: 0.4`, `Positioned(top: 30, right: -46)` inside an `IgnorePointer` (nudged down + further off the right edge + fainter in a same-day follow-up so it clears a long first name in the greeting), as the first child of a `Stack` so all header content paints over it; the canopy `Container` gained `clipBehavior: Clip.hardEdge` to trim its right-edge bleed. `flutter_svg` import added (already a dep). `dart format` NOT run (`home_screen.dart` is non-canonical at HEAD); `flutter analyze` clean; verified in the Flutter-web preview against the mockup.
    - **Follow-up 2026-09-08 then fully REVERTED same day (per "revert back to previous 3 changes in the home screen of student").** A short-lived round tried: (1) shrinking the `"$greeting, "` prefix to `AppFont.md` with the name at `AppFont.xl`; (2) recolouring the whole greeting (green, then dark); (3) adding a thin glowing rule at the header ↔ content seam. All three were reverted — the greeting is back to the split style at `AppFont.xxl` (dark `"$greeting, "` + `authBrandTeal` first name) and there is **no seam rule** (the pale wash → sheet join stays seamless). The rest of the header (pale canopy, bell in the top row, building watermark, search copy) is unchanged.~~ (mint→white wash, bell in top row, no chip, split `AppFont.xxl` greeting, grey subtitle, card-shadow search pill, the building watermark). The `_CollegeSpotlightCard` / `_CollegeCard` pale-mint palette + rating row are **not** part of this revert and stay.
- **Mentor detail screen trims + chat-starter move (2026-09-06)** — the track-record row drops "Minutes mentored" and "Students helped" (only the rating/review-count stat stays; full review list still below); "Usually free on" now sits above "Can help with". **Then (2026-09-06, per screenshot request): the academic tags moved up.** Field of study / degree / specialisation (`stream` or legacy `specialty`, `qualification`, `specialization`) render as chips in `_IdentityCard` right under the college line (`_academicTags` getter), not mixed into the expertise chips anymore. What's left of that section is just languages — renamed "Can help with" → **"Speaks"**. Also (same request): `welcome_screen.dart`'s "What You Get" slide got a `_Slide.quote`/`quoteAuthor` field (rendered italic + centred under the feature cards) and reworded feature copy — "Mentor Guidance" → "Know your college completely before you step in. Make your biggest decision with confidence 💪", "The Right Mentors" → "Connect with currently studying or graduated students who can give you real insights about campus life and culture.", quote "The best people to help you choose a college are the people who are already studying there." — someone wise. The "Chat" button opens the thread directly — `showPreChatConfirmSheet` / `pre_chat_confirm_sheet.dart` is **deleted**. The starter questions moved into `ChatThreadView`'s empty state (new `starterSuggestions` param, `kMentorChatStarters` in `session_chat_screen.dart`): shown only while the thread has zero messages, gone the instant one exists (sent, received, or loaded on reopen), tapping one drops it in the composer. Passed only for an aspirant's side of a mentor chat — the mentor's side and the support chat keep the plain empty state.
- **Profile-completeness checklist honest (2026-09-06)** — `ProfileHomeScreen`'s "Complete your profile" card was nagging for "your goals" (not collected in onboarding *or* Edit Profile — removed in `693f09b`) and "your preferred language" (onboarding-only, no edit field), so it could never hit 100% and the "Next: add …" hint pointed at nothing. Aspirant checklist trimmed to photo / city / state / qualification / field of interest — all set at onboarding and editable afterward.
- **Profile details are read-only now — no user-facing edit for either role (2026-09-06, explicit client decision).** `EditProfileScreen` (route `/profile/edit`, kept for call-site stability, retitled "Profile Details") was rewritten from an editable form into a plain read-only `_DetailRow` list — mentor: display name / ID / gender / field of study / bio / languages; aspirant: display name / ID / gender / qualification / field of interest / state / city. No Save button, no `_save`/`updateProfile` call. **The avatar stays editable** (the pencil badge → `/profile/avatar` — per explicit client instruction). The `_ProfileCompleteness` "Complete your profile" card was deleted from `ProfileHomeScreen` (it nudged users to fill fields they can no longer fill), and the Profile-tab menu row "Edit Profile" → "Profile Details". The mentor "Accepting call bookings" switch (`MentorAvailabilityCard`) is untouched — it's a booking control, not a profile field. **Backend NOT locked** — `PATCH /users/me` still accepts profile fields (needed for the avatar-config write and the availability toggle, both of which go through `updateProfile`); this is a UI-only removal. Supersedes the "EditProfileScreen is role-aware" / "Others language" / "sticky avatar" editing details above — those described the old form.
  - **Partial revert for MENTOR only (2026-09-07, explicit follow-up request — conflicted directly with the bullet above, resolved by asking the user which was correct).** A mentor's situation legitimately changes over time in a few specific ways the fully-locked version above didn't account for, so `EditProfileScreen` now restores a small editable set for MENTOR accounts: **current status (Currently Studying/Graduated) → year of study or graduation year, plus its "keep private" toggle (`yearInfoPrivate` — pre-existing field, mirrors the mentor onboarding wizard's own step), bio, languages (with the same "Others" free-text pattern as onboarding), and available time slots (`availableDays`, capped at 2, same as onboarding).** Reused the onboarding wizard's own widgets (`OnboardingFieldLabel`/`OnboardingSingleChipGroup`/`OnboardingChipGroup`/`OnboardingToggle` from `onboarding_widgets.dart`) so the edit form and the wizard can't visually drift. Everything else (display name, ID, gender, field of study/stream, college/degree — fixed identity facts set once at verification) stays read-only exactly as shipped above. **ASPIRANT is untouched — still fully read-only, no editable fields, per the original decision.** No backend change needed — `PATCH /users/me` already accepted these fields and none of them are in the write-once-only list (only `gender` is gated that way).
  - **Reversed again — display name is now the ONLY editable field, both roles (2026-09-08, explicit request "allow only the display name to be edited from the profile details").** `EditProfileScreen` was rewritten once more: a single "Display name" `TextField` (max 60, client guard: trimmed length ≥ 2 and changed from the loaded value) with a "Save" button at the top, then everything else — mentor: ID / gender / field of study / bio / languages; aspirant: ID / gender / qualification / field of interest / college / state / city — back to the read-only `_DetailRow` list. The MENTOR editable set from the 2026-09-07 sub-bullet above (current status / year / `yearInfoPrivate` / bio / languages / `availableDays`) is **gone from this screen** — bio/languages are read-only rows again, the rest just aren't shown here (a mentor still edits `availableDays` via the mentor onboarding wizard and `isMentorAvailable` via `MentorAvailabilityCard`; year-of-study has no other in-app editor now — flagged if it matters). Save calls `updateProfile(displayName:)` (already supported end-to-end — onboarding uses the same path; backend has a DB-level `lower(display_name)` unique index and returns `ConflictException` "That name is already taken — try another.", surfaced inline). On success it also `setUser(updated.toAuthUser())` so greetings/avatars app-wide update immediately. Avatar still editable via the pencil badge. `onboarding_widgets.dart` / `profile_options.dart` imports dropped. `flutter analyze` clean; not click-tested (browser pane can't drive the Flutter canvas).
  - **Reverted again — the MENTOR editable set is back, display name is NOT editable (2026-09-08, per an annotated screenshot: "Mentor can have a edit profile ONLY for year of study/year of graduation (and its live/hidden status toggle), bio, languages and available time slot … rest all stays the same").** `edit_profile_screen.dart` was restored to its pre-`b614ead` state (`git checkout b614ead^ -- …`), i.e. the 2026-09-07 partial-revert version: a **MENTOR** sees read-only `_DetailRow`s for Display name / ID / Gender / Field of study, then an editable block — Current status (Currently Studying / Graduated) → Year of study (`kYearsOfStudy` chips) or Year of graduation (number field) + a "keep private" `OnboardingToggle` (`yearInfoPrivate`), Bio, Languages (`kLanguageOptions` + "Others" free-text), Available time slot (`kTimeSlots`, `maxSelections: 2`) — and a "Save changes" button → `updateProfile(bio/languages/availableDays/yearInfoPrivate/yearOfStudy/graduationYear)`. An **ASPIRANT** is fully read-only again (no editable fields, the "set during sign-up… contact support" helper). Display name is a read-only row for both — the 2026-09-08 "display name is the only editable field" change (commit `b614ead`) is undone. Avatar still editable (pencil badge → `/profile/avatar`) for both. No backend change — `PATCH /users/me` already accepts all six mentor fields and none are write-once-gated (only `gender` is). Reuses `OnboardingFieldLabel`/`OnboardingSingleChipGroup`/`OnboardingChipGroup`/`OnboardingToggle` so the edit form and the onboarding wizard can't drift. `dart analyze` clean; not click-tested (browser pane can't drive the Flutter canvas).
- **College detail hero: back / save buttons pinned to the top (2026-09-06)** — the icon `Row` was a plain child of a `StackFit.expand` Stack, so it got tight constraints and centred vertically over the 300px hero (landing next to the college name). Wrapped in `Positioned(top:0)` + the status-bar inset.
- **UX-fix batch from an annotated-screenshot review (2026-09-07):**
  - Help Centre's "How do I recharge my wallet?" FAQ said "₹250, which credits 20 Uniminutes" — stale copy from before the recharge packages were finalized (see "Pricing" — ₹250 has always credited **10**, not 20). One-line content fix in `help_screen.dart`'s `_kArticles`.
  - `MentorDetailScreen` showed the mentor's rating twice — once inline in the identity card header, once again as a big standalone `_TrackStat` block right below it (a leftover from the 2026-09-06 trim that removed its sibling stats but left the rating one in place). Removed the redundant block + the now-unused `_TrackStat` widget; the inline rating stays as the only copy.
  - Profile tab's "Saved Colleges" / "Saved Mentors" counters (`_Stat` in `profile_home_screen.dart`) were static numbers with no tap handler. Now navigate to the already-existing `/colleges/saved` / `/mentors/saved` screens.
  - Mentor Home's "Uniscope" wordmark (`mentor_landing_screen.dart`) was rendered at `AppFont.sm` in a translucent grey — much smaller/fainter than the aspirant Home's `AppFont.xxl` `logoNavy` treatment for the same wordmark. Matched to the aspirant version (icon 22→30px, text sm→xxl, `logoNavy`).
  - College detail's rating/tag summary card (`ReviewSummaryCard`) moved from the top of the **Reviews** tab to the top of the **Overview** tab — it's a snapshot of the college, and Overview is where a browsing aspirant looks first; Reviews stays focused on reading (and a mentor writing) the actual review list.
    - **Follow-up 2026-09-08:** the summary card's "See full review breakdown" footer (and the equivalent embedded `ReviewSummaryBody` tap target inside a Discover college card) was **dead on device** — both did `context.push('/colleges/detail/reviews', extra:{universityId, universityName})`, a deep nested go_router location whose intermediate `detail` builder re-runs without a `universitySlug` in `extra` and whose 3-level match was silently not surfacing `ReviewBreakdownScreen` at all. Both now push `ReviewBreakdownScreen` directly via `Navigator.of(context).push(MaterialPageRoute(...))` (the same pattern `help_screen.dart` / `call_request_sheet.dart` already use), which has none of that fragility. `university_detail_screen.dart` no longer imports `go_router` (that was its only use). The now-unused `/colleges/detail/reviews` `GoRoute`s in `app_router.dart` (both branches) are left registered — harmless, no caller. `dart analyze` clean on the touched files.
    - **Also 2026-09-08:** the persistent "See mentors from this college" CTA at the foot of `UniversityDetailScreen` (whose only action is `setState(_active = 'mentors')`) is now **hidden on the Mentors tab** — it was dead weight once you're already there. Shown on Overview / Reviews only (`if (_active != 'mentors')`); the scroll area picks up the bottom `SafeArea` inset itself when the CTA isn't there to provide it.
    - **Also 2026-09-08 (per screenshots):** two review-layout moves on `university_detail_screen.dart` + `review_breakdown_screen.dart`. (1) `ReviewBreakdownScreen` is now the **aggregate view only** — the "From the Reviews" section (the individual `ReviewCard` list + its inline write/edit button) was removed from it; that screen keeps just the score ring, "Rated by Category", "Student Experience Breakdown", and "Student Highlights". The individual review cards live on the **Reviews tab** instead, which was switched from `CompactReviewCard` (star + written comment only, silently dropped rating/tag-only reviews) to the full `ReviewCard` (badge · date · stars · tags · any comment) over **all** reviews — so a tag-only review like the VELS one is now visible somewhere again. `CompactReviewCard` in `review_widgets.dart` is now unused (left in place). `ReviewBreakdownScreen` dropped its `users_api`/`auth_controller`/`university_review_screen` imports and the `reviewsAsync`/`canReview` plumbing. (2) On the **Overview tab**, the structured-facts block (the Stream / Established `AppCard` + "Programs offered" chips) moved **above** the `ReviewSummaryCard` — it's the primary "what is this college" info; the review snapshot and description now follow underneath. Added an `if (rows.isNotEmpty)` guard so an empty facts card can't render at the very top. `flutter analyze` clean; `dart format` NOT run on `review_breakdown_screen.dart` (non-canonical at HEAD like `home_screen.dart` — would reflow the `CategoryRatingBar` subtitle lines) — edits were surgical.
  - Report reasons (`ReportReason` in `reports_api.dart`): "Spam" and "Inappropriate content" were two separate, overlapping-sounding picks — merged into one "Spam/inappropriate content" option (backend `INAPPROPRIATE` enum value is untouched, just no longer offered as a *new* pick — old reports keep their label). In the freed slot: **"Call ended before the booked slot"**, mapped to the backend's existing `CALL_DROPPED` reason — that value already existed (`ReportsService`/schema, added 2026-09-02-ish for exactly this case) but had never been wired into the mobile picker until now. No migration needed either way.
  - See the "Partial revert for MENTOR only" sub-bullet above for the Edit Profile change from this same pass.
  - Discover/Colleges search box only matched `University.name`. Widened (`university_list_screen.dart`) to also match `city`/`state` text — so a district/city term like "Basti" now finds colleges there. No new "district" field/data source added (there isn't one — only city/state exist on `University`); this is the closest real match without a bigger data project.
  - **District-name college search — real bug found+fixed 2026-09-08 (user report: "works on website, not in app").** The Discover-tab widening above was real but not the whole bug the user hit — that screen already loads the *entire* catalogue uncapped (`browse=true`) and filters client-side. The actual gap was **`CollegeSearchField`** (`college_search_field.dart` — the live-typing "search box" used by the mentor + aspirant onboarding wizards' College field, and the far more literal match for "search box" in the report): its backing call, `UniversitiesApi.search()`, sent a plain `?search=&limit=8` request — capped to the top 8 matches. Per `UniversitiesService.findAll`'s own doc comment (`backend/src/modules/universities/universities.service.ts`), backend `search` matches **`University.name` only** (city/state matching was deliberately tried and reverted there for giving confusing results on a name lookup) — a district term matches because many imported college names embed their full address/district as text (e.g. "XYZ Medical College, Basti"), the same reason the *website*'s `CollegeSearch.tsx`/`searchUniversities` finds it. But the website always sends `browse=true` (uncapped, same mode `UniversitiesApi.list` already used for Discover) and renders every match in a scrollable dropdown, while mobile's `search()` was capped to 8 and ranked name-starts-with-query first — a district term (which almost never starts a college's name) landed in the alphabetical "contains" bucket and was routinely pushed past position 8, so it silently never appeared. Fixed: `UniversitiesApi.search()` now also sends `browse=true` and takes the first 50 of the (already relevance-ranked) response client-side instead of relying on the backend's `limit` param — bounded against a pathological 2-char query matching thousands of rows, but generous enough that any real search surfaces its match. **Live-verified 2026-09-08** against production: `search=Pune` had 40 real matches; the old `?search=&limit=8` call only returned 8, `?browse=true&search=Pune` returns all 40. **Pushed to `feature/flutter-migration`.**
  - **Second, real district-search bug — screenshot-confirmed 2026-09-08 (Discover tab, "Medical" + "Diploma" + search "bast" → "No colleges found" despite two real matching colleges existing).** Not a stream-default problem (the user had explicitly picked Medical) — the client-side search on Discover only ever checks the flat `University.name`/`.city`/`.state` fields, but once a **curated degree** (Diploma, DNB, DM/MCh, etc.) is picked, the college list is narrowed to `GET /universities/curated`'s own dataset, which for many DNB/Diploma-granting facilities (typically standalone hospitals, not full "University" entities) carries a materially richer `label` than the base University row — e.g. base `University.name` = "District Male Hospital" (`city: null`, no district anywhere), while the curated `label` for the exact same id is "District Male Hospital, **Basti**, Uttar Pradesh". Live-confirmed: `GET /universities?browse=true&search=District Male Hospital` returns id `c84150b3-…` with no "Basti" anywhere on the row; `GET /universities/curated?stream=Medical&degree=Diploma&browse=true&search=basti` returns that same id with the district baked into `label`. So `matchesQuery` was checking fields that structurally can't carry district info for this class of college, while the one field that does (the curated `label`) was never consulted. Fixed (`university_list_screen.dart`): `matchesQuery` now also checks `curatedById[u.id]?.label` whenever a curated degree (`curatedKey != null`) is active. `flutter analyze` clean. Not click-tested (browser pane can't drive the Flutter canvas) — verified via the live API data + code trace, matching the exact repro in the user's screenshot. **Pushed to `feature/flutter-migration`.**
    - **2026-09-08 proper fix — `district` is now a real field on the `/universities` response.** `withSpecializations` (backend `universities.service.ts`) derives it from any of the college's programs' `description` (the same district text `findCurated`'s label uses — populated for the hospital-type DNB/DM-MCh/Diploma/MDS rows, null for MBBS-only colleges whose MD/MS source has no district). `findBySlug` computes it the same way. Mobile `University.district` + `University.locationLabel` (`"<district or city>, <state>"`, dropping the place when it duplicates the state or is already in the name); the Discover card's location line and the client-side search both use it now — so the district shows in the *list*, not only once a curated degree is picked. Backend `tsc` clean after `prisma generate` (2 pre-existing spec-file `uniqueId` errors unrelated); `flutter analyze` clean. Needs the backend deploy to land before the app sees it (mobile falls back to city/state until then).
    - **2026-09-09 — audit + name cleanup.** Full audit ([artifact report](https://claude.ai/code/artifact/71cebcfa-99b1-4b72-bde6-dabac4756e72)): of 10,262 active colleges, ~9,513 have a district in a real field (`city` or a program `description`), ~346 only inside the name, ~403 nowhere (almost all Medical — MBBS from the NMC seat matrix, which has no district column). Engineering/Law/Dental are ~100% covered via `city`; the gap is Medical-only. Migration `20260909120000_extract_location_from_university_names` (same class as `..._clean_university_name_import_junk`) pulls the location back out of `University.name`: (1) strips a trailing bare comma, (2) strips a trailing full state name / abbrev (`U.P.`/`M.P.`/etc.), (3) strips a trailing `, <district>` when the segment validates against the app's `kStateDistricts` list (765 districts, generated into the migration) and copies it onto `city` when `city` is empty. Dry-run vs prod: 130 rows for steps 1–2, ~492+ for step 3, 0 over-strips. End-anchored + length-guarded so a re-run is a no-op; non-district trailing segments (villages, addresses, institute-name commas) are left alone. Migration `20260909170000_extract_trailing_district_without_comma` does the **no-comma** case — `"DISTRICT HOSPITAL MALE BUDAUN"` → name `"DISTRICT HOSPITAL MALE"` + city `Budaun`. **Medical stream only** — Engineering/Law/Dental already have `city` for ~100% of rows, and their varied name formats mangled under the strip in testing (`"University of Lucknow"` → `"University of"`). Guards: keyword still present, length ≥ 6, result must not dangle on a connector/punctuation (`of`/`the`/`dist`/`-`/`,`). Medical dry-run: ~146 rows (partial gazetteer; full run more). **Still not done:** the ~140 genuinely-blank Medical districts (need an external source — queued task), and adding `cleanDistrict()` to `findCurated`'s label so the web form stops rendering `nan, PIN nan`.
    - **2026-09-09 — dedup + nan-state migration (`20260909200000_dedup_universities_drop_nan_state`).** Part A (deactivate 38 `state='nan'` junk rows) committed cleanly on the first deploy (`c47b87b`, prod active count 10262→10224). Part B (merge 68 true-duplicate rows — identical `lower(name)+lower(state)+lower(city)+stream`, e.g. "ACS Medical College and Hospital", Chennai ×2) is what the user's screenshot is about (still-visible dupes). It failed to deploy **twice**: v1 used `CREATE TEMPORARY TABLE` which the Supabase pgBouncer pooler drops between statements (`relation _dup_merge does not exist`); v2 (`c4e52c2`, temp-table-free — 68 `(loser,winner)` pairs inlined as a repeated `WITH m(...) AS (VALUES ...)` CTE per statement) failed on `operator does not exist: text = uuid` because **every id column in this schema is Prisma `String` = Postgres `text`, not `uuid`** (`universities.id`, `programs.university_id`, `reviews.university_id`, etc.) and the generator had cast the VALUES to `::uuid`. **v3 fix (this session): `sed 's/::uuid/::text/g'` across the migration file** (680 casts) — nothing else changed. **Deployed green (`0e215f6`) and verified live**: `_prisma_migrations` shows `20260909200000` `finished_at` set / not failed, 68 losers `is_active=false`, ACS loser's programs repointed, 0 `state='nan'` active, active-uni count 10224→10156. `railway.toml` startCommand **reverted to the plain `npx prisma migrate deploy && node dist/main.js`** (bundled with the pass-2 dedup migration below; the resolve-prefix was a one-shot, now no-op). `execute_sql`/`apply_migration` for the merge are classifier-blocked, so the Railway `migrate deploy` path is the only way to apply these.
    - **2026-09-09 — dedup pass 2 (`20260909210000_dedup_universities_same_or_null_city`).** Pass 1 only merged rows with an identical **non-blank** city, so it missed the very common seed artifact where the dup rows agree on one city but a copy has `city` NULL — e.g. ACS still showed **2** rows after pass 1 (Chennai winner + a `city=NULL` row whose district came from a program `description`). Pass 2 scope: active-uni groups sharing `lower(name)+lower(state)+stream` where the rows that *do* carry a city name **exactly one** distinct city (so no ambiguity) — **170 groups / 188 loser rows**. Winner = has-a-city, then most programs, then reviews, then oldest, then lowest id. Same temp-table-free idempotent structure as pass 1 (`::text` casts), plus one extra statement: backfill the winner's `city` from a loser when the winner's is blank. Groups where rows genuinely name 2+ different cities ("Government Medical College" across districts — 94 groups) and all-blank-city generic-name groups (12) are **deliberately left alone**. **First deploy (`4a738d5`) FAILED** — `duplicate key ... programs_university_id_name_key`: 18 winners have 2+ losers, and when two losers feeding one winner both have a program of the same name, the plain `UPDATE ... NOT EXISTS` guard (evaluated against the pre-UPDATE snapshot) moved *both*. **Fix (`76ae2f9`): the programs-move and saved_universities-move statements now use `WITH ... p_move AS (SELECT DISTINCT ON (winner_id, name) ... ORDER BY winner_id, name, id) UPDATE ... FROM p_move`** so exactly one row per `(winner, name)` moves and the trailing `DELETE` mops up the rest. **Deployed green and verified live**: `20260909210000` applied / not failed, active-uni count 10156→**9968** (188 losers deactivated), ACS collapsed to **1 row** (`202f74ac`, Chennai), 0 reviews/user_profiles/verification_requests/saved_universities left pointing at any deactivated row. `railway.toml` reverted to the plain start command (the resolve-prefix was a one-shot). **Duplicate landscape now:** every `lower(name)+lower(state)+stream` group where the city-bearing rows agree on one city is merged (0 left); what remains is **94 groups that genuinely name 2+ different cities** (real distinct colleges — "Government Medical College" per district, "Government Polytechnic" per city) and **110 all-blank-city groups** (e.g. "Apollo Hospital, Telangana" ×3 — no city to anchor identity, could be different branches; left for a future pass with an external source).
    - **2026-09-09 — district backfill (`20260909220000_backfill_missing_college_districts`).** After the dedup passes, **318** active colleges (305 Medical, 12 Dental, 1 Engineering) still had no district in any real field — blank `city` and no program `description` carrying one. All 318 resolved: **194 read from the college's own name** (matched against the app's 765-district list + a city→district alias table — "Government Medical College Machilipatnam" → Krishna, "Kokrajhar Medical College" → Kokrajhar), **124 from web search** of NMC / official college listings (Anna Gowri → Tirupati, MAHABODHI → Gaya, VELS → Tiruvallur, …). District names normalised to the app's list (Balasore, Sri Ganganagar, SAS Nagar (Mohali), YSR Kadapa, Komaram Bheem Asifabad); Telangana/AP use post-2022 names (NTR, Medchal-Malkajgiri). Institute-type rows (Tata Memorial, BARC, Command Hospital) placed in their host city's district. **Reviewed by the user via the audit artifact before applying** ([district-review](https://claude.ai/code/artifact/68aa869f-07bf-4272-9251-12a452fbd1fd)). Migration is one `UPDATE ... FROM (VALUES <318 (id, district)>) WHERE id = v.id AND city IS blank/'nan'` — `::text`-cast, idempotent (a re-run skips any row whose city is now populated), touches only those 318 ids. **Dry-run vs prod: 318 pairs, 318 ids exist + active + currently blank — exact match, no typos.** After this lands, **every active college has both a district and a state (100%)** — `no_state` was already 0 (the 38 stateless junk rows were deactivated by pass 1). Writes `universities.city`; the API's `district` field (derived from `programs.description`) stays null for these, and mobile `locationLabel` falls through district→city, so cards show "City, State". Generator + inputs: `scratchpad/{gen_backfill,match2,consolidate,finalize}.mjs`, `city_district.json`, `websearch_results.json`, `final_map.json`.
    - **2026-09-09 — "District Male Hospital" split (`20260909230000_split_district_male_hospital_agra_basti`), + a documented backlog.** User report: their review of "District Male Hospital, **Basti**" showed the college as **Agra**. Root cause (**pre-existing import bug, NOT from any 2026-09-09 migration** — the programs' `updated_at` is 2026-08-23): the original import created **one `University` row per generic institution name** and hung DNB/Diploma hospital-training seats from *different districts* off it. `c84150b3` "District Male Hospital" held a DNB seat at Agra's + a Diploma seat at Basti's + the reporting mentor's profile/verification/review (they're the Basti Diploma), and the card's `district` = first program's `cleanDistrict` = "Agra". A separate `a006a628` was the real Basti row (Diploma + DNB, both Basti, no user data). **Fix:** `a006a628` → `city='Basti'` + all of `c84150b3`'s user links (2 profiles, 1 verification, 1 review) repointed to it; `c84150b3`'s duplicate Basti Diploma dropped; `c84150b3` → `city='Agra'`, keeps only the DNB/Agra seat. Idempotent, dry-run verified (2 profiles / 1 verif / 1 review / 1 program — exact). **Backlog (deliberately NOT fixed — product call needed):** ~**73 more** active rows have programs spanning **2+ genuinely different districts** ("District Hospital" ×many in Karnataka, "Civil Hospital", "GMERS Medical College" across Gujarat, "Government Law College" across TN, "ESIC Medical College" Alwar||Jaipur, …) + ~33 fuzzier ones. All are generic-named DNB/Diploma *hospital training sites* — **0 saves, 0 reviews** across all of them (the one review was the case fixed above). Splitting them mostly *duplicates* real per-district rows that already exist, so the right fix is either merge-into-existing (per-row judgment) or a product decision that hospital training sites shouldn't be in plain Discover browse at all (they're already district-labelled in the curated per-degree picker). Scoping data + classifier: `scratchpad/{classify_conflated,conflated_raw,splits,setcity,skip_fuzzy}.{mjs,json}`.
    - **2026-09-11 — Basti's own Diploma program was `is_active=false`, hiding it from the curated College search (`20260911120000_activate_basti_district_male_hospital_diploma`).** User report: after the split above, searching "district mal" under Medical + Diploma in the onboarding wizard still didn't surface "District Male Hospital, Basti". Root cause: `UniversitiesService.findCurated` filters `Program.isActive: true` — Basti's own DIPLOMA program (`c87a78df-970e-4451-b9d6-bcdafe1210e4`, real data: `specializations: ["Tuberculosis & Chest Disease - DTCD"]`, `description` a full postal address) was seeded `is_active=false` on **2026-08-22** (pre-dates every migration in this repo — not something this session's earlier split broke) and has no active duplicate anywhere, so it was silently unsearchable/unbrowsable from day one. **Fix: flip that one row's `is_active` to `true`.** **Wider audit while investigating (found, NOT fixed — flagged separately):** **407** curated-degree programs (`DM/MCh` 207, `Diploma` 110, `DNB` 74, `MD/MS` 15, `M.Tech` 1) on otherwise-active universities are `is_active=false` with **no active sibling row** for the same university+degree — i.e. not dedup leftovers, just silently unreachable data, same shape as this bug. **Follow-up (same day, `20260911130000_activate_orphaned_curated_programs`): audited and fixed all 406 remaining.** For each of the 5 program names, checked: any empty `specializations` array (0), any test/placeholder/garbage name or specialization text via regex (0), and — because DM/MCh's 207 rows all had a blank `description` while only 8/108 *active* DM/MCh rows do — whether that's a real quality gap or cosmetic; it's cosmetic: `findCurated` already renders a blank description as "name, state" instead of "name, district, state" (the exact fallback 100% of MD/MS's own rows already rely on), so a blank description here just means "no district on file," same as MD/MS, not "bad data." Manually sampled rows are real, well-known institutions (SVIMS Tirupati, Sri Ramachandra Chennai, Father Muller's Mangalore, Assam Medical College, N.K.P. Salve Nagpur, …) with realistic specialization lists — clearly just orphaned, not garbage. Breakdown: DM/MCh 207, Diploma 109, DNB 74, MD/MS 15, M.Tech 1. Migration mirrors the Basti fix's guard (`is_active=false` + university active + no active sibling row for the same `(university_id, name)`) generalized across all 5 curated program names, so it only ever activates a program that's currently unreachable in curated search and never risks creating a second "active" row where a good one already exists.
  - **Third bug from the same "Medical + Doctorate" screenshot, found 2026-09-08: the Specialization pill showed raw curated program names (e.g. "MD - Bio-Physics") instead of a general specialization list.** `kCuratedDegreeMapByStream['Medical']` maps both `'Doctorate'` and `'Others'` to the `'MD/MS'` curated key — needed so their College picker still searches real MD/MS colleges — but per `web/components/MentorForm.tsx`'s own explicit special case (doc comment: *"Others and Doctorate both use the old static MEDICAL_SPECIALIZATIONS picklist (not tied to the MD/MS dataset), always in full regardless of which college is picked, per explicit request"*), the **Specialization** list for these two must show the flat, general `kMedicalSpecializations` list instead — never the raw curated per-college MD/MS program strings. Mobile's `specializationOptions` in `university_list_screen.dart` had no such carve-out, so any Medical `curatedKey != null` degree (MD/MS, DNB, Diploma, DM/MCh, **and Doctorate/Others**) all showed the raw curated list. Fixed: added an `isDoctorateOrOthers` check that routes Medical's Doctorate/Others to the flat list (matching MentorForm.tsx exactly), while every other Medical curated degree (MD/MS, DNB, Diploma, DM/MCh) keeps showing its own real per-college data, unchanged. `matchesSpecialization` needed the identical carve-out — once the filter value is a flat `kMedicalSpecializations` string like "Cardiology" instead of a raw curated string like "MD - Cardiology", the curated-lookup branch would never match, silently returning zero colleges for any picked specialization; it now falls through to checking `University.specializations` instead, same as every other non-curated Medical branch. `curatedDegreeKey`/College-picker filtering (`matchesDegree`) is untouched — only the specialization list + its filter changed. `flutter analyze` clean; onboarding wizards (`mentor_onboarding_screen.dart`/`aspirant_onboarding_screen.dart`) already had this exact carve-out (`_needsMedicalStreamWideSpecialization`) — the bug was isolated to the Discover screen. **Live-verified 2026-09-08** the raw curated data really does look like "MD - Anaesthesiology"/"MD - Bio-Chemistry"/etc. (`GET /universities/curated?stream=Medical&degree=MD/MS&browse=true`) — confirming this was genuinely showing MD/MS clinical program names, not a general list, before the fix. Not click-tested (browser pane can't drive the Flutter canvas).
  - **Dental (and Engineering/Law) + Doctorate/Others showed no Specialization field at all (2026-09-11, per report "see in dental doctrate specialization is not showing").** `kCuratedDegreeMapByStream['Dental']` only maps `'MDS'` — `'Doctorate'`/`'Others'` have no curated key, so `_needsSpecialization` (mentor/aspirant onboarding wizards) evaluated false for them and the field never rendered, even though `web/components/MentorForm.tsx` already shows one for exactly this case via a separate mechanism mobile lacked entirely: `hasStreamWideSpecialization = isDoctorateOrOthersDegree && STREAMS_WITH_COLLEGE_DATA.has(stream)` where `STREAMS_WITH_COLLEGE_DATA = {Dental, Engineering, Law}` — populated by unioning `.specializations` across every curated-degree-key the stream *does* have (for Dental, just MDS), the same "stream-wide union" idea mobile already had for Medical's Doctorate/Others (`streamWideSpecializationsProvider`, added earlier for the Medical-specific bug below). **Fix:** ported the mechanism instead of writing a new one — new `kStreamsWithCollegeData = {'Dental','Engineering','Law'}` + `needsStreamWideSpecialization(stream, degree)` in `profile_options.dart`; both onboarding wizards' `_needsSpecialization`/`_specializationOptions` now OR in `needsStreamWideSpecialization(...)` and, when true, watch the *existing* `streamWideSpecializationsProvider` with that stream's full curated-degree-key set (no new provider). Also applied the same fix to the Discover college-filter screen (`university_list_screen.dart`), which had the identical gap (`curatedKey == null` for these combos → Specialization pill silently hidden) — added a `needsGenericStreamWide` branch alongside the existing Medical Doctorate/Others carve-out, folded into `curatedLoading`. `flutter analyze` clean on all four touched files. **Follow-up same day — the field still didn't render after this fix.** Real root cause: a second, independent bug in `streamWideSpecializationsProvider` itself (pre-existing, silently masked for Medical). Its family key is a Dart record `({String stream, List<String> curatedDegrees})`; record `==` delegates field-wise, and `List<String>`'s `==` is identity equality, not content equality — every call site built the `curatedDegrees` list fresh via `.toSet().toList()` **on every widget build**, so each build's family argument was a "new" key even for the same stream. Riverpod never matched it back to an in-flight/completed provider instance: each rebuild started a brand-new fetch that got abandoned (autoDispose) the moment the next rebuild ran, so the watched value was stuck at `AsyncLoading` forever and never rendered. Medical's Doctorate/Others masked this completely — its result is always unioned with the static `kMedicalSpecializations` list, so the picklist was never empty even though the live fetch silently never resolved. Dental/Engineering/Law have no such static fallback, so the bug was 100% reproducible there: zero options → `specializationOptions.length > 1` false → pill/field never shown, exactly the user's report, confirmed via a hard-refresh retest that ruled out stale-build caching first. **Fix:** new `kCuratedDegreesForStream` map in `profile_options.dart` — the per-stream curated-degree-key list computed **once** (`List.unmodifiable`, module-level), so every call site now passes the *same instance* for a given stream instead of rebuilding it. All 4 call sites (both onboarding wizards' Medical + generic branches, the Discover filter) switched from `kCuratedDegreeMapByStream[stream]!.values.toSet().toList()` to `kCuratedDegreesForStream[stream]!`. `flutter analyze` clean. Not click-tested (browser pane can't drive the Flutter canvas) — this is a real, previously-undetected Riverpod family-key gotcha (unstable `List` in a family argument), not a logic gap; worth remembering for any future `.family` provider whose key embeds a `List`/`Map`/mutable collection.
  - **Verification-document camera never opened during registration (2026-09-11, per report "while applying verification document the camera is not opening").** Both photo-upload spots — the mentor onboarding wizard's College Details/verification step (`mentor_onboarding_screen.dart`, card literally labelled "Take picture to upload") and the standalone `VerificationScreen` (reachable later from Profile) — called `ImagePicker().pickImage(source: ImageSource.gallery, ...)` only; `ImageSource.camera` was never wired up anywhere in the app, so there was no code path that could ever open a camera — tapping the card always opened the gallery/file picker, contradicting its own label. Compounding it on Android: `AndroidManifest.xml` never declared `android.permission.CAMERA`, which `image_picker` 1.x requires for `ImageSource.camera` to work at all — so even changing the source alone wouldn't have been enough. **Fix:** both `_pickImage()` methods now show a small bottom sheet ("Take a photo" / "Choose from gallery") before calling `pickImage`, so a real camera capture is available for the first time, with the working gallery flow kept as the explicit second option (not a regression for mentors on cameraless devices, e.g. some tablets/emulators). Added `<uses-permission android:name="android.permission.CAMERA"/>` + `<uses-feature android:name="android.hardware.camera" android:required="false"/>` (the `required="false"` keeps the app installable on camera-less devices — the gallery option alone still gets you verified). iOS `Info.plist` already had `NSCameraUsageDescription` declared (unused until now) — no iOS change needed. `flutter analyze` clean; not click-tested (browser pane can't drive the Flutter canvas, and a camera doesn't exist in a headless browser anyway) — needs a real-device check.
  - **Role Selection + Profile Setup copy edits (2026-09-11, per annotated screenshots).** Role Selection (`role_selection_screen.dart`): removed "You can update this later." from the subtitle (kept "This helps us show you the right content."); Aspirant card copy → "Get guidance from verified mentors, learn from their real experiences. Make confident choices."; Mentor card copy → "Currently studying or graduated - guide aspirants and turn your experience into income." Profile Setup (`profile_setup_screen.dart`): the display-name field label changed from "Display name *" to "Alias/Display name *". Pure copy, no logic changed either place.
  - **Mentor's 18+ age-confirmation dialog fired one step too early (2026-09-11, per report "the warning message ... in mentor register process the message comes in 3rd step itself but it should come in last step").** The aspirant wizard's `_finish()` asks `showAgeConfirmationDialog` at its actual last step (Avatar). The mentor wizard's equivalent, `_confirmAgeThenSave`, was wired to the Current Status → Avatar transition (`_next()`'s `_step == 3` branch) instead of the wizard's real last step (Verify, index 5) — two steps (Avatar, Verify) still followed it, so the legal gate landed mid-wizard rather than at the actual point of submission. **Fix:** moved the age gate out of the Current-Status save path (that step now just calls `_saveProfile()` directly, unchanged otherwise — the profile still needs saving there since Avatar/Verify depend on it existing) and into `_submitVerification()`, gating the wizard's actual final action exactly like the aspirant side gates its final `_finish()`. `flutter analyze` clean.
  - **Mentor onboarding's Specialization field couldn't find a specialization outside the picked college's own list (2026-09-11, per report: "in website when user click their college the specialization list show according to the college but if user want other specialization if they search the other specialization in the degree shows but in app the feature is missing").** Web's MentorForm.tsx: the Specialization combobox defaults to the picked college's own programs (`browseSpecializations`), but once the mentor actually types something, `options` widens to `browseSpecializations ∪ allSpecializationsForDegree` (every specialization known anywhere for that stream+degree) — so a real specialization their college's curated data just isn't mapped to yet is still searchable, not silently rejected. Mobile's `OnboardingSearchableField`/`_SearchableOptionSheet` only ever filtered the single fixed `options` list it was opened with — no such widen-on-search step existed at all, so a mentor typing a specialization their college's own list didn't have would just see "No matches" even though it's a real, valid specialization recorded against other colleges for the same degree. **Fix:** added an optional `widenedOptions` param to both `OnboardingSearchableField` and `_SearchableOptionSheet` — with nothing typed the sheet still shows/filters `options` (the narrow, college-scoped list) as before; once the query is non-empty, it filters `widenedOptions` instead when provided. `MentorOnboardingScreen` wires this using the existing `specializationsForDegreeProvider` (already fetched for the "college has zero programs" fallback case, now always watched via new `_curatedDegreeWideOptions()` so it's ready regardless of whether the narrow list is empty). Aspirant's own Specialization field needed no change — `AspirantForm.tsx` (and mobile's `aspirant_onboarding_screen.dart`) already show the full degree-wide union unconditionally with no college-scoping step to widen past. `flutter analyze` clean; not click-tested (browser pane can't drive the Flutter canvas).
  - **Aspirant onboarding's Specialization field showed before a college was even entered (2026-09-11, per request: "in all stream except ug degree specialization field should [show] after selecting college").** `MentorOnboardingScreen` already gates its Specialization block on `_collegeNameController.text.trim().isNotEmpty` (only renders once a college is typed/picked); `AspirantOnboardingScreen`'s equivalent block had no such gate — it rendered as soon as `_needsSpecialization` was true, i.e. the instant a qualifying stream+degree was picked, with the College field still empty. Matches neither the mentor wizard's own behavior nor the request. **Fix:** added the identical `_collegeNameController.text.trim().isNotEmpty` gate to the aspirant wizard's Specialization block. UG-equivalent degrees (MBBS/BDS/UG) were already excluded via `_needsSpecialization` itself, unaffected by this change. `flutter analyze` clean.
  - **"Mentor gets message-notification sound, aspirant doesn't" investigated (2026-09-11) — no code-path bug found; added per-device push failure logging so a real recurrence is diagnosable.** Traced the exact reported chat ("okay"/"hoo"/"jii"/"notification varudha"/"giii", channel `403f34c2…`, mentor → aspirant) in the live DB: `NotificationsService.send` correctly created the in-app "New message" `Notification` row for the aspirant for **every one** of those messages, at the right timestamp — `chat.service.ts`'s `notifyOthers` has no role-based branching at all (recipient = "whichever participant isn't the sender", symmetric for both roles), so the in-app/notify-creation path is confirmed NOT the bug. The aspirant did have a registered `PushToken` at the time. Couldn't get FCM's actual per-device send result for that specific historical send — Railway log retention had already rotated past it (10+ redeploys since). Root cause is most likely at the FCM delivery step itself (a stale/not-yet-propagated token, or a device-side push issue — the reporting device is a tablet, possibly without full Google Play Services) rather than app or backend logic, but this can't be proven without a fresh occurrence. **Real gap found and fixed regardless:** `pushToDevices` only ever logged a bare `success=N failure=N` count — a failure for any reason OTHER than the two known "stale token" FCM error codes was completely invisible, logged nowhere. Added a per-response `logger.warn` with the actual FCM `error.code`/`error.message` for every failed send, so if/when this recurs, the real cause (quota, credential mismatch, internal error, etc.) is visible in logs instead of an unexplained gap. `pushBulk` (admin broadcasts, a different code path from 1:1 chat messages) left as-is — out of scope for this report. `tsc --noEmit` clean (2 pre-existing, unrelated `uniqueId` fixture errors in `mentor-response.spec.ts`/`user-response.spec.ts` predate this change, confirmed via `git stash`).
  - **RLS enabled on all 26 `public` tables (2026-09-11, `20260911140000_enable_rls_all_tables`), following user + developer sign-off on a published impact analysis.** This app has never used Supabase Auth or RLS (own phone-OTP+JWT scheme instead) — `ai/SECURITY.md` finding S-6 already flagged this as safe-today-but-structurally-fragile after the chat migration, and this session re-verified it live before acting: `anon`/`authenticated` have **zero grants** on any table (`information_schema.role_table_grants`), and the backend's DB connection authenticates as `postgres`, which has `rolbypassrls=true` (confirmed via `pg_roles` before applying) — so nothing in the actually-working app depended on RLS being off, and enabling it changes no runtime behavior. The only client-side use of the anon key anywhere (mobile's `chat_thread_view.dart` Realtime **Broadcast** "new message" ping, handed out by `ChatService.connectionInfo()`) doesn't read/write any table and isn't RLS-gated at all. web/ and admin/ never touch Supabase directly (grepped, zero references). No policies were added — deny-by-default for `anon`/`authenticated` is exactly the desired backstop against a future accidental grant (e.g. someone using Supabase Studio's table editor on one of these tables), which was the actual gap, not a live leak. Storage (verification docs, avatars) is a separate policy system (`storage.objects`), untouched by this migration. Live-verified post-deploy: `pg_class.relrowsecurity = true` for all 26 tables, deployment `SUCCESS`, and a live API call (`GET /universities/curated`) returned 200 immediately after — no permission errors. Full trace published to the user as an artifact ("RLS Impact Analysis") before applying.
  - **A write-once college review had no way to see what you'd actually submitted (2026-09-12, per report: "it shouldn't show review form again... it should show you have already submitted and show their review what they given").** The write-once feature (2026-09-09) correctly blocked re-submission everywhere — investigated thoroughly first (traced a real just-submitted review live in the DB, confirmed `hasReviewed`/`hasReviewedOwnCollege` both correctly return `true` for it, no drift between `reviews.university_id` and the reviewer's current `profile.universityId`) — but every "already reviewed" surface (the Profile "Rate Your College" row, the college detail Reviews tab, `openUniversityReview`'s own re-open guard) only ever showed a locked row or a snackbar; nothing displayed the review's actual content, even though the backend already had a `GET .../reviews/mine/detail` route sitting unused for exactly this (kept "harmless" when the write-once feature removed the old edit flow, per that session's own note). **Fix:** restored a client method for it — `UniversityReviewsApi.findMine()` + `myUniversityReviewProvider` (both had been deleted alongside the removed edit path) — and added a new read-only `MyUniversityReviewScreen` (route `/college-review/mine`) that renders the submitted review's full 13 answers, tags, and free-text body. `openUniversityReview()` now pushes this screen instead of showing a snackbar when `hasReviewed` is true; the Profile row unlocks (was `locked: true`) and both it and the Reviews-tab button now always call `openUniversityReview`, which internally decides whether to show the write form or the read view — removing three separate "already reviewed" special-cases in favor of one shared decision point. `flutter analyze` clean; not click-tested (browser pane can't drive the Flutter canvas) — needs a real device with the rebuilt app.
  - **Mentor Sessions tab now groups by student, reversing the earlier "mentors deliberately unaffected" decision** (per explicit follow-up — a mentor with a repeat student was seeing every past chat/call as its own separate card, reading as a messy wall of near-duplicates). `_groupAllSessionsByStudent` (mirrors the aspirant-side `_groupAllSessionsByCounterpart`, keyed by `aspirantId` instead of `mentorId`) replaces the old `_groupActiveSessions` (deleted, along with the now-unused `_MergedSessionCard`). New `_MentorSessions`/`_MentorStudentRow` — one row per student, same shape as `_AspirantMentorRow`: avatar, name, a status subtitle from the latest session, and a **single** `_SessionActions` (dense) for that student's most-urgent active session. With no active session, the row just opens "History with {student}" (`showMentorSessionHistory`, now takes an `isMentor` flag so the mentor side can reuse the same sheet the aspirant side already had). **Single-primary-action fix (2026-09-08):** the row originally looped `_SessionActions` over *every* active session — a test student with 8 open chats + a pending call rendered 8 chat icons + Reject/Accept, overflowing the row. Now `_pickPrimaryAction(activeSessions)` (top-level in `session_list_screen.dart`) picks one by urgency — pending AUDIO_CALL (Reject/Accept) → live AUDIO_CALL (Join) → active CHAT (one Open-chat icon) → null (history chevron), most-recent within a tier. Everything not picked stays one tap away via the row → history. `dart analyze` clean; the aspirant row was never affected (it always showed exactly one call + one chat icon).
  - **History sheet is calls-only now (2026-09-08, per request).** `showMentorSessionHistory` filters `sessions` to `type == 'AUDIO_CALL'` before sorting/rendering — a CHAT session card in there just carried a redundant "Open Chat" button (you're already in the thread). Empty → an `EmptyState` ("No calls yet · Past audio calls with this person will show up here."). Both call sites (aspirant `SessionChatScreen` history action, mentor `_MentorStudentRow`) are unchanged — they still pass the full list; the sheet does the filtering. `dart analyze` clean.
  - **Mentor can start a chat with any student on their Sessions tab (2026-09-08, per request — "for mentors the session screen only allows the call option, they should be able to chat or call anyone").** Two parts. **(1) Mentor-initiated chat — new backend capability.** Aspirants normally open the first chat (`POST /sessions` is aspirant-scoped — a mentor calling it 404s on `mentorsService.findById`). New `POST /sessions/chat-with/:aspirantId` (`SessionsService.startChatWithStudent(mentorId, aspirantId)`): caller must be `role === MENTOR`; they must **already share ≥1 session** with that student (any type/status — "message someone you're working with", not cold outreach / people-search); block-checked either direction (404-not-403, same as `create`); **chat only** — a mentor never initiates an `AUDIO_CALL` (calls are booked + paid by the aspirant, mentor is never billed — locked pricing rule, unchanged). Idempotent: returns the existing active CHAT thread if there is one, else creates one exactly as `create`'s CHAT branch does (`ratePerMinuteMinor: 0`, `ensureChannelForSession`, status → `ACCEPTED` + `respondedAt`, `MESSAGE` notification to the student "A mentor started a chat with you."). `UserRole` added to the `@prisma/client` import in `sessions.service.ts`. Mobile: `SessionsApi.startChatWithStudent(aspirantId)` (`POST /sessions/chat-with/:id`, same backend-message-surfacing catch as `create`). **(2) Mentor Sessions row — persistent chat icon + the call action, not one-or-the-other.** `_MentorStudentRow` (now a `ConsumerWidget`) always renders a filled-green chat `_RowIconButton` → `_openChat`: opens the existing CHAT thread in the group if there is one, else calls `startChatWithStudent` to find-or-create. Beside it, **only when there's a pending/live call**, the `_SessionActions` dense control (Reject/Accept for pending, Join for live). `_pickPrimaryAction` → `_pickPrimaryCall` (dropped the CHAT fallback — chat is the persistent icon now); the bare history chevron for "no active session" is gone (row-body tap still opens the calls-only history sheet). So a student with a pending call now shows **both** "open chat" and "Reject/Accept", where before the call won and chat was unreachable from the row. Backend `tsc` + mobile `dart analyze` clean. **Live-verified against the real deployed backend + DB** (mock-OTP, real mentor "Gentle Wolf #6378" `+919876543210`): create → new `CHAT`/`ACCEPTED`/`rate 0` session + linked `SESSION` `chat_channel`; idempotent repeat → same id; find branch (student with an existing chat) → returns the existing id; no shared session → 403; aspirant caller → 403; self → 409. Throwaway created session + channel deleted afterward via the backend's own `PrismaPg` adapter (bare `new PrismaClient()` fails — Prisma 7 needs the adapter; Supabase-MCP and generic node writes to user/auth data are classifier-blocked).
    - **Aspirant `POST /sessions` CHAT is now idempotent too (2026-09-09, from a device report: "Could not start chat: … You already have an active chat with this mentor" when tapping a mentor's chat icon on the Sessions tab).** The aspirant create path threw a 409 when an active CHAT already existed, and `startChatWithMentor` (mobile) recovered by catching the 409 and scanning `GET /sessions` for the existing row — but `SessionsApi.list()` sends no cursor, so it only ever sees the newest `DEFAULT_LIMIT = 20` sessions; on a heavy test account the old CHAT had fallen off page 1, the scan came up empty, and the 409 surfaced as a toast. (Notifications kept arriving because those are call-flow pushes, independent of the chat session being fetchable.) Fix: `SessionsService.create`'s `existingActive` branch now `return this.toResponseById(existingActive.id)` for `dto.type === CHAT` — find-or-create, mirroring `startChatWithStudent`. A second `AUDIO_CALL` while one is outstanding still 409s ("You already have an active call request with this mentor" — byte-identical, just the `else` now). The mobile 409-recovery in `startChatWithMentor` is left as a vestigial fallback (its doc comment updated). Live-verified against the real DB: 1st CHAT POST → 201 new id; 2nd + 3rd → 201 **same id**, DB still holds exactly one CHAT session for the pair.
    - **`SessionsApi.list()` now walks all pages (2026-09-09, follow-up).** It sent no `cursor`, so every consumer (both Sessions tabs' grouping, the Home "pick up where you left off" strip, `startChatWithMentor`'s 409-recovery) only ever saw the newest `DEFAULT_LIMIT = 20` sessions — whole mentor/student relationships silently dropped off a heavy account. `list()` now loops `GET /sessions?limit=50&cursor=…` following `nextCursor` until it's null (20-page / 1000-row runaway guard), accumulating the full set. A single user's session count is bounded so this is 1 request for almost everyone, a few for a heavy tester. Backend `findAll` already implemented the cursor contract (`?cursor`/`?limit` on `ListSessionsDto`, `nextCursor` in the response) — no backend change. Live-verified: aspirant `de1d21dc…` (36 sessions in the DB) → the walk returns 20 + 16 rows across 2 pages, `nextCursor: null` on page 2, 36 total. `MentorsApi.list()` still has the same uncapped-first-page bug — untouched here. (The `list()` code change was swept into commit `68a55f9` — labelled "Saved heart on Discover/Mentors" — by a concurrent session's broad `git add` in the shared tree; the code is correct and on `origin`, just under a misleading message.)
    - **`MentorsApi.list()` now walks all pages too (2026-09-10, from a client report "students can't see all the mentors — older accounts are missing").** Root-caused: `MentorsApi.list()` (`mentors_api.dart` — the Mentors tab's `mentorsListProvider` + the Home "Top mentors for you" strip) sent no `limit`/`cursor` and read `data` once, so it only ever saw `MentorsService.findAll`'s `DEFAULT_LIMIT = 20` rows, ordered `createdAt desc` — i.e. **only the 20 newest mentor accounts**. Measured against prod: **132** listable mentors (active, not banned, not deleted, all have profiles), so **112 were invisible** in discovery purely from the missing pagination; the oldest mentor on page 1 had signed up that same morning. Same fix as `SessionsApi.list()` above — loop `GET /mentors?limit=50&cursor=…` following `nextCursor` (20-page / 1000-row runaway guard), accumulating the full set; the discovery filters (`stream`/`qualification`/`specialization`/`language`/`universityId`) ride on every page. Backend `findAll` already had the cursor contract (`cursor`/`limit` on `ListMentorsDto`, `nextCursor` in the response) — **no backend change**. Live-verified against the shared DB: old call → 20 rows + a non-null `nextCursor` the app ignored; new walk → 3 pages → **132** unique mentors, matching the DB count exactly. `dart analyze` clean. Note: this is still a client-side "fetch everything" — a mentor roster in the thousands will want real server-side ranked pagination + infinite scroll, but 132→thousands is a long runway. Null-profile theory checked and ruled out (0 listable mentors have a null `UserProfile`).
  - **College review form is now 13 questions, not 12** (new Q5 "Restroom Facilities", per explicit request — the original Q5–Q11 choice set all shift down to Q6–Q12, Q12 overall-star becomes Q13). New nullable `Review.restroomFacilities` column (migration `20260907120000_add_review_restroom_facilities`, applied to prod — plain additive `ADD COLUMN`, and since the Supabase `apply_migration` path used to apply it doesn't share Prisma's own migration-history table, a matching row was inserted into `_prisma_migrations` by hand so a future `prisma migrate deploy` from any session doesn't try to reapply it). Single-choice, 5 options (`CLEAN`/`ADEQUATE`/`INCONSISTENT`/`POOR`/`UNUSABLE`, best→worst same as every other Q5–Q12 field), added to `REVIEW_CHOICE_FIELDS` so `reviewSummary()`'s `choiceDistributions` picks it up with no other service change needed (the aggregation loop is already field-name-generic — only the `select` in the summary's batch query needed the new column added explicitly). `CreateUniversityReviewDto` gained `@IsIn(RESTROOM_FACILITIES) restroomFacilities!: string` (required, same as its six siblings); `create`/`update` both spread the DTO directly onto the Prisma row so no mapping code was needed there either. Mobile: `kReviewChoices` gained the new spec (renders automatically — the form loops the list and auto-numbers by index, `_ChoiceCard(index: i + 5, ...)`); `UniversityReview`/`UniversityReviewDraft` (`university_reviews_api.dart`) gained the field; `university_review_screen.dart`'s `_total` bumped 12→13 and the two hardcoded field lists (init/submit) got the new line. The aspirant-facing "Student Experience Breakdown" (`review_breakdown_screen.dart`) needed **no changes** — it already iterates `kReviewChoices` generically against `summary.choiceDistributions[spec.field]`, a plain string-keyed map on both ends. Backend `tsc` + mobile `dart analyze` clean; DB column existence verified live post-migration. **Live-verified 2026-09-07** against the real deployed backend, using the same real MENTOR test account ("cosmic blade") reviewing their own linked college: `POST .../reviews` with all 13 fields → 201, `restroomFacilities: "ADEQUATE"` echoed correctly; `GET .../reviews/summary` → `choiceDistributions.restroomFacilities: {ADEQUATE: 1}`; `PATCH .../reviews` changing it to `"CLEAN"` → 200, summary updated to `{CLEAN: 1}` with `reviewCount` still 1 (no duplicate row); an invalid code (`"SPARKLING_CLEAN"`) → 400 with the expected `@IsIn` message. The test review was deleted afterward (summary back to `reviewCount: 0`) — it was real content on a real college's public review board, not something to leave behind.
  - **Payout minimum removed, replaced by a weekly cadence** — see the `payouts` module bullet above for the full design (`PAYOUT_REQUEST_COOLDOWN_DAYS = 7`, no minimum amount). Mobile copy updated to match: the withdraw confirm dialog (`wallet_screen.dart`), the mentor-only "How and when do I get paid?" Help Centre FAQ, and `PayoutsApi.requestPayout`'s doc comment. Backend `tsc` clean. **Live-verified 2026-09-07** against the real deployed backend + real DB, using a real mentor with genuine unpaid earnings ("cosmic blade", ₹60 unpaid `SESSION_CREDIT`): first `POST /payouts` → 201 (`amountMinor: 6000`); an immediate second call → 409 "already have a payout request in progress" (the pre-existing open-request guard, confirmed unaffected); that request's status flipped to `FAILED` directly in the DB (no admin API access available to this session) to clear the open-request guard and isolate the cooldown check — a same-moment third call → 400 "You can request a payout once every 7 days" with the correct next-eligible timestamp; the same request's `created_at` backdated 8 days → a fourth call → 201, correctly past cooldown. Both test `PayoutRequest` rows deleted afterward (back to 0 for this mentor) — no real wallet debit ever occurred (only a `COMPLETED` transition triggers that, never invoked here).
- **Second UX-fix batch from the same annotated-screenshot review (2026-09-07):**
  - **"Rate Your College" now always shows on the mentor Profile menu** instead of disappearing until eligible (was `if (isMentor && isVerified && universityId != null)`). `_MenuRow` gained `locked`/`subtitle` params (dimmed icon+text, lock glyph instead of the chevron) — an ineligible mentor sees the row greyed out with "Verify your account first" (routes to Verification on tap) or "No college linked to your account yet" (shows a support-message snackbar — a verified mentor with no linked college is an edge case with no self-service fix), so the feature reads as "locked," not "missing."
  - **Aspirant onboarding "Preferred Timing" capped to 2**, matching the mentor wizard's own identical step exactly (`maxSelections: 2` on the shared `OnboardingChipGroup`).
  - **"Top colleges for you" is now rating-ranked on the aspirant Home too**, not alphabetical. `GET /universities/top-for-mentor`'s `@Roles` widened from `MENTOR` to `MENTOR, ASPIRANT` — the service method (`UniversitiesService.topForMentor`) was already role-agnostic (only ever reads the caller's own `profile.stream`/`universityId`), so no service change was needed, just the guard. Mobile `home_screen.dart` now reads `topCollegesForMentorProvider` (kept under its original name — same endpoint path, renaming would've been pure churn) and falls back to the old alphabetical set only when the ranked list is empty (no stream yet, or nothing in it reviewed yet), so the rail never just goes blank for a new user. The card itself was unchanged in this pass — this was about fixing the *ranking*, not the visual (the `★ X.X (N)` rating row was added to it later, 2026-09-08 — see the bullet directly below).
  - **`top-for-mentor` now always populates the rail (2026-09-08, per request "make that active always for all mentors same as student").** The mentor Home only ever hid the whole "Top Colleges For You" section when the endpoint returned `[]` (no stream, or nothing in the stream reviewed yet) — the aspirant Home patched that gap client-side (`_collegesForYouFallback`), the mentor Home didn't. Rather than duplicate the client fallback (and pay the ~5.5 MB `browse=true` catalogue fetch on the mentor Home just for it), `UniversitiesService.topForMentor` grew a **server-side alphabetical fallback**: no `profile.stream` → return the top `TOP_FOR_MENTOR_LIMIT` active colleges by name (minus the caller's own); stream set but nothing in it reviewed → same, scoped to that stream. Fallback rows carry `rating: null` / `reviewCount: 0` so the card just omits the star row. Only genuinely `[]` when the catalogue has zero matching active colleges. The ranked path (reviewed colleges, rating-desc) is unchanged when reviews exist. Benefits both Home screens (role-agnostic method); the aspirant's client fallback is now near-dead but kept as belt-and-suspenders. `mentor_landing_screen.dart`'s stale "hidden until there's real data — no fabricated filler" comment updated. Live-verified against the real DB on 3 mentors: Design (1 college, 0 reviews) → 1 row `rating:null`; Medical (3 reviewed) → 3 rows ranked 5/4/3; no-stream → 8 alphabetical `rating:null`. `tsc` + `dart analyze` clean.
  - **University rating row on the Home college cards, both roles (2026-09-08, per request "bring the rating into the university card same as mentor card in the home screen for both mentor and students").** The aspirant Home's `_CollegeSpotlightCard` (`home_screen.dart`) previously showed no rating at all; it now renders a `★ X.X (N)` row (star `Icons.star_rounded` 13px `AppColors.warning` + `rating.toStringAsFixed(1)` + `(reviewCount)`) under the `stream · state` line, matching the mentor-card style. Shown only when `university.rating != null` — i.e. reviewed colleges from the ranked `top-for-mentor` feed; the `browse=true` alphabetical fallback leaves `rating` null and the row is simply omitted. The "Top colleges for you" rail's fixed height went 128→150 (both the skeleton-loading `SizedBox` and the `ListView.separated` `SizedBox`) to fit the extra line. The mentor Home's `_TopCollegeCard` (`mentor_landing_screen.dart`) already showed a bare `★ X.X`; it now appends `(reviewCount)` too, so the two rails' rating chips read identically. `flutter analyze` clean on both files; **`dart format` deliberately NOT run** on `home_screen.dart` — it is not kept canonically formatted (a concurrent session committed it mis-indented) and running it reflows ~240 unrelated lines (see the "onboarding avatar step" bullet for the same precedent). Edits were surgical.
  - **18+/consent checkbox added to both web enrollment forms** (`AspirantForm.tsx`, `MentorForm.tsx`), on the final step right before Submit — required, blocks submission until checked. Deliberately **not** a literal "I am 18+" claim (the source reference was from an unrelated travel site) — aspirants can legitimately be 15-17 (Higher Secondary/12th is a valid qualification), so that would misstate reality for a real chunk of the audience. Wording instead: "I have read and agree to Uniscope's Terms & Privacy Policy [linked to /terms, /privacy]. If you're under 18, please use Uniscope only under the supervision of a parent or legal guardian." New shared `ConsentCheckbox` in `form-bits.tsx`. **Mobile equivalent added 2026-09-08** (`showAgeConfirmationDialog` in `onboarding_widgets.dart`) — a modal shown right before the profile is submitted in *both* the aspirant wizard (`_finish`, covers Finish + Skip-for-now) and the mentor wizard (`_confirmAgeThenSave`, leaving the Current Status step — the last data step): title "Age confirmation (18+)", the same guardian-supervision notice worded with "Uniscope", a required "I have read and agree to the above." checkbox, Cancel / Continue. This mirrors the wording the user saw on the deployed `zenter.in` enrollment site (which is a **separate codebase, not in this repo** — its copy still incorrectly says "Zenter" and can't be fixed from here; flagged to the user).
  - **Aspirant onboarding's Academics step rebuilt as a real replica of the web enrollment form's own "Academics" step** (`AspirantForm.tsx`) — the old mobile flow had this backwards (a flat, Medical-shaped qualification picklist asked *before* the stream, on its own separate step) and the old "Current Status" (Currently Studying/Graduated) chip was traced to `_finish()` and found to never actually be sent to the backend at all — pure dead UI, silently discarded every time. Fixed by merging what were two separate steps ("Academic Qualification" + "Stream / Field of Interest") into one **Academics** step, reordered and gated exactly like the website: Field of interest (stream) picked first → Current qualification's options depend on it (`degreesForStream(stream)`, "Higher Secondary (12th)" always prepended) → College (shown for any qualification except 12th, via `CollegeSearchField` — the same find-or-create search the mentor onboarding wizard already used, now reused rather than duplicated) → Specialization (Medical only, past 12th/MBBS — same scope limitation the mentor wizard already has; mobile has no curated-per-degree specialization data source for other streams yet) → "Course you're aiming for" (moved here from the old "Preferences" step, matching the website — it was never really a preference). "Current Status" is gone entirely, not just relocated. **Backend addition to support this:** `UpdateProfileDto` gained an optional `universityId` (UUID) — `UsersService.updateProfile` only applies it for an ASPIRANT caller (silently ignored for MENTOR, whose `universityId` stays verification-linked via `VerificationService.review` and must never be touched by this self-service path); college resolution (`findOrCreate`) fires when leaving the Academics step, same pattern as the mentor wizard's own `_resolveCollegeThenSave`. Backend `tsc` + mobile `dart analyze`/`flutter test` clean (one pre-existing, unrelated `SectionHeader` viewport-overflow test failure confirmed present on the base commit too, before this change — not something this pass introduced or was asked to fix). **Live-verified 2026-09-07 against the real deployed backend + real accounts**, using two real phone numbers (OTP relayed by the user): a real MENTOR ("cosmic blade") confirmed `top-for-mentor` still 200s (no regression from the role widening) and confirmed `PATCH /users/me {universityId: ...}` is silently ignored — their college stayed "Academy of Medical Sciences," untouched, even after sending a different real university's id. A real ASPIRANT (`+919442849817`, the same account from the earlier promotion-fix investigation) confirmed the positive path end to end: `top-for-mentor` returned 200 (was 403 before this change, empty array since their stream — Engineering — has no reviewed colleges yet, which is correct), `PATCH /users/me {universityId: "<VELS Medical College>"}` returned it in the response and a **separate, fresh** `GET /users/me` confirmed it persisted (not just an echo), and `universityId: null` correctly cleared it back out. Both real accounts were reset to their original state (aspirant's `universityId` cleared back to null) after the test — no residual test data left behind. **Real gap found and fixed 2026-09-07 while reviewing this**: the new College field had nowhere to be seen afterward — the aspirant's read-only "Profile Details" screen (`edit_profile_screen.dart`) listed Display name/ID/Gender/Qualification/Field of interest/State/City but no College row, so a picked college was write-only from the aspirant's own point of view. Added a `('College', profile.universityName!)` row, shown only when set (i.e. never for a 12th-grader, who has no college yet) — `profile.universityName` already came through the exact `GET /users/me` JSON shape (`university.name`) confirmed live above, so this needed no new backend call, just reading a field that was already there. `dart analyze` clean.
  - **Session-status labels consolidated into one `sessionStatusView` (2026-09-08, per "remove all charged/paid texts … then implement it").** Seven hand-maintained functions across five files turned `SessionStatus` + `endReason` into text/colour and had drifted (`_statusDotColor` grey vs `_statusColor` blue for the same COMPLETED call; `_lastActivityLabel` flattened every `FAILED` to "No answer" while `_statusLabel` on the same session showed the real no-show label; the mentor dashboard rendered the raw `COMPLETED` wire string always-green). Replaced by **`lib/features/sessions/session_status.dart`** — `sessionStatusView(Session, {isMentor, style})` → `SessionStatusView(label, color)`, with `SessionStatusStyle { compact, standard, prompt }` (list rows / card chip / dock banner respectively) and a `sessionStatusViewOf({statusWire, endReason, isCall, isMentor, style})` variant for the dashboard's `MentorDashboardRecentSession` (not a full `Session`). One `switch` on status, one nested on `endReason` (`NO_ANSWER` / `ASPIRANT_NO_SHOW` / `MENTOR_NO_SHOW` / `ADMIN_CLOSED` / `SLOT_EXPIRED` all get real labels — previously fell through to a bare "Failed"), one `isMentor` branch. **No "charged"/"paid"/"credited" wording anywhere** — per explicit instruction, money movement is the wallet/ledger/notifications' job, not a status label's (aspirant no-show → aspirant sees "Missed" / "You didn't join in time", mentor sees "Aspirant no-show" / "Aspirant didn't join"; the mirror for mentor no-show). Deleted: `_statusDotColor`, `_lastActivityLabel`, `_SessionActionsState._statusColor` / `._statusLabel` (session_list_screen.dart), the `_statusText` switch (active_session_dock.dart, now a one-line delegate — `call_time_windows.dart` import dropped there), `_EndedView._reasonLabel` (call_screen.dart — `_EndedView` gained an `isMentor` param). Repointed: the four `session_list_screen.dart` call sites (one `statusView` local per row / per `_SessionActions` build), `mentor_landing_screen.dart:522` (fixes the raw-uppercase-green chip). `dart analyze` clean project-wide; the one pre-existing `flow_test.dart` "full journey" `SectionHeader` overflow failure is unchanged (present on base). Not click-tested (browser pane can't drive the Flutter canvas).
  - **In-app notification tap → blank screen + dead Sessions tab, fixed (2026-09-09, user report).** `notifications_screen.dart`'s row `onTap` did `context.push('/chats')` — but `/chats` (and `/chats/room`) are `StatefulShellBranch` routes and `NotificationsScreen` is a **top-level** route (`/notifications`, pushed from the bell), so `push`-ing a shell-branch location onto the root navigator rendered a blank white page and left the tab bar wedged (tapping Sessions did nothing). Exactly the gotcha the Help Centre bullet documents for `/help` → `/chats/support`, and `push_service._handleDeepLinkData` already sidesteps with `router.go('/chats')` ("/chats is a tab — switch to it rather than push"). Fix: the tap now uses `context.go` — a `MESSAGE` (chat) notification → `context.go('/chats/room', extra: {'sessionId': …})` (opens the actual thread; `sessionId` is already the guard condition), everything else with a `sessionId` → `context.go('/chats')` (Sessions tab, same as a real push tap). `dart analyze` clean; not device-tested (browser pane can't drive the Flutter canvas). Not touched: the several `context.push('/chats/room', …)` call sites in `home_screen.dart` / `mentor_landing_screen.dart` / `session_list_screen.dart` — those fire from *inside* the shell (Home/Sessions tab screens), where pushing a shell-nested route is the normal working pattern.
- **Web forms now provision a real account (contra the note below)** — `EnrollmentsService.createAspirantLead`/`createMentorLead` call `provisionAccountFromLead` → `UsersService.provisionAccountFromLead`, which creates a real `User` + `UserProfile` (+ empty `Wallet`, + generated avatar) keyed by the same phone hash OTP login uses, pre-filled from the submission; on later OTP login `findOrCreateByPhoneHash` finds it (`isNewUser:false`) and the app skips role-selection + onboarding. Best-effort/non-fatal (the lead is saved regardless). No password is set — auth is still phone-OTP only. **Mentor verification from the web (2026-09-06 fix):** a web mentor submission with a resolved `universityId` + doc-type + uploaded doc creates an auto `VerificationRequest` (SUBMITTED) so it lands in the admin `/verification` queue. This used to fire **only for a brand-new phone** (`isNewUser`) — a re-submission on a phone that already had an account silently dropped the doc + college, so nothing appeared in the verification page. Now it also fires for an **existing MENTOR account** whose `verificationStatus` isn't VERIFIED/SUSPENDED and has no SUBMITTED/UNDER_REVIEW request pending (`EnrollmentsService.provisionAccountFromLead`, live-verified all four cases: new / existing-DRAFT / existing-with-pending / existing-VERIFIED). Still skipped when no doc was attached (those mentors verify in-app). **Free-typed-college fix (2026-09-10, from a client question "new-user leads are high but verification isn't"):** the web mentor form lets a mentor *type* a college instead of picking it from the dropdown; that left `universityId` null, and since `VerificationRequest.universityId` is required, `provisionAccountFromLead` skipped creating the request — the college ID they uploaded was stranded on the `EnrollmentLead`, never reaching the admin queue. Now `EnrollmentsService.createMentorLead` resolves a free-typed name to a real `University` row via `UniversitiesService.findOrCreateByName` (created `isActive: false`, same as the in-app path) whenever there's a document to attach, so the doc + college always land in verification. `EnrollmentsModule` imports `UniversitiesModule` (no cycle; app boots clean). Live-verified against the real DB: free-typed college + doc → an inactive `University` is created, lead + `profile.universityId` link to it, and a SUBMITTED `VerificationRequest` with the doc key is created (`verificationStatus → SUBMITTED`); free-typed college + **no** doc → nothing extra created (unchanged). **Existing stranded docs:** `backend/scripts/backfill-web-mentor-verification.mjs` (dry-run by default, `--apply` to write) — finds converted mentor leads with a stored doc but no `VerificationRequest`, resolves the college, and creates the missing SUBMITTED request. **Dry-run 2026-09-10 vs prod: 104 mentor leads have a doc; 77 already have a request; 1 already verified; 25 are genuinely stranded** (22 need a new inactive `University` row, 3 already had a `lead.universityId`). **`--apply`-ed against prod 2026-09-10 (with the user's go-ahead):** 25 backfilled — 22 new `isActive:false` `University` rows + 25 SUBMITTED `VerificationRequest` rows + 25 `verificationStatus → SUBMITTED` + `profile.universityId` set where it was unset. Confirmed via an idempotent re-run: "would backfill 0 / already has a request 102" (was 77). The 22 junk-name inactive `University` rows (e.g. "dd", "Others", "Mgr university") are now in the admin catalogue as `isActive:false` — invisible to the public `GET /universities` / mobile Discover, visible in the admin universities list; an admin can activate/rename/merge them when reviewing each verification. **Known gaps:** the onboarding-skip is unconditional (`isNewUser` binary, no completeness check) so a minimal web submission — only `fullName` + `phone` are DTO-required — lands a live account that never gets prompted to finish; `uniqueId` isn't assigned until the first in-app `GET /users/me`; `displayName`/alias isn't uniqueness-checked on this path; `lastActiveAt` is stamped `now()` at provisioning. **Aspirant→Mentor promotion on website re-registration (2026-09-06 fix):** a real gap distinct from the one above — a phone number with an *existing Aspirant app account* (built before ever touching the website) that then registers as a **Mentor** on the website was silently ignored by `UsersService.provisionAccountFromLead`'s create-only `if (existing) return existing` guard: no role change, no profile update, no `VerificationRequest`, even with a resolved college + uploaded doc on the lead. Found live via phone `+919442849817` / user `9ab2ac5d-c03a-4329-beaf-5027b3664f6d`, who never appeared in the admin verification queue after registering. Fixed by extending the `existing` branch: when `existing.role === ASPIRANT` and the lead's role is `MENTOR` (an already-allowed transition per `ALLOWED_ROLE_TRANSITIONS`), promote the role and apply the submitted profile/name data in one `$transaction`, then fall through to the normal verification-request creation logic above. Every other existing-account case (existing MENTOR, existing ADMIN, etc.) is untouched.

Admin panel (`admin/`) now has real UI beyond login: `/dashboard` (live pending-verification/open-report counts), `/dashboard/verification` (approve/reject with signed document preview), `/dashboard/moderation` (resolve/dismiss reports, optional manual refund), `/dashboard/users` (search/filter by role + verification status, banned-only toggle, ban/unban, "View details" per row), `/dashboard/users/[id]` (full user detail — every onboarding field, university, wallet balance, verification history, activity counts, decrypted real name, inline verification-document viewer, ban/unban; backed by new ADMIN-guarded `GET /users/:id`; now also **editable** — `PATCH /users/:id` (ADMIN) accepts every field a user could set about themselves plus role (ASPIRANT/MENTOR only), a manual `verificationStatus` override with no approval side effects, and the two free-tier counters, applied without the self-service guards, refusing to touch an ADMIN account — plus a wallet ledger tab, `GET /wallet/admin/:userId/ledger` (cursor-paginated newest-first) with a manual `POST /wallet/admin/:userId/adjust` (writes an `ADJUSTMENT` ledger entry through the same `applyLedgerEntry` path every balance change uses, required reason becomes the audit note, refuses a debit that would push the balance below zero)), `/dashboard/universities` (search/filter by type, inline edit, create, activate/deactivate toggle — backed by new ADMIN-guarded `GET /universities/admin/list`, `POST /universities`, `PATCH /universities/:id` routes; deactivated universities are hidden from the public `GET /universities` listing and mobile Colleges tab but remain visible in the admin list, verified live against the real DB), and `/dashboard/payouts` (FIFO list with status filter tabs, mentor name + amount + earnings period + wallet balance per row flagged when short of the payout amount, Overdue badge past the 48h SLA, mark processing/paid/failed — "Mark paid" takes an optional bank reference and confirms before debiting the wallet; `GET /payouts` now includes mentor displayName + wallet balance so the reviewer isn't looking at bare UUIDs, and `processPayout` returns the failure reason instead of throwing so an insufficient-wallet-balance message actually reaches the admin). Every list screen (Users, Leads, Moderation, Universities) now has real "Load more" pagination via a shared `<InfiniteList>` client component that accumulates pages using the `nextCursor` every list endpoint already returned but the panel used to just drop — rows past the first page (50, or 100 for leads) were previously unreachable from the panel at all. It authenticates to the backend's ADMIN-guarded endpoints via a short-lived JWT minted server-side in `admin/lib/backend.ts` for one seeded ADMIN `User` row (id in `admin/.env.local` as `ADMIN_PANEL_USER_ID`) — this is separate from the admin panel's own hardcoded-credential session cookie. No delete endpoint for universities by design — deactivate is the only removal path. Users have deactivate/ban plus, since 2026-09-03, a deliberate GDPR-erasure exception (`POST /users/:id/erase` — anonymize-in-place, never a row delete; see "GDPR profile erasure" below).

**Admin UI design system** (2026-09-03): the panel was restyled to a clean neutral direction (Linear/Vercel-style — white surfaces on faint grey, hairline zinc borders, near-black primary). Shared primitives live in `admin/components/ui.tsx` (`Button`/`ButtonLink` with variants primary/secondary/danger/dangerSolid/successSolid/ghost, `Badge` + `toneFor(status)`, `Card`, `fieldClass` + `Input`/`Textarea`/`Select`/`Label`, `PageHeader`, `FilterTabs`, `EmptyState`) — use these, not ad-hoc Tailwind, when adding admin UI. `globals.css` is tokens-only (no dark mode). `DashboardShell` has a two-section sidebar (Operations / Directory). All row/panel components (`UserRow`, `LeadRow`, `ReviewRow`, `SessionRow`, `PayoutRow`, `UniversityRow`, `VerificationRow`, `EditUserPanel`, `WalletPanel`, `interactive.tsx`, `InfiniteList`) were swept onto the primitives; page-level filter-tab blocks still use equivalent inline classes. The Data Import page (`DataImportPanel`) also got a usability pass in the same push: inline "when/why to run this" guidance, a filter box + Select-all/none/high-confidence controls on the diff, a confirm step before Apply writes to the live catalogue, a Retry button on FAILED runs, and older job cards collapsed behind a "Show N older runs" toggle. All frontend-only — the `/admin/data-import` backend is unchanged.

**Polish pass (2026-09-03)** on top of the design-system pass, after the client said the panel still "looked basic / not professional": `Card` now carries a real soft shadow (not just a border) and `rounded-xl`; a `Table` primitive (`Table` + `Table.HeadCell`/`Row`/`Cell`, framed + shadowed + row hover + divider rows) was added to `components/ui.tsx`; `InfiniteList` gained a `variant="table"` mode (pass `tableHead`, `renderItem` returns a `<tr>`). `components/icons.tsx` is a small inlined line-icon set (Lucide path data, no dependency). Sidebar nav items now have icons + a "U" logo mark; the sticky header has a hairline shadow. The **Users list is now a real data table** (columns User / Role / Verification / Joined / Actions); its filter row uses the shared `FilterTabs`. Overview got icon tiles on the queue stat cards (amber when non-zero), trend chips (pill + up/down arrow) instead of ▲▼ text, and section-header icons. The other list screens were then also converted to tables (client asked for consistency): `components/ExpandableRow.tsx` is the shared pattern — a summary `<tr>` (whole row toggles on click, except clicks on `button/a/input/textarea/select/label`) plus a `<tr><td colSpan>` detail panel below. Sessions, Leads, Verification, Payouts, Moderation use it (Payouts/Moderation pass `defaultOpen` so actionable rows start expanded); Reviews is a flat table (no expansion). Every list page's filter row now uses the shared `FilterTabs` + the standard shaded search input.

**Session chat transcript for moderation** (2026-09-03): `GET /sessions/admin/:id/messages` (ADMIN, cursor-paged via `before`) returns a CHAT session's messages plus the two parties' ids + names, read-only — it never provisions a channel, so an audio call or a never-opened chat returns `messages: []` with `sessionType`. `SessionTranscript` (`admin/app/dashboard/sessions/`) renders it as a labelled bubble log with "Load older messages"; shown as a lazy button in the expanded Sessions row, and auto-opened inside a Moderation report whose `targetType === 'SESSION'` so an admin can read the conversation where they resolve the report. Not live-tested (local dev DB down) — verified via tsc + mock backend + UI click-through; route registration confirmed on deploy.

**GDPR profile erasure** (2026-09-03): `POST /users/:id/erase` (ADMIN) — `UsersService.eraseUser`. Irreversible, and deliberately NOT a row delete (sessions / wallet / ledger / reviews / payouts FK to the user and must survive). It scrambles `phoneHash` to `erased:<uuid>` so the login flow can never resurrect the account (contrast `deleteMe`, the reversible self-serve soft delete that re-verifying the phone reactivates), sets `displayName` to `deleted_user_<id8>`, nulls `refreshTokenHash`, sets `isActive:false`/`isBanned:true`/`deletedAt`, wipes every `UserProfile` PII field, and deletes push tokens, notifications, blocks and saved-mentor/university rows — all in one `$transaction`. Chat message bodies are left alone (scrubbing them would destroy the counterparty's history + the moderation transcript). Idempotent (`phoneHash.startsWith('erased:')` → no-op); refuses ADMIN accounts. Admin UI: a red "Danger zone — delete profile" card at the bottom of the user detail page (`DangerZone.tsx`), gated behind typing the exact display name, redirects to the user list on success; shows an "already been erased" state for a stub account (detected by the `deleted_user_` prefix). Not live-tested (local DB down) — tsc + mock backend + UI click-through (typed-confirm gate, anonymization, already-erased state).

**Sortable table columns** (2026-09-03): the admin list endpoints (`/users`, `/sessions/admin/all`, `/enrollments`, `/admin/reviews`, `/payouts`) take optional `sortBy` / `sortDir` query params. `backend/src/common/helpers/admin-sort.helper.ts` `adminOrderBy(sortBy, sortDir, allow, fallback)` maps a client column key to a Prisma field via a per-endpoint allowlist and always appends `{ id: 'asc' }` so cursor pagination (`cursor: { id }`) stays stable under any sort. Admin side: `components/SortableHeader.tsx` is a clickable `<th>` that writes `sort=<key>&dir=asc|desc` into the URL (click the active column to flip direction); each list page reads those params, forwards them to the backend, threads them through `FilterTabs` hrefs + the `loadMore` action, and includes them in the list `key` so the accumulated rows reset on a sort change. Sortable columns: Users (name/role/verification/joined), Sessions (status/requested), Leads (name/type/status/submitted), Reviews (rating/status), Payouts (amount/status/period). Not live-tested (local DB down) — tsc + mock backend + UI click-through (verified the URL toggles, the caret flips, and rows reorder).

**Bulk actions** (2026-09-11) on the Leads and Reviews list screens — select several rows with checkboxes and act on all of them at once (e.g. mark 10 leads "Contacted", hide 5 reviews), instead of one row at a time. Backend: `PATCH /enrollments/bulk` (`{ids, status}`, declared before the single-lead `:id` route so it isn't swallowed) and `PATCH /admin/reviews/bulk` (`{kind, ids, status}`, `kind` picks the mentor vs. university table) both use a plain `updateMany` and return `{updated: count}` — silently no-ops on an id that doesn't match rather than throwing, same as the rest of the app's bulk-by-id-list operations. Admin: `components/InfiniteList.tsx` gained optional `selectable` (`{getId}`) and `renderBulkBar` props — when set, `renderItem(item, selection)` gets a second arg (`{checked, onToggle}`) for the row to render its own checkbox cell, and a bulk-action bar renders above the list once ≥1 row is selected. **Important footgun avoided**: `items.map(renderItem)` would leak `Array.map`'s `(item, index, array)` call signature into `renderItem`'s second param when nothing was selectable — every call site now wraps in an arrow function (`items.map((item) => renderItem(item, ...))`); the same bug class bit the payouts `toPayoutRequestResponse` sort wiring earlier this session, worth grepping for `.map(someFnWithMoreThanOneParam)` if this pattern gets copied again. `LeadRow`/`ReviewRow` take an optional `selection` prop; `LeadsBulkBar.tsx`/`ReviewsBulkBar.tsx` are the actual bar UI (status buttons + "Select all N shown" + "Clear"), calling the new bulk server actions and then patching the local item list via `updateItems` instead of a full re-fetch. Each row's `key` now includes its own status (`` `${id}|${status}` ``) so a bulk-updated row remounts and its local `useState` (initialized from the `status` prop at mount) picks up the new value — without this, an already-mounted row's own status badge would silently keep showing the pre-bulk-update value. Not live-tested (local DB down) — tsc + eslint + `next build` clean; full UI click-through against the mock verified selection, the bulk bar, both endpoints actually persisting (re-fetched from the mock after acting), and that non-selectable `InfiniteList` consumers (Users, Sessions, Payouts, …) are unaffected.

**Bulk actions extended to Users/Verification/Sessions (2026-09-12).** Same pattern as the Leads/Reviews batch above, chosen after each single-item operation was checked for side effects first: **Users ban/unban** is a plain column flip (like Leads/Reviews status), so `UsersService.bulkSetBanned` is a raw `prisma.user.updateMany({where: {id: {in: ids}, role: {not: ADMIN}}}, {isBanned})` behind `PATCH /users/bulk-ban` — `UsersBulkBar.tsx` wired into `UsersList.tsx`'s existing `InfiniteList` exactly like Leads/Reviews (checkbox column, `Ban`/`Unban`, `${id}|${isBanned}` row key). **Verification review** and **Sessions force-end** are NOT raw bulk updates — their single-item methods (`VerificationService.review`, `SessionsService.forceEndAdmin`) each do a `$transaction`/notification-send/hold-release, so a bulk `updateMany` would silently skip all of that. Instead `bulkReview`/`bulkForceEndAdmin` **loop the existing single-item method** in a try/catch, collecting `{succeeded, failed: [{id, reason}]}` — `PATCH /verification/bulk-review` and `POST /sessions/admin/bulk-force-end` (route order checked against each controller's existing `:id` routes, same recurring risk called out at every prior bulk endpoint). **This is the rule going forward**: before adding a bulk version of any admin action, read the single-item service method first — a plain column flip gets `updateMany`, anything with a transaction/side effect gets a loop-with-try-catch so partial failure is reported per-id instead of silently dropping side effects. The Verification queue has no pagination/cursor at all (`GET /verification/queue` returns the whole array, no `InfiniteList`) — unlike every other bulk-actionable list — so it got its own bespoke `VerificationQueueList.tsx` client wrapper (holds `items`/`selectedIds` state locally, no `BulkBarContext` generic) instead of retrofitting `InfiniteList`'s pagination-coupled selection machinery onto a non-paginated list; `page.tsx`'s inline `<Table>`+`.map()` was replaced by this wrapper, `VerificationRow` gained an optional `selection` prop (colSpan 5→6 when present) the same way `SessionRow`/`UserRow` did. Sessions bulk force-end reuses `InfiniteList` normally (it already had cursor pagination) — `SessionRow` gained a `selection` prop, `SessionsBulkBar.tsx` uses a `ConfirmButton` (unlike Users/Verification's plain buttons) since force-ending a session is the more destructive, less-reversible action of the three. Live-verified against the mock backend (fixtures added to `scratchpad/mock-backend.js`, not committed — a per-session throwaway): Verification bulk-approve with a 2-succeed/1-fail mix correctly removed the two approved rows and left the failed one selected with a "2 approved, 1 failed" toast; Sessions bulk force-end via the `ConfirmButton`'s two-click flow correctly flipped an `IN_PROGRESS` row to `FAILED` in place. `tsc`/eslint/`next build` clean on `admin/`; backend `tsc --noEmit` clean (the two pre-existing unrelated `uniqueId` spec-fixture errors, confirmed via `git stash`, are untouched). Not live-tested against the real DB (local Postgres down all session, per the standing note elsewhere in this file).

**Admin Settings screen** (2026-09-11): a small, deliberately narrow set of operational knobs the admin can change without a code deploy — new `SettingsModule` (`backend/src/modules/settings/`). `settings.registry.ts` is the single source of truth (`{key, label, description, unit, default, min, max}`); a new `AppSetting` table (`app_settings`, migration `20260911150000_add_app_settings`, plain additive `key/value` rows, RLS enabled to match the rest of the schema) holds only overrides — a key with no row just uses the registry default, so the table can be completely empty and nothing breaks. `SettingsService.getNumber(key)` is what a consuming service calls in place of the old hardcoded constant; `getAll()`/`setMany()` back the admin endpoints (`GET`/`PATCH /admin/settings`, ADMIN-guarded — `setMany` validates every entry against its registry bounds before writing anything, all-or-nothing). Currently wired: `payoutCooldownDays` (was the hardcoded `PAYOUT_REQUEST_COOLDOWN_DAYS = 7` in `PayoutsService` — request cooldown + reminder cadence) and `payoutOverdueHours` (was `OVERDUE_AFTER_MS = 48h` in `payout-response.ts` — the admin-panel "Overdue" badge threshold, display-only). Deliberately did NOT make the flat mentor ₹10/min rate, the free-tier grant, or the call slot minutes configurable here — those are load-bearing for the Uniminute-conversion invariant / baked into `UserProfile`'s Prisma column defaults / tied to the no-show grace-period math, and are locked-in business decisions per this file, not a "safe to just expose" knob. Admin UI: `/dashboard/settings` (new "Admin" nav section, gear icon) — one card per setting with its description, default + allowed range, a "using default" badge, a number input, and a per-field Reset; changes are batched into one "Save N changes" that validates client-side before hitting the PATCH. Not live-tested (local DB down) — tsc + eslint + `next build` clean, full UI click-through against the mock (load, edit, save, out-of-range rejection, reset).

**Verification history** (2026-09-11): the verification screen used to only show the pending queue — no way to look up a past decision. `GET /verification/history` (ADMIN, cursor-paginated, `status`/`search` filters) — `VerificationService.findHistory` returns resolved (VERIFIED/REJECTED) requests newest-decision-first, with the reviewer's note and `reviewedAt`, but deliberately without the full applicant snapshot (`applicant` stays undefined — that heavy payload is only useful while a decision is pending; `VerificationRequestResponse` now also carries a light `userRole` field so history rows can still show Aspirant/Mentor without it). Admin: `/dashboard/verification` gained a **Pending queue / History** tab switch (`?view=history`), with its own decision filter (Any/VERIFIED/REJECTED) and a name search box; `HistoryList.tsx` + `VerificationHistoryRow.tsx` render it as a flat table (Applicant · College · Decision · Reviewed · Note) via the existing `InfiniteList` table variant. Not live-tested (local DB down) — tsc + eslint + `next build` clean, full UI click-through against the mock (tab switch, both status sub-filters, search).

**UI polish batch — toasts, skeletons, Universities table, page intros, empty states (2026-09-11, frontend-only, no backend change).** Five items, all client-requested after being asked "what would make the admin panel more professional / easier to use": (1) **Toast notifications** — `admin/lib/toast.ts` is a module-level pub/sub store (`toast.success/error/info`, 4s auto-dismiss), not React Context, because the panel's pages are independent server-rendered routes with their own client islands (`DashboardShell` is invoked fresh per page, not via a shared client layout) — a context provider would need re-mounting per page anyway, and a module-level store survives client-side navigation correctly (a toast fired just before a `router.push()`, e.g. `DangerZone`'s account-erasure flow, still shows on the destination page since `subscribeToasts` replays the current array to a newly-mounted listener). `ToastHost` (top-right, `role="status"`) is mounted once in `DashboardShell`. Every action that previously showed only small inline "Saved"/error text now also fires a toast: user ban/unban (both the Users-list row and the user-detail page), university save/toggle-active/photo-upload, settings save, broadcast send, lead status change (single + bulk), review status change (single + bulk), verification approve/reject, moderation resolve/dismiss, payout status change. (2) **Loading skeletons** — `TableSkeleton`/`Skeleton` added to `components/ui.tsx`; every list route (`users`, `sessions`, `leads`, `reviews`, `payouts`, `moderation`, `verification`, `universities`) got a `loading.tsx` (Next.js App Router convention) rendering `<DashboardShell title="..." email={null}><TableSkeleton .../></DashboardShell>` — `DashboardShell`'s `email` prop is optional specifically so these can render instantly with no async fetch. (3) **Universities converted from its old card-list style to a real data table**, matching every other list screen — `UniversityRow` rewritten onto `Table.Row`/`Table.Cell` for the view state and a bare `<tr><td colSpan={4}>` for the inline-edit state (edit mode replaces the whole row rather than expanding detail beneath it, so this uses the simpler two-shape-`<tr>` pattern rather than the shared `ExpandableRow`); `AddUniversityForm` was also swept onto the shared `Button`/`Card`/`fieldClass` primitives in the same pass (it had never been touched in the earlier 2026-09-03 design-system sweep). (4) **Page intros** — every list page (`users`, `sessions`, `leads`, `reviews`, `payouts`, `moderation`, `verification`, `universities`) gained a one-line description under its `PageHeader` explaining what the screen is for. (5) **Stronger empty states** — `EmptyState` in `components/ui.tsx` now takes an `icon` (from `components/icons.tsx`) and renders an icon badge + centered message instead of bare paragraph text; applied to Payouts (`icon="wallet"`) and Verification (`icon="shieldCheck"`) so far — other screens' empty states already routed through `InfiniteList`'s own empty-state handling. Live-verified against the mock backend: toast fires and displays correctly on a real ban action, Universities renders as a table with working inline edit, the enhanced empty state renders on Payouts with zero rows in a given status filter. Not tested against the real local Postgres DB (down all session, per the standing note elsewhere in this file) — `tsc`/`eslint`/`next build` all clean.

**Empty-state icons extended to every remaining list screen (2026-09-11, same-day follow-up).** `InfiniteList` gained an `emptyIcon?: IconName` prop threaded straight into its internal `EmptyState`; every `*List.tsx` wrapper that renders through `InfiniteList` now passes one — Users (`users`), Leads (`message`), Reviews (`star`), Sessions (`clock`), Moderation/Reports (`flag`), Universities (`building`), Verification history (`shieldCheck`) — so every list on the panel now shows the icon-badge treatment on zero rows, not just Payouts/Verification's top-level pending queue from the batch above. One mechanism, one prop, no per-page bespoke markup. Live-verified against the mock backend (Sessions filtered to a status with zero rows renders the clock-icon empty state correctly); `tsc`/`eslint`/`next build` all clean.

**Toasts now fire on error paths too, not just success (2026-09-11, same-day follow-up).** Every action that already called `toast.success` on its happy path (user ban/unban, university save/toggle-active/photo-upload, settings save, broadcast send, lead status change single+bulk, review status change single+bulk, verification approve/reject, moderation resolve/dismiss, payout status change, wallet balance adjustment, GDPR erase, profile edit) now also calls `toast.error(message)` on its matching failure branch (`catch` blocks and `!res.ok` branches from server actions) — mirroring the existing `setError(...)` call so the toast text matches the inline red text exactly, just also visible as a banner. Deliberately scoped to *real* action failures (server rejected the request, or the async call threw) — pre-submit client-side validation guards (e.g. "Name, state, and city are required", "Nothing changed.", "Refund amount must be a positive number") were left toast-free since they fire before any request goes out and the inline text sits right next to the field already; adding a toast there would be premature noise. Functions that never had a `toast.success` in the first place (e.g. the various "load more" / "view document" fetches) were left as inline-only, unchanged — the rule applied was strict success/failure symmetry, not "add a toast to every catch block in the codebase". Live-verified against the mock backend: triggered a real 409 (marking a payout paid when the mentor's wallet balance is below the payout amount) and confirmed the red error toast fires with the exact backend message, alongside the pre-existing inline red text; confirmed the success toast still fires correctly on a clean payout completion. `tsc`/`eslint`/`next build` all clean.

**Confirmation step added to Ban and Deactivate university (2026-09-11, same-day follow-up).** Both actions previously fired immediately on click — a misclick banned a user or dropped a college from every public listing with no way to catch it before it happened (each is reversible via Unban/Activate, but neither should be a single-click accident). New `admin/components/ConfirmButton.tsx` (client component, mirrors the existing `SortableHeader.tsx` pattern of living beside `ui.tsx` rather than inside it, since `ui.tsx` itself has no `'use client'` and most of its exports are plain server-safe functions) — a button that on first click swaps itself for an inline `Sure? [Confirm] [Cancel]` row in place, no modal, and only calls `onConfirm` on the second click; `ui.tsx`'s `ButtonVariant`/`ButtonSize` types were exported (previously file-local) so `ConfirmButton` could reuse them without duplicating the union. Wired into the three call sites that trigger a ban (`UserRow.tsx` on the Users list, `interactive.tsx`'s `BanToggle` on the user-detail page) and the one that deactivates a university (`UniversityRow.tsx`) — in every case only the destructive direction (Ban, Deactivate) is gated; the reversing action (Unban, Activate) stays a single click, since undoing isn't the thing that needs a speed bump. The existing GDPR-erase danger zone (`DangerZone.tsx`) already had a stricter typed-confirm gate (type the exact display name) and was left as-is — `ConfirmButton`'s lighter two-click pattern is for the Ban/Deactivate tier, not erasure. Live-verified against the mock backend: Ban → shows the inline confirm row → Cancel reverts cleanly with no request sent → Ban → Confirm fires the real request and the success toast; Deactivate on Universities shows the same confirm row → Confirm attempts the real PATCH (the mock backend doesn't have a route for its synthetic seed ids, so the request itself 404s — the confirm step is what mattered here, and as a bonus this also incidentally re-confirmed the error-toast batch above fires correctly on that failure too). `tsc`/`eslint`/`next build` all clean.

**Keyboard navigation added to the global search (2026-09-11, same-day follow-up).** `components/GlobalSearch.tsx` (the header quick-search, `/` to focus, ≥2 chars to query `GET /admin/search`) previously only supported mouse clicks on a result. Now: `ArrowDown`/`ArrowUp` move a `highlight` index through a flattened (users, then colleges — same order the panel renders in) result list, wrapping at both ends; `Enter` opens the highlighted row, or the first result if none has been arrow-key-highlighted yet; `Escape` closes the panel and blurs the input. Highlight state is reset to `-1` whenever a fresh result set lands (folded into the existing debounced-search effect's `setRes` call, rather than a separate effect watching the derived `flatItems` — a first attempt at that tripped the `react-hooks/set-state-in-effect` lint rule, since deriving one piece of state from another inside `useEffect` is exactly the "you might not need an effect" case). Hovering a result with the mouse also updates `highlight`, so keyboard and mouse stay in sync rather than fighting over which row looks selected. Standard combobox ARIA wired through: the input is `role="combobox"` with `aria-expanded`/`aria-controls`/`aria-activedescendant`, the panel is `role="listbox"`, each row is `role="option"` with `aria-selected` and a stable id (`user-<id>` / `uni-<id>`) that `aria-activedescendant` points at. Live-verified: the browser-automation tool's synthetic `key` presses didn't reach the focused input in this sandbox (a tooling quirk, not an app bug — confirmed by dispatching real `KeyboardEvent`s via `element.dispatchEvent` instead, which is how screen readers and most keyboard-testing tools actually exercise a page) — with real dispatched events, `ArrowDown` ×3 correctly landed the highlight on index 2 with `aria-activedescendant` matching the highlighted option's id, `Enter` navigated to that exact row's `href`, and `Escape` removed the listbox from the DOM. `tsc`/`eslint`/`next build` all clean.

**Dark mode (2026-09-11, same-day follow-up).** The panel was light-only (`globals.css` was explicitly documented as "tokens-only, no dark mode"). Now a real, class-based dark theme with a header toggle, covering every screen. **Mechanism:** Tailwind v4 has no `darkMode` config option (that's a v3 thing) — dark-variant support is declared in CSS: `@custom-variant dark (&:where(.dark, .dark *));` in `globals.css`, so `dark:` utilities apply under any ancestor carrying the `dark` class (in practice `<html>`). `ThemeToggle.tsx` (new) toggles that class and persists the choice to `localStorage` (`uniscope-admin-theme`); `layout.tsx` injects a small blocking inline script (`themeInitScript`, exported from `ThemeToggle.tsx`) into `<head>` that reads `localStorage` — falling back to `prefers-color-scheme` when nothing's stored — and applies the class *before first paint*, so there's no flash of the wrong theme. `globals.css` also gets a `.dark` block redefining `--app-bg`/`--ink` (the same near-black-on-near-white inversion pattern used everywhere else) plus a dimmer `.dark :focus-visible` ring. The toggle itself sits in `DashboardShell`'s header next to "Sign out" (sun/moon icons added to `icons.tsx`); it renders a blank placeholder until mounted (`useEffect` reads the class the inline script already set, `// eslint-disable-next-line react-hooks/set-state-in-effect` — a genuine one-time read of state set by an external script, not derived React state, which is exactly the case that lint rule's own guidance carves out) so SSR/hydration markup never depends on the visitor's actual stored theme. **Coverage:** every shared primitive in `components/ui.tsx` (Button variants, Badge tones, Card, Table/HeadCell/Row/Cell, form fields, PageHeader, FilterTabs, EmptyState, Skeleton/TableSkeleton) got a `dark:` companion for each colour utility, plus `DashboardShell`, the login page, and every other shared chrome component (`GlobalSearch`, `ToastHost`, `ConfirmButton`, `ExpandableRow`, `InfiniteList`, `SortableHeader`). Since nearly every page-specific component is built from these primitives, that covered most of the surface automatically — the remaining ~30 page-specific files (row components, forms, filter bars) each had a handful of raw Tailwind colour utilities not routed through a primitive (inline error/subtext colour, status highlights, filter-tab pills styled inline rather than via `FilterTabs`). Those were swept with a one-shot, idempotent Node script (`scratchpad/dark_mode_pass.mjs`, not committed) that appends the correct `dark:` companion immediately after each of ~70 known light-mode-only colour tokens, boundary-anchored (only matches a token delimited by quote/backtick/whitespace/string-edges, so e.g. `bg-zinc-50` never matches inside `bg-zinc-50/95`) and skips a token if its dark companion is already present right after it (safe to re-run). Three call sites (`SessionTranscript.tsx`, `SupportView.tsx`, `TechnicalReportsList.tsx`) paired `bg-zinc-900` with `text-white` for an "active tab" pill — the script's blanket `bg-zinc-900 → dark:bg-zinc-100` companion needed a matching `text-white → dark:text-zinc-900` added by hand in those three spots specifically (bare `text-white` elsewhere sits on solid saturated buttons/chips that read fine in both themes unchanged, so it wasn't given a blanket mapping). After the script, a final sweep confirmed zero files contain a colour utility with no `dark:` token anywhere in the file. Live-verified in the browser, both directions: toggled dark→light→dark on Overview (stat cards, chart, amber/red icon tiles), Users (table, badges, ban/unban buttons — including the toast firing correctly in dark mode with the right emerald tint), Settings (form inputs, "using default" badges), Payouts (expandable row, PENDING/Overdue badges, Mark-paid buttons), Universities (inline edit form, Deactivate confirm), and Payouts' empty state (icon badge). `tsc`/`eslint`/`next build` all clean.

**Sticky table header on long lists (2026-09-11, same-day follow-up).** Every list screen uses the shared `Table` primitive (`components/ui.tsx`); scrolling past the first several rows previously meant losing sight of the column labels entirely. The header row (`<thead>`) is now `sticky top-0` inside the table's own frame, which stays visible while the rows underneath it scroll — but getting there needed fixing a real CSS trap first, not just adding the sticky class. **First attempt (reverted the same session):** `sticky top-14` (matching `DashboardShell`'s own `h-14` sticky app header) on `<thead>`, keeping the table's existing `overflow-x-auto` wrapper unchanged — this looked plausible but silently didn't stick at all, confirmed live via `getBoundingClientRect()`: scrolling the page by 300px moved the `thead` by exactly 300px too, identical to non-sticky behavior. **Root cause:** per the CSS overflow spec, an element with `overflow-x: auto` and `overflow-y` left at its default `visible` doesn't actually keep `overflow-y: visible` — the spec forces the *used* value of `overflow-y` to `auto` too whenever the other axis is non-visible (you cannot have one axis scrollable and the other genuinely `visible` on the same box). That silently turns the `overflow-x-auto` wrapper div into `thead`'s sticky *containing block* — but since that div's height was never capped, it was sized to fit its content and its own `scrollTop` never moved from `0`, so `thead`'s "stick to the top of my scroll container" had nothing to react to and it just sat at its normal flow position, scrolling away with the rest of the page. **Fix:** capped the wrapper's height and made both axes scroll on the same box (`max-h-[70vh] overflow-auto` instead of the unbounded `overflow-x-auto`), and changed `thead` to `sticky top-0` (relative to that box, not the viewport) — the same "frozen header, bounded-height, internally-scrolling frame" pattern most spreadsheet/data-grid UIs use. A short list that fits within `70vh` looks and behaves exactly as before (no internal scrollbar appears until content actually overflows the cap); a long one now scrolls inside its own frame with the header pinned, rather than stretching the whole page. Live-verified against the mock backend, worth noting given this session's dev server intermittently shows a real, previously-documented streaming artifact (a stale duplicate `hidden` copy of the DOM subtree lingers briefly alongside the live one after a navigation, which `document.querySelector` can pick up before the live one settles — confirmed harmless by cross-checking against `getComputedStyle`/`getBoundingClientRect` and a fresh screenshot each time): with a stable element reference, scrolling the table's own container by 40px left the header's on-screen position pixel-for-pixel identical before and after (`342.76` both times), and a screenshot scrolled mid-list showed the previous row's text partially sliding *under* the still-pinned header row, with the container's own scrollbar visible on the table's right edge — exactly the intended behavior. `tsc`/`eslint`/`next build` all clean.

**University duplicate-merge tool (2026-09-11).** Every college-dedup fix to date (see the long "2026-09-09 — dedup + nan-state migration" / "dedup pass 2" / "district backfill" history above) was a one-off hand-written SQL migration run by a Claude session — there was no way for an admin to merge two duplicate colleges themselves. New `/dashboard/universities/duplicates` (linked via a "Find duplicates" button next to "+ Add University"): lists active-college groups sharing the same name+state (the exact heuristic the historical migrations used), oldest row first as the default "winner," a radio to pick a different one, and a `ConfirmButton`-gated "Merge N duplicates" action. Backend: `GET /universities/admin/duplicates` (`UniversitiesService.findDuplicateGroups`, a raw grouped query capped at 100 groups, largest first — "lightweight tool, not a full data-quality dashboard," matches the doc comment) and `POST /universities/admin/merge` (`{winnerId, loserIds}` → `UniversitiesService.mergeUniversities`) — both routes declared before `:slug` so the literal path isn't swallowed. The merge itself generalizes the exact pattern the shipped migrations proved out by hand: inside one `$transaction`, repoints every FK a University can carry (`Program`, `Review`, `SavedUniversity`, `UserProfile.universityId`, `VerificationRequest`, `EnrollmentLead`) from each loser onto the winner, backfills the winner's `city` from a loser's if the winner never had one, then deactivates the losers — never a row delete, consistent with the rest of this module (no delete endpoint exists for universities at all). Three of those FKs carry a uniqueness constraint a naive bulk repoint could violate (`Program` on `[universityId, name]`, `Review` on `[authorId, universityId]`, `SavedUniversity` on `[aspirantId, universityId]` — e.g. the same aspirant may have saved both the winner and a loser as separate rows for a true duplicate pair); for those three the service only moves a loser's row when the winner doesn't already have one for that key, leaving a colliding one on the now-deactivated loser — harmless (unreachable once inactive), same "duplicate left as a harmless leftover" outcome the hand-written migrations settled on. Returns a summary (`programsMoved`, `reviewsMoved`, `savedMoved`, `profilesMoved`, `verificationRequestsMoved`, `enrollmentLeadsMoved`) that the admin UI surfaces in the success toast, so a merge isn't a black box. `tsc`/`eslint`/`next build` clean on both `admin/` and `backend/` (2 pre-existing, unrelated `uniqueId` spec-fixture errors noted elsewhere in this file are untouched). **Not live-tested against the real Supabase DB** — no `backend/.env` exists in this session (the standing "local Postgres dev DB down all session" limitation) — verified instead via a thorough click-through against a purpose-built fixture in the session's mock backend (two synthetic duplicate groups, sizes 2 and 3): winner-selection radio correctly re-labels "Keep this one," the confirm step blocks an accidental single click, a successful merge removes exactly that group from the list and fires a toast with the real per-entity move counts, and the page correctly falls back to `EmptyState` once every group has been resolved. Worth a real-DB dry run (a genuine prod duplicate pair, verified read-only first) before leaning on this for a large cleanup pass.

**Activity counts on user detail are now clickable through to the actual filtered lists (2026-09-12).** "Sessions as aspirant: 34" and its five siblings (Sessions as mentor, Reports filed, Reports against, Mentor reviews received, College reviews written) used to be bare numbers — an admin could see *that* a user had 34 sessions but had no way to actually look at them without guessing at a name search on the Sessions page. Each is now a link (`ActivityLink` in the user-detail `page.tsx`, muted when the count is 0) straight to the real filtered list, and each destination list gained a proper server-side filter to make that precise rather than approximate: **Sessions** — `GET /sessions/admin/all?userId=` (new `ListSessionsAdminDto.userId`, `SessionsService.findAllAdmin`'s `where` now combines a `userId` OR-clause (`aspirantId` or `mentorId`) with the existing name-search OR-clause via a top-level `AND`, so the two filters compose correctly instead of one silently overwriting the other under the same `OR` object key — a real bug caught and fixed during this same change, not shipped). **Moderation** — `GET /reports?reporterId=` / `?targetUserId=` (new `ListReportsDto` fields; `targetUserId` maps to `targetType: USER, targetId` — the exact same condition `UsersService.findDetailAdmin`'s `reportsAgainst` counter already used, so the link and the count it's attached to can never disagree). **Reviews** — `GET /admin/reviews?authorId=` / `?subjectId=` (new `ListReviewsDto` fields; `authorId` filters either review kind by who wrote it, `subjectId` only makes sense for `type=mentor` — the mentor being reviewed). Each destination page (`sessions/page.tsx`, `moderation/page.tsx`, `reviews/page.tsx`) grew a small "Filtered to … `{userName}` · Clear" banner (the linking page passes `userName` as a plain display-only query param so the destination doesn't need a second fetch just to show a name) and threads the filter through its `FilterTabs`/pagination so switching status or paging further doesn't silently drop it. Moderation's status tabs gained a 5th "Any status" (`ALL`) option, defaulted to when arriving from a user-detail link (previously the page defaulted to `OPEN` unconditionally, which meant clicking "3 reports filed" on a user whose reports had all since been resolved would show an empty list even though the count was correct) — this also required changing `ReportRow`'s `readOnly` (whether the resolve/dismiss controls show) from a single page-level flag to a per-row computation off each row's own `status`, since an "Any status" list can now mix open and resolved reports in the same view. Live-verified end to end against the mock backend (with the mock's static fixtures adjusted to carry real matching ids where they'd previously used placeholder strings like `'x'`/`'y'`): all six links resolve to the exact right destination+query, the Sessions/Moderation/Reviews pages each render the correctly-filtered subset with the right banner and a working Clear link, and Moderation's mixed-status view correctly hides the resolve controls on already-resolved rows while keeping them on open ones. `tsc`/`eslint`/`next build` clean on both `admin/` and `backend/`.

**Global search** (2026-09-10): `GET /admin/search?q=` (ADMIN, new `AdminSearchModule`) — cross-entity quick lookup returning `{ users, universities }`, each capped small. Users match on `displayName` (contains, case-insensitive), the registry `uniqueId` (exact, upper-cased), or a raw UUID; ADMIN rows excluded. Universities match on `name` or `city`. Admin side: `components/GlobalSearch.tsx` is a debounced search box in the `DashboardShell` header (`/` focuses it from anywhere), with a grouped results dropdown — a user result navigates to `/dashboard/users/:id`, a college result to `/dashboard/universities?search=<name>`. Server action `globalSearch` in `admin/app/dashboard/search-actions.ts`. Not live-tested (local DB was down) — tsc + eslint + `next build` clean, full UI click-through against the mock (typeahead, grouped results, both navigation targets).

**Support inbox** (2026-09-06): `/dashboard/support` (nav label "Support", `lifebuoy` icon) — two tabs via `SupportView.tsx`. **Conversations** (`SupportInbox.tsx`): the staff side of the per-user support chat — a conversation list (name + role badge + last-message preview + `awaitingReply` amber dot + Refresh button) and a thread view (bubble log, staff bubbles right/green, "Load older messages", a reply box that sends on ⌘/Ctrl+Enter). **Technical reports** (`TechnicalReportsList.tsx`, tab badge = open count): the Help Centre's "Report a technical issue" queue — each card shows the message + reporter (name/role/uniqueId) + platform/version/date, an internal-note textarea, and Mark resolved / Reopen. Server actions in `admin/app/dashboard/support/actions.ts` (`'use server'`) wrap the ADMIN backend routes for both (`SUPPORT_ACCOUNT_ID` lives in a plain `constants.ts` sibling, not the `'use server'` file). `next build` + tsc + eslint clean; page verified rendering against the live backend. See "Support chat" under Business model decisions + the `technical-reports` backend module for the designs.

**Broadcast announcements** (2026-09-03): `/dashboard/broadcasts` (nav label "Announcements") — compose a title + optional message, pick an audience (All users / Students only / Mentors only), see a live "will notify N people" count, confirm, send. Backend `BroadcastsModule` (`admin/broadcasts` ADMIN-guarded): `POST /` fans out via `NotificationsService.sendBulk` (new — one `notification.createMany` for the in-app rows, type `SYSTEM`, then a best-effort FCM multicast batched at 500/call), then records a `Broadcast` row (new table, migration `20260903120000_add_broadcasts` — plain additive CREATE TABLE) as the audit trail; `GET /` lists the last 30; `GET /preview?audience=` returns the recipient count. Audience resolves to active + non-banned + non-deleted users of the matching role — ADMIN accounts are never recipients, including under "All". Mobile needs no change: a `SYSTEM` notification renders with the default bell icon and its tap has no deep-link (no `sessionId`). The write path was NOT live-tested (local dev DB `localhost:5432` was down and sending a real broadcast would notify ~1000 real users) — verified via tsc + the mock backend + full admin UI click-through; the migration and route registration get their real check on deploy (health stays 200, `/admin/broadcasts` flips 404→401).

**Ban enforcement**: `User.isBanned` existed in the schema for a long time but was never actually checked anywhere except mentor-listing eligibility — a banned user's existing JWT kept working fully (could still chat, book, etc.) until it happened to expire. Fixed in `JwtStrategy.validate()`: it now does one DB lookup per authenticated request and rejects immediately if `isBanned`/`!isActive`/`deletedAt` is set. This is a deliberate stateless→lookup tradeoff (small latency cost, indexed by id) specifically so a ban takes effect instantly rather than waiting out `JWT_ACCESS_TTL`.

**Call screen (`call_screen.dart`) improvement pass (2026-09-04, `feature/flutter-migration`; a real 2-device Android call is now confirmed working end-to-end — push, join, audio, end — and the `[call]`/`[session]` debug traces were removed once it was):** the screen was a bare 👤 emoji + "In call" + an elapsed counter. Now: the peer's real name + avatar (`AppAvatar` solid — data was always on `Session`, just unshown), slot **time remaining** ("4:48 left", amber under a minute, coral under 20s, "0:12 over" once the slot runs out — the auto-cutoff grace was previously invisible), "Ringing…" / "Connecting…" status with a ring on the avatar, labelled controls (Mute / Speaker / Earpiece), a call-end glyph + "with {name}" on the ended screen, and a depth pass on the background (dark-green gradient + radial glow behind the avatar). **Android foreground service** (`CallForegroundService.kt` + `uniscope/call` MethodChannel in `MainActivity.kt` + `FOREGROUND_SERVICE`/`FOREGROUND_SERVICE_MICROPHONE`/`POST_NOTIFICATIONS`/`WAKE_LOCK` in the manifest): started when the call goes `inProgress`, stopped on end/dispose — keeps Agora's audio + the timer alive while backgrounded, shows an ongoing notification with a live chronometer, tap-to-return, and an "End call" action that routes back through `MainActivity.onNewIntent` → the channel's `endCall` → `CallScreen._endCall()`. Also keeps the screen awake during a call (`keepScreenOn`) and requests `POST_NOTIFICATIONS` (Android 13+) after the mic grant. All hand-rolled (no plugin) for the same AGP-9/Kotlin-2.3 reason as the mic channel. `flutter analyze` clean. Core call flow device-confirmed on Android 2026-09-04; the foreground-service-survives-backgrounding and notification-action edge cases still lightly tested. SDK-agnostic: the Kotlin service carries over unchanged if calls move to Stream Video. P1 landed 2026-09-04: **weak-signal + "Reconnecting…" indicators** (Agora `onNetworkQuality` / `onConnectionStateChanged` / `onRejoinChannelSuccess`), **peer-muted** shown in the in-call status line (`onUserMuteAudio`), and **haptics** (medium impact on connect, heavy on end, selection click on the mute/speaker toggles). Also 2026-09-04: **connecting-state pulse** (expanding ring off the avatar in `_CallPeerHeader`, `SingleTickerProviderStateMixin`, skipped under `MediaQuery.disableAnimations`) and an **audio-route picker** — the speaker button now opens a Speaker / Earpiece / Bluetooth sheet (Bluetooth row appears once a BT route has been seen via `onAudioRoutingChanged`; picking it just turns the loudspeaker off and lets Agora route to the connected headset). The in-call route control's icon+label reflect the live route. Still pending: minimize-to-bubble, incoming-call full-screen notification, iOS CallKit. **Batch 2026-09-04:** (1) **speaking indicator** — `enableAudioVolumeIndication(250ms)` + `onAudioVolumeIndication`; `_peerSpeaking` (remote uid != 0, volume > 20) drives a green halo + 1.04 scale on the peer avatar in `_CallPeerHeader` (`speaking` flag threaded through `_CallStage`/`_ActiveCallView`, suppressed while muted/reconnecting). (2) **proximity sensor** — `MainActivity.setProximityScreenOff(bool)` acquires a `PROXIMITY_SCREEN_OFF_WAKE_LOCK` for the call's duration (released on stop + `onDestroy`); called from `_startCallService`/`_stopCallService` on the `uniscope/call` channel. (3) **tabular figures** on the elapsed-time / countdown text so digits don't jitter. **Visual redesign 2026-09-04:** every live phase (connecting / ringing / active) now renders through one shared `_CallStage` (status pill up top → peer block in the upper third → control row pinned to the bottom via `_kCallBottomPad`), so the avatar no longer jumps between phases and `connecting`/`requestingPermission` show the peer block instead of a bare spinner (`_ConnectingView`). Controls unified into one `_CallControl` (58px, translucent + hairline border; `active` → solid white; `danger`+`big` → the 66px red end button with a glow, kept in the same row so all three share a baseline). One `_StatusPill` above the avatar by priority: Reconnecting → Weak signal → countdown (amber/coral `tone`). `_GlyphBadge` is the shared round icon mark for the message / ended states; ended screen wraps Duration/Used in a translucent card and only shows it when there was a real charge. Name bumped to `xxl`. `_RoundIconButton`/`_EndCallButton`/`_CallChip` deleted. `flutter analyze` clean; seen on a real Android call. **Two fixes 2026-09-04 from a real 2-device test (Railway logs, session `b7d022d9`):** (1) the aspirant never joined — mentor accepted, waited, and the client's 45s no-answer timeout ended the call `NO_ANSWER`; then a second token request on the now-`COMPLETED` session 409'd into a raw "Could not connect" screen. `CallScreen._start()` now checks for a terminal status right after `findById` and shows the ended summary instead of letting `getCallToken` 409. (2) the no-answer timeout was bumped 45s→90s (the backend no-show sweep at half the slot is the real backstop). **The underlying problem — the aspirant not being pulled into the call when the mentor accepts.** Root cause was: `mobile_flutter/android/app/google-services.json` was a placeholder, so `FirebaseMessaging.getToken()` couldn't register and every `[notify] push` logged `SKIPPED (no registered devices)`. **Resolved 2026-09-04** — the real `google-services.json` (`uniscope-83eeb`) is now in place and push delivery + deep-linking is device-confirmed (see "What's NOT built yet"). The push payload already carries `notification: {title, body}` so Android auto-displays it; `_handleDeepLink` now routes a tapped `SESSION_ACCEPTED`/AUDIO_CALL push into `/call/:id` (aspirant) and a tapped `SESSION_REQUEST` push into `/chats` (mentor — the Sessions tab where the Accept button + dock live). No ringtone/full-screen-incoming-call work planned — a plain system notification is the agreed UX. **`CallRequestWatcher`** (`call_request_watcher.dart`, mounted in `MainShell` for both roles) — while the user has an outstanding AUDIO_CALL it polls `sessionsListProvider` every 4s, and on the aspirant side navigates straight to `/call/:id` the instant the session flips to `ACCEPTED`. Started as the interim stand-in for push; now that FCM push is confirmed working it's kept as a redundant foreground fallback (covers the case where a foregrounded app's push is delayed). **Batch 2026-09-07 (per screenshots):** (a) **elapsed ↔ time-left swapped** — the running elapsed time now sits in the top `_StatusPill` (same place/size the countdown used to); the slot time-left drops to the small line under the `@id` (`_CallStage`/`_CallPeerHeader` gained `statusColor`/`statusFontSize` overrides), keeping its amber/coral urgency tint. (b) **"Time's up" prompt fires on time** — it keyed off `session.billedMinutes` (0 until billing settles) so it could land ~15s after the real slot end; `_checkSlotCutoff` now uses the same `billedMinutes>0 ? billedMinutes : callSlotMinutes` fallback the countdown display uses, and runs every 1s tick, not only the 2s poll. (c) the dialog now appends "You have N Uniminutes available." (`walletBalanceProvider.asData?.value.availableUniminutes`). (d) **the extend prompt now fires at 1 minute left, not at slot end, with a warning beep** (per client screenshot): `_checkSlotCutoff` shows the dialog + fires `_beepWarning()` when `remaining <= 60s` (once per slot, latched by `_oneMinWarned`, re-armed after an extension or when remaining climbs back past 75s); if the app was backgrounded through that window the negative-remaining branch still surfaces the prompt. The beep is a native `ToneGenerator` (`TONE_PROP_BEEP`, 200ms, `STREAM_VOICE_CALL`) via a new `beep` method on the `uniscope/call` MethodChannel (`MainActivity.playWarningBeep()`) — no audio plugin, same hand-rolled pattern as the rest of this screen's native bits — plus a `HapticFeedback.heavyImpact()` fallback. The hard auto-cutoff (20s grace past true slot end → `_endCall('SLOT_EXPIRED')`) is unchanged. Dialog title "Time's up" → "1 minute left", body "Your slot has ended" → "Your slot is almost up". `dart analyze` clean; Kotlin not compile-checked here (no Android build in this sandbox). **Batch 2026-09-07b (per the "Uniscope Call Both Sides" mockup — two of its four adds):** (1) **in-call context card** — a translucent `_ContextCard` pill under the peer's `@id`: the aspirant sees the mentor as `College · ★4.8 · Specialty`, the mentor sees the student as `Aspirant · Stream · Qualification · Target: Course`. Backed by six new `SessionResponse` fields — `mentorCollege` / `mentorSpecialty` (falls back specialty→specialization→stream) / `mentorRating` / `mentorReviewCount` / `aspirantStream` / `aspirantQualification` / `aspirantCourse` — `SESSION_WITH_NAMES_INCLUDE` now pulls the extra profile columns + `mentor.university.name`, and `toResponseById` computes the mentor rating via `ReviewsService.ratingSummary` (only the single-session path — the list path doesn't, it's not shown there; `ReviewsModule` added to `SessionsModule.imports`). Mobile `Session` model mirrors the fields; `call_screen.dart`'s `_peerContext` getter builds the line role-aware (`authControllerProvider.user.role`), shown only when ≥2 facts are available. (2) **persistent signal chip** — `_SignalChip` (3 bars + `STRONG`/`FAIR`/`WEAK`) sits top-left of `_CallStage`, always visible once Agora reports quality (hidden only on `unknown`). `onNetworkQuality` now derives a `_SignalLevel` (worst of tx/rx) instead of a bare `_weakSignal` bool — `_weakSignal` is kept in sync so the prominent `_StatusPill` "Weak signal" warning still fires. Both threaded through `_ActiveCallView` → `_CallStage`. The **slot-progress ring** landed 2026-09-07c: a thin arc just outside the peer avatar fills as the booked slot elapses — green for the aspirant, white for the mentor (`_SlotRing`/`_SlotRingPainter`, `_slotProgress` = elapsed/slotSeconds recomputed each 1s tick, threaded `_ActiveCallView` → `_CallStage` → `_CallPeerHeader`, active phase only). NOT built from that mockup: the "Save a note" control. Live-verified the new session fields on `GET /sessions/:id`; `tsc` + `dart analyze` clean. **Minimize-to-bubble (2026-09-07c) — the pending item is now built.** The call is no longer a `go_router` route; it's an **overlay** mounted above the whole app. `mobile_flutter/lib/features/calls/call_overlay.dart`: `CallOverlayController` (singleton `ChangeNotifier` — `sessionId` + `expanded`), `CallOverlayHost` (wraps `MaterialApp.router`'s `builder` child in a `Stack`; renders the one `CallScreen` inside a `Navigator` pinned by a `GlobalKey` so its `State` — engine, timers — is **never re-created** on minimize/expand; `Offstage` hides it when minimized, `TickerMode(enabled: expanded)` pauses only the pulse animations, the plain `Timer`s keep running), and `_MiniCallBar` (avatar · name · slot-remaining · mute · end, tap-to-expand). `CallScreen` gained `inOverlay` (default false) + a `_leave()` that `close()`s the overlay vs `context.pop()`, `_toggleMute()`/`_syncPresence()` helpers, a top-left minimize chevron in `_CallStage` (`onMinimize`, only in overlay mode), and `_CallPresence` — a one-way mirror the bar reads. Back / swipe on a live call **minimizes** (never ends); End (from the bar or the screen) auto-expands so the summary shows. All ~5 launch sites (`home_screen`, `session_list_screen` ×3, `active_session_dock` ×2, `call_request_watcher`, `push_service`) now call `CallOverlayController.instance.open(id)` instead of `context.push('/call/:id')`; the `/call/:sessionId` route is kept as a `redirect` that hands off to the overlay and bounces to `/home` (deep links / FCM taps). Billing/dual-confirm/slot-cutoff logic in `CallScreen` is **unchanged** — it just stays mounted now. `flutter analyze` clean; **not device-tested this session** — the minimize/expand state-preservation and the mini-bar's engine callbacks need a real 2-device run before shipping. **Back-button fix (2026-09-08, per real-device report "minimize/back doesn't work, can't use the app during a call").** Root cause: the `CallScreen` lives in a **detached** `Navigator` (that's what keeps its engine alive across minimize), so its own `PopScope` never receives the Android system-back press — the press fell through to go_router and navigated the app *behind* the still-covering call screen, so back looked dead and the only working minimize path was the tiny chevron. Fixes: (1) `CallPresence` gained an `onBack` hook — `CallScreen` (overlay mode) points it at `_handleBack` in `initState`, clears in `dispose`/`clear()`; (2) `CallOverlayHost` wraps the **expanded** call in a `BackButtonListener` that calls `CallPresence.onBack` (→ minimize a live call, close a terminal one) and returns `true` to **consume** the press so the app router doesn't also move; when minimized the listener isn't mounted so normal app back works. (3) The on-screen minimize chevron's tap target went 30→~44px (`HitTestBehavior.opaque` + transparent padding; the header row `SizedBox` height 34→44 to fit). (4) `_MiniCallBar` now floats **64px** off the bottom instead of `10px`, so it clears the app's bottom-nav bar and the tabs stay tappable while a call is minimized. `dart analyze` clean; still needs a real-device confirm (no emulator in the sandbox). **Slot-progress ring removed (2026-09-11, explicit request).** The thin arc around the avatar that filled in as the booked slot elapsed (`_SlotRing`/`_SlotRingPainter`, added 2026-09-07c above) is deleted entirely, along with the `slotProgress`/`progressTint` params threaded through `_ActiveCallView` → `_CallStage` → `_CallPeerHeader`, and the now-unused `_slotProgress` getter on `_CallScreenState`. The pulsing expanding-ring animation on `_CallPeerHeader` (shown only during "Connecting…"/"Ringing…", unrelated — see the "connecting-state pulse" note above) is untouched and still there. Slot time-remaining is still shown as text under the `@id` (unaffected — that's `_CallStage`'s `status`/`statusColor` line, not the ring). `flutter analyze` clean project-wide (one pre-existing, unrelated `test/flow_test.dart` failure from the 2026-09-10 UPI-ID feature, out of scope). Not device-tested (browser pane can't drive the Flutter canvas). **Countdown moved off the peer's face (2026-09-12, client feedback — "make the time left very smaller", then "can we move it somewhere else? it will create panic or anxiety for the students").** Shrinking `statusFontSize` first (12→10) wasn't the real ask — the client's actual objection was the countdown's *position*: it sat directly under the peer's name/avatar/context card, right where a student's eyes land for the whole call. `_CallStage` gained a `bottomLabel` slot (rendered just above the control row, between the `Spacer()` and Mute/End/Speaker) — the active-call countdown ("4:48 left" / amber under a minute / coral under 20s or overrun, unchanged text/colour logic) moved there instead. The line under the `@id` is now used only for the "{name} is muted" note, and — since that's usually empty — `_CallPeerHeaderState` skips rendering it (and its spacing) entirely when `status` is blank, so a normal unmuted call shows nothing at all under the context card. Removed the now-dead `statusColor` param from `_CallStage`/`_CallPeerHeader` (nothing set it once the countdown moved) rather than leave it as unused plumbing. `dart analyze` clean (0 errors project-wide). Not device-tested.

**iOS first-connect-attempt timeout, auto-retry added (2026-09-12, device report + screenshot: "Could not connect" after ~15s of "Connecting…", but tapping Go Back → reopening the same call connects instantly every time).** That manual-retry-always-works pattern is the signature of a cold-start cost (first-ever ICE/TURN negotiation, ATS/cert warm-up) exceeding the existing 20s join timeout on the first attempt and being cheap on a second — so instead of making the user retry by hand, `_start()` now does exactly that automatically. New `_joinAgoraChannelWithRetry` wraps `_joinAgoraChannel` in up to 2 attempts before surfacing the "Could not connect" error. A real subtlety this required: `Future.timeout()` does **not** cancel the underlying operation — an abandoned join keeps running in the background — so `_joinAgoraChannel` now assigns `_engine = engine` immediately on creation (was only at the very end, after a successful join) so a timed-out attempt's engine can always be found and force-released (`leaveChannel()` + `release()`) before the retry gets a clean one, rather than leaking a second native engine racing the first. `dart analyze` clean project-wide. Not yet device-confirmed this fixes the underlying report — only that the retry-and-cleanup mechanism is structurally sound; still worth a real on-device re-test.

Mentors now have a dedicated profile detail screen (`mentor_detail_screen.dart`, route `/mentors/:id`) — stats (rating, free-chat/call-rate), bio, expertise tags (specialty + languages), and a reviews list, all from the already-existing `GET /mentors/:id` and `GET /reviews/mentor/:mentorId` endpoints (no backend work needed). On the mentor list, tapping the avatar/name opens this detail screen; tapping the rest of the card still starts a free chat unchanged — two distinct `GestureDetector` tap zones on the same `MentorCard`. Reviews are still only ever written from the post-session flow in `SessionListScreen`, never from this screen.

**Mentor's own view of their reviews (2026-09-03):** `mentor_reviews_screen.dart` (`MentorReviewsScreen`, route `/profile/reviews`) — the full list of reviews aspirants left for the signed-in mentor, with an average-rating + count summary header, reusing the same anonymous `_ReviewTile` pattern as `mentor_detail_screen`. Backed by `myMentorReviewsProvider` (`reviewsApi.listForMentor(myUserId)`) — the single source now shared with the Home tab's Reviews preview (the old local `mentorOwnReviewsProvider` in `mentor_landing_screen.dart` was removed in favour of it). Entry points, both MENTOR-gated: a "Your Reviews" row on the Profile tab menu (next to "Verification"/"Rate Your College"), and the Home tab's Reviews section is now a `SectionHeader` with a "See all" into the same screen. No backend change — `GET /reviews/mentor/:mentorId` already existed and is used by the aspirant-facing `mentor_detail_screen`. Still no mentor reply path to a review (none planned).

**Post-signup onboarding wizards**, matching the Figma flow, replacing the old display-name-only `ProfileSetupScreen` → `/home` path: after role selection + display name, an ASPIRANT is routed to `AspirantOnboardingScreen` (3 steps: DOB/gender/state → qualification/status/stream/course → language/goals/timing) and a MENTOR to `MentorOnboardingScreen` (4 steps: DOB/gender → qualification/status → area of guidance → languages/available days, then the existing `POST /verification` call folded in as a final identity-verification step, ending in a "Verification Submitted!" screen reusing the same college-dropdown + doc-type + photo-upload UI as the standalone `VerificationScreen`). Both wizards share `onboarding_widgets.dart` (progress bar, step scaffold, dropdown, date field, chip group) and `profile_options.dart` (picklists, also used by `EditProfileScreen`) so the two flows can't drift apart. New `UserProfile` fields added for this: `dateOfBirth`, `courseInterested`, `preferredLanguage`, `preferredMentorshipTiming`, `availableDays`. "Skip for now" is preserved at every step, going straight to `/home`. Deliberately has no per-mentor "Call Rate" field even though Figma's design shows one — see the locked-in flat ₹10/min pricing decision above. The aspirant wizard was verified live end-to-end (fresh signup, all 8 profile fields round-tripped through a real fetch); the mentor wizard's backend round-trip was verified live via curl with the exact payload the wizard sends, but its own UI could not be click-tested end-to-end in this session due to browser-pane click flakiness that was also reproduced on pre-existing, previously-verified screens (Role Selection, the Welcome screen's Get Started button) — worth a real click-through next time the mobile web preview is touched.

## Chat architecture (migrated off Stream Chat, 2026-09-02 — cost-driven)

Two Prisma models: `ChatChannel` (`SESSION` type, one per CHAT-type `Session`; or `SUPPORT` type, one per user, persistent) and `ChatMessage` (`text`, `senderId`, `channelId`, `clientMessageId` for send-retry idempotency, indexed `(channelId, createdAt)` for pagination). Postgres is the single source of truth for message content — nothing about a message ever leaves through any other channel.

**Realtime delivery is deliberately content-free.** `ChatService.sendMessage` persists the message, then does a best-effort Supabase Realtime **Broadcast** `httpSend('new_message', {})` on topic `chat:<channelId>` — an empty-payload ping, never the text. Mobile subscribes to that topic with the public Supabase anon key and, on any ping (or on first successfully subscribing, or on app-resume), refetches history over the authenticated REST API. This sidesteps needing Postgres RLS / Supabase Realtime Authorization, which would otherwise require handing Supabase a session it can verify — this app has no native Supabase Auth session (custom phone-OTP+JWT), so there's nothing for RLS to key off. **Verified live against the real Supabase project** that this is actually safe: `chat_channels`/`chat_messages` have RLS disabled (like every table in this app), but the `anon`/`authenticated` Postgres roles have zero GRANTs on them either — so even though the anon key ships to every mobile client, Supabase's public REST API (PostgREST) cannot be used to read chat data directly, only the NestJS-authorized path can. This is correct today but fragile: any future Supabase Studio action that grants `anon`/`authenticated` SELECT on a public table (e.g. its table editor's "Enable API access" convenience) would silently expose this data with no RLS to stop it. Re-check grants (`information_schema.role_table_grants`) after any hands-on Supabase Studio session.

**Authorization** (`ChatController.requireChannel`): a session's chat is readable/writable only by its two parties (aspirant/mentor), only while the session is `ACCEPTED`/`IN_PROGRESS`/`COMPLETED` (so history stays readable after a session ends, matching the rest of the app's "never erase access to something already shared" pattern), 404-not-403 for a non-party. **Blocking gates sending, not reading**: if either party has blocked the other, `sendMessage` 403s in both directions, but `listMessages` still works for both — an existing conversation's history doesn't disappear just because someone got blocked later.

**Idempotent sends**: `SendMessageDto.clientMessageId` (a client-generated UUID, unique per `(channelId, clientMessageId)`) — a retried/double-tapped POST with the same id returns the original message instead of creating a duplicate, and skips re-notifying/re-pinging. Mobile's `ChatThreadView` mints one id per logical send attempt and reuses the *same* id on a manual retry (via the "Retry" SnackBar action) — generating a fresh id on retry would defeat the whole point.

**Push + in-app notifications**: `ChatService.sendMessage` notifies whoever didn't just send (both session parties minus the sender, for SESSION channels; the real user, for a SUPPORT-channel reply from the sentinel — moot today since nothing replies as the sentinel yet) via the existing `NotificationsService` (`NotificationType.MESSAGE`, `metadata.sessionId` for deep-linking — the mobile notification-tap handler already reads `notification.sessionId`, not a chat-specific key). Best-effort, never blocks the send.

**Pagination**: `GET .../chat/messages?before=<messageId>` — cursor-based, 50 messages per page, `hasMore` in the response. Mobile's `ChatThreadView` loads the next page when the user scrolls within 200px of the top, preserving scroll position across the prepend.

**Reconnection**: the Realtime broadcast subscription is fire-and-forget by nature (a dropped connection just means missed pings), so `ChatThreadView` compensates two ways — (1) a fresh subscribe always triggers an immediate refetch (catches anything that landed in the gap before the subscription took effect, and doubles as reconnect-catchup since a `channelError`/`timedOut`/`closed` status tears down and re-subscribes a fresh channel after a 3s backoff — the underlying package throws if `subscribe()` is called twice on the same channel instance, so a genuinely new instance is required each time); (2) independently, `didChangeAppLifecycleState(resumed)` also forces a refetch, since resuming from background is exactly when a channel is most likely to have silently dropped without its error callback ever firing.

**Known gaps, not built**: typing indicators, read receipts/unread counts, presence/online status (all deliberately out of scope for this migration's v1 — the CLAUDE.md mentor-availability decision above is a real precedent for *not* building presence without a clear product reason), and an offline send queue (a failed send surfaces a retry action, not automatic background retry). None of these block normal use; all were true of the pre-migration Stream implementation too except read receipts/typing (Stream provided those as SDK defaults but the pre-migration screens never enabled them — `disableAttachments:true`/`showCommandsButton:false` was the extent of the customization — so this is feature parity with what was actually shipped, not a regression from a richer surface).

## What's NOT built yet

- Real push notification delivery — **DONE and device-confirmed 2026-09-04.** The real `google-services.json` (`uniscope-83eeb`, app `com.uniscope.uniscope_mobile`) is in `mobile_flutter/android/app/` (gitignored — every build machine needs its own copy; paste-in came from the user). A real device registered an FCM token, uploaded it, and received a push for a session event with the deep-link routing working. Railway's production `FIREBASE_SERVICE_ACCOUNT_KEY_BASE64`/`FIREBASE_PROJECT_ID` are therefore also correct (the push wouldn't have sent otherwise). The `[push]` `debugPrint` diagnostics in `push_service.dart` were removed once this was confirmed. **Verified live 2026-09-08 via Railway logs** that prod push is healthy (`[notify] push result … success=1`); `FIREBASE_DISABLED=false` on Railway.
  - **Wrong-recipient push bug fixed (2026-09-09, per report "student sends a request, the student — not the mentor — gets the notification; mentor accepts, the mentor gets the 'accepted' one").** Not a targeting bug — `SessionsService.create`/`accept` send to the correct `userId`. Root cause: `PushService.initializeAndRegister()` was fully guarded by `_initialized`, so the FCM token was uploaded **once per app process**. `PushToken.token` is `@unique` and `registerPushToken` upserts `where {token}` reassigning `userId`, so there's one row per device pointing at *whoever last uploaded*. Switching accounts **without restarting the app** (exactly how it's tested) left the row bound to the previous user → every push for that user hit this device (now showing someone else), and pushes for the current user went nowhere. Fix — role-agnostic, one systemic change: (1) `initializeAndRegister()` split — `_setupOnce()` keeps the permission/listener wiring behind `_initialized`, but `getToken()` + `_upload()` now run on **every** call, i.e. on every auth→authenticated transition (`main.dart`'s existing `ref.listenManual` already fires then), so the token re-binds to the current user on each login/switch. (2) New `unregister({accessToken})` + `DELETE /users/me/push-token` (`RemovePushTokenDto`, `NotificationsService.removePushToken` → `deleteMany where {userId, token}`, scoped to the caller so a stale call can't wipe a since-reassigned row) — `main.dart` calls it on the authenticated→logged-out transition using the *previous* state's still-valid `accessToken` (captured before clear; `UsersApi.deletePushToken` sets an explicit `Authorization` header since the Dio interceptor only *adds* one, never overwrites). Backend live-tested 5/5 against the real DB: register→row=student; re-upload as mentor→row **rebinds** to mentor; DELETE→row gone; DELETE by the wrong user→no-op; empty body→400. `tsc` + `dart analyze` clean. Not device-tested (browser pane can't drive the Flutter canvas) — needs a real two-account swap to confirm end to end.
  - **Foreground notifications now render (2026-09-08, per user report "only shows the in-app notification, not the real one").** The backend payload always carried `notification: {title, body}`, so Android draws the system-tray notification itself **only when the app is backgrounded/terminated**. In the foreground FCM hands the message straight to `onMessage` in Dart, and the app had no code to display anything — so a foregrounded user only saw the in-app list update. Added **`flutter_local_notifications` ^19.5.0**: `push_service.dart` creates one Android channel (`uniscope_default`, `importance: high`) at startup and, on every `onMessage`, calls `_showLocalNotification(message)` to post a heads-up notification built from `message.notification` (skipped for data-only messages); tapping it decodes `message.data` (JSON in the notification payload) and runs the same deep-link routing as a tapped FCM push (`_handleDeepLink` refactored to a shared `_handleDeepLinkData(Map)`). The existing `onMessage → _handleDeepLink` call is kept, so a foreground `SESSION_ACCEPTED` still auto-opens the call overlay *and* shows the notification. Android build needed **core library desugaring** for the plugin: `isCoreLibraryDesugaringEnabled = true` + `coreLibraryDesugaring("com.android.tools:desugar_jdk_libs:2.1.4")` in `android/app/build.gradle.kts`. `flutter analyze` clean; **not device-tested** (no Android build/emulator in the sandbox — Kotlin/Gradle not compile-checked here, needs a real `flutter run` to confirm the channel + desugaring build). Background/terminated behaviour is unchanged (still the OS-drawn tray notification). **Correction (2026-09-12): iOS does have real APNs/Firebase config** — `ios/Runner/GoogleService-Info.plist` (real values, `BUNDLE_ID: com.uniscope.uniscopeMobile`, `PROJECT_ID: uniscope-83eeb` — same Firebase project as Android) and `aps-environment` in both `Runner.entitlements` (development) and `Runner-Release.entitlements` (production) have been in place since 2026-07-31, predating this note — the "Android only" claim above was never actually verified against the `ios/` directory. Still true: no push has ever been confirmed actually received on a real iOS device (the whole platform is otherwise unexercised — see "What's NOT built yet" below). Other reasons a device may still show nothing when backgrounded: notification permission denied, or OEM battery-optimization/autostart killing background FCM (Xiaomi/Oppo/Vivo/Realme/Samsung) — both device-settings issues, not app bugs.
    - **Every tapped notification now deep-links somewhere (2026-09-10, client report "tapping a notification just opens the app, not the notification/related screen").** `_handleDeepLinkData` opened with `if (sessionId == null) return;` and its type chain ended in `else { return; }`, so any push without a `sessionId` (**`VERIFICATION`, `PAYMENT`, `LOW_BALANCE`, `SYSTEM`**) and any unhandled type (`MESSAGE`, `SESSION_REJECTED`, `SESSION_ENDED`) was a no-op on tap — the OS just foregrounded the app wherever it was. Rewrite: (1) split the handler with a `fromTap` flag — a foreground **receipt** (`onMessage`) still auto-routes *only* for a live/imminent call (`SESSION_STARTING`, or `SESSION_ACCEPTED`+`AUDIO_CALL` within ~2 min), everything else waits for an actual tap so a received "you're verified" can't yank you off your screen; (2) on tap: `MESSAGE`+`sessionId` → `/chats/room` (the thread), scheduled `SESSION_ACCEPTED` tap → `/chats/room`, `SESSION_REQUEST` → `/chats`, call types → `/call/:id`, **everything else → `/notifications`** (the in-app list) as the catch-all so a tap never dead-ends. New `_navigate(target, {extra})` helper: `push` for the root-navigator routes (`/call/:id`, `/notifications`), `go` for tab locations (`/chats`, `/chats/room`) — same shell-vs-top-level rule the in-app notification-tap fix uses. `dart analyze` clean; mobile-only, no backend change. Not device-tested (no Android build in the sandbox) — needs a real `flutter run` + a tapped tray notification to confirm.
    - **Call notifications now ring, not just appear (2026-09-09, per user report "when the call is coming it shows a notification but it should come with sound").** Root cause: the FCM send (`notifications.service.ts` `sendEachForMulticast`) passed only `notification: {title, body}` + `data` — no `android` config, so no channel id, no explicit sound, normal delivery priority → on many devices an incoming-call push landed silently; and the single mobile channel `uniscope_default` (`Importance.high`, no explicit sound) can't be given a sound retroactively (Android freezes a channel's sound/importance at creation). **Backend:** new `deliveryConfig(type)` helper spreads `{ android: { priority:'high', notification:{ channelId, sound:'default', priority, defaultVibrateTimings:true } }, apns:{ headers:{'apns-priority':'10'}, payload:{aps:{sound:'default'}} } }` into both `sendEachForMulticast` calls; `channelId`/`priority` are `uniscope_calls`/`max` for `CALL_NOTIFICATION_TYPES` (`SESSION_REQUEST`, `SESSION_ACCEPTED`, `SESSION_STARTING`), else `uniscope_default`/`high`. **Mobile (`push_service.dart`):** a second channel `uniscope_calls` (`Importance.max`, `playSound`, `enableVibration`, `AudioAttributesUsage.notificationRingtone`) created at startup alongside the default; `_showLocalNotification` picks the channel via `_isCallType(message.data['type'])` and, for a call, sets `importance:max` + `category: AndroidNotificationCategory.call` + ringtone-stream audio + iOS `interruptionLevel: timeSensitive`. `VIBRATE` added to the Android manifest. `tsc` + `flutter analyze` clean; **not device-tested** (no Android build here — needs a real `flutter run` + a live call to confirm it rings). Not built: a looping ringtone / full-screen CallStyle incoming-call UI (needs `USE_FULL_SCREEN_INTENT` + Android-12 CallStyle notification — a much bigger change; flagged as a follow-up).
    - **iOS never received any push at all — root-caused 2026-09-12, per user report "notifications are not receiving [on iOS], not in android."** `PushService.initializeAndRegister()` called `FirebaseMessaging.instance.getToken()` immediately after `requestPermission()`, wrapped in a catch-all that silently swallowed any failure. On iOS, `getToken()` requires an APNs token to already be attached to the FCM instance — iOS delivers that token to the app **asynchronously** (via `didRegisterForRemoteNotificationsWithDeviceToken`) some short time after requesting permission, not synchronously. Calling `getToken()` before it arrives throws on iOS (Android has no such handshake and was never affected), and the silent catch meant the whole method could run to completion having uploaded nothing, with zero visible error — so an iOS device could sit fully authenticated and never once have a `PushToken` row. Fixed: `initializeAndRegister()` now polls `FirebaseMessaging.instance.getAPNSToken()` on iOS (up to 10× / 500ms, ~5s total) before calling `getToken()`; if it genuinely never arrives (permission denied, or a real APNs/entitlement problem) the method just returns rather than letting `getToken()` throw. `dart analyze` clean. **Not yet device-confirmed** — needs a real iOS device to prove push actually arrives now; if it still doesn't, the APNs token itself never arriving would point at a real provisioning-profile/entitlement gap (the `aps-environment` entitlements exist per the correction above, but an actual push has never been received to confirm they're wired correctly end to end).
- **Pre-chat flow** — `showPreChatConfirmSheet` (`pre_chat_confirm_sheet.dart`, wired from `MentorDetailScreen`) shows the "Ready for [Mentor]?" confirmation with four generic sample questions. Tapping a question pops `_PreChatResult(draft: q)` → `startChatWithMentor(..., draft: q)` → route `extra['draft']` → `SessionChatScreen.initialDraft` → `ChatThreadView` pre-fills the composer once on mount. "Start Chat" passes no draft. Still not wired into the mentor-card / home tap paths (those start a chat immediately).
- **Richer mentor stats** — Figma's mentor detail view shows students-helped count and average response time alongside rating; `MentorDetailScreen` only shows rating + reviewCount today (the counters don't exist in the backend).
- **Low-balance interstitial** — `showLowBalanceSheet` (`low_balance_sheet.dart`) gates the *call-request* action when balance can't cover the shortest slot (6 Uniminutes). Chat itself is always free, so there is no mid-chat "recharge to keep chatting" gate and won't be one. Figma's mid-chat framing doesn't match the billing model.
- **Nav structure** — aspirant bottom nav is now Home / Discover / Mentors / Sessions / **Wallet** / Profile (6 tabs; `_TabButton` padding tightened to fit). Wallet moved from a pushed route (`if (!isMentor) GoRoute('/wallet')`) to its own `StatefulShellBranch` in `_buildAspirantBranches` — the standalone route was removed to avoid a path collision. The Mentors-tab AppBar wallet icon was dropped (redundant); Profile-menu / low-balance-sheet links to `/wallet` switched `push`→`go` so they select the tab. (Figma's own bar omits Mentors — kept here since browsing mentors is the core aspirant loop.)
  - **Mentor bottom nav (2026-09-06): Home / Discover / Sessions / Wallet / Profile** — the 4th tab was "Dashboard" (`MentorDashboardScreen`: a wallet-card + payout-requests + recent-activity overview) and is now just the Wallet screen itself. The mentor branch's `GoRoute` still lives at path `/dashboard` (kept so it can't collide with the aspirant-only top-level `/wallet`) but builds `WalletScreen`; the nested `/dashboard/wallet` route is gone. `WalletScreen` already renders balance + Withdraw + payout requests + ledger, so nothing was lost — and the earnings/session stats that were also on the old Dashboard already live on the mentor Home tab (`MentorLandingScreen`). `mentor_home_screen.dart` was gutted to just `mentorDashboardStatsProvider` (still consumed by `MentorLandingScreen`); `MentorDashboardScreen` + `_WalletCard`/`_RecentActivityCard`/`_PayoutRequestCard` + `mentorRecentActivityProvider` deleted as dead code. `dart analyze` clean.
- **Stream glyphs on college cards (2026-09-06)**: the per-stream icon + colour + tint ("duotone thumbnail") lives in `mobile_flutter/lib/features/universities/stream_visuals.dart` (`StreamVisual`, `kStreamVisuals`, `streamVisualFor(stream)`, `streamGlyph(v, {size})`) — **extracted from `university_list_screen.dart`** so it's shared. Material Icons has no tooth/stethoscope glyph and there's no icon-pack dependency, so **Dental → `assets/icons/tooth.svg`** and **Medical → `assets/icons/stethoscope.svg`** (single-path SVGs via `flutter_svg`, tinted to each stream's colour with `ColorFilter … srcIn`); other streams keep their Material `IconData` fallback. Used by the Discover/Colleges tab's 44px card thumbnail **and** the aspirant Home "Top colleges for you" rail's 60px card hero (`_CollegeSpotlightCard` in `home_screen.dart`). `dart analyze` clean.
  - **2026-09-08, per request ("replace the current header colour with … same as the [Home header] in the app now"): the two Home college-card headers now use the green `AppGradients.canopy` gradient with a white glyph, instead of the pale per-stream tint.** `_CollegeSpotlightCard`'s 60px hero → `BoxDecoration(gradient: AppGradients.canopy)` + `streamGlyph(visual, size: 28, color: Colors.white)`; `_CollegeCard`'s ("Keep Exploring" list) 44px thumb → same gradient + a white `account_balance` icon (was `authBrandTeal @ 12%` + teal icon). `streamGlyph` gained an optional `color` param (`color ?? v.color`) for the white-on-green case — non-breaking, the Discover tab's `streamGlyph(visual)` call is unchanged and still tints per-stream. **Scope: aspirant Home cards only** — the Discover/Colleges tab keeps its per-stream duotone thumbnail, and the mentor Home `_TopCollegeCard` + the university detail `_Hero` were not touched. `dart analyze` clean.
    - **Superseded 2026-09-08 (same day, follow-up "make the complete screen with the same colour palette for each universities"):** the green `AppGradients.canopy` header on `_CollegeSpotlightCard` / `_CollegeCard` was too saturated against the newly-paled Home canopy (see "Home canopy greeting + seam polish" → 2026-09-08 rework). Both now use a flat `AppColors.primaryLight` (the header's pale mint) behind the glyph in `AppColors.primary` (brand green) — one uniform palette across every college card, no per-stream hue. `streamGlyph`'s `color` param stays (now passed `AppColors.primary` instead of `Colors.white`). Same aspirant-Home-only scope.
- Note: Figma's mentor wizard has a per-mentor "Call Rate" field, which conflicts with the locked-in flat ₹10/min-for-every-mentor decision above — don't build that field without re-confirming the pricing model with the user first.
- Real Agora server-side "user joined channel" webhook — still the interim dual-client-confirm approach
- Android Agora native-call crash — **root-caused and fixed at the build level, not yet confirmed live on a device.** `io.agora.rtc:iris-rtc` and `io.agora.rtc:agora-special-full` both ship an `AndroidManifest.xml` with `package="io.agora.rtc"`, which AGP 8.3+/9.x hard-fails the build on. The previous workaround (`exclude(group="io.agora.rtc", module="iris-rtc")` in `android/app/build.gradle.kts`) let the app build but dropped iris-rtc's JNI wrapper (`libAgoraRtcWrapper.so`) entirely, crashing "Join Call" with `UnsatisfiedLinkError`. Real fix: both AARs are pure native-lib containers (empty `classes.jar`, no manifest components in either) — the `package` attribute is inert at runtime, so it's safe to vendor a copy of iris-rtc with its manifest package rewritten to `io.agora.rtc.iris` (see `mobile_flutter/android/app/libs/README.md` for how it was produced/regenerated) and exclude only the unpatched upstream module. `flutter build apk --debug` now succeeds and `unzip -l` on the built APK confirms both `libAgoraRtcWrapper.so` and `libagora-rtc-sdk.so` are packaged for all four ABIs (previously only the latter was present). Still needs a live run on a real Android device or a working local emulator to confirm the Agora engine actually connects a call end-to-end — this dev sandbox can't boot a hardware-accelerated Android emulator (qemu process starts but never leaves `offline` in `adb devices`), so this remains the one thing only verifiable on the user's own hardware. Untested on iOS/web for actual **calls** — Agora doesn't work on Flutter web at all (native-only plugin), and Agora specifically has never been exercised on iOS (still needs a live run). **Correction (2026-09-12) to two claims that used to sit here:** the iOS build has now been exercised for the first time — `flutter build ios --no-codesign` and a `--simulator --debug` build both succeed, launched and screenshotted cleanly on iPhone 17 Pro Simulator (Welcome screen, teal palette intact) — see the iOS-build/TestFlight bullet below for the full signing story. And the mic-permission channel does have a Swift side: `ios/Runner/AppDelegate.swift`'s `didInitializeImplicitFlutterEngine` registers a `uniscope/permissions` `FlutterMethodChannel` (`requestMicrophone`/`openAppSettings`, same contract as Android's `MainActivity`) — this predates 2026-09-12 too, the "no Swift equivalent exists yet" claim was stale. What's still genuinely unverified on iOS: an actual Agora call connecting, the camera-picker fix, and push actually arriving on a device.
  - **iOS call audio investigated 2026-09-12, per real-device report "calls are not hearable [on iOS], not in android" — root cause not yet confirmed, but a key diagnostic gap fixed.** The user confirmed: **neither direction has audio**, and **the system mic-permission dialog never appeared at all** (`AVAudioSession.sharedInstance().recordPermission` never went through `.undetermined`, or the request never actually ran). That combination points away from a mic-permission denial (which would still let the *incoming* audio play) and toward the call never really reaching the native audio-capture stage on iOS, despite the call *screen* itself reaching an apparently-connected state (timer running, peer shown) — the Dart-level Agora join can succeed at the signaling layer independently of whether local audio capture/playback actually works. **Fixed as real, if secondary, gaps regardless of the exact root cause:** (1) `ios/Runner/Info.plist`'s `UIBackgroundModes` only had `remote-notification` — added `audio` (the iOS counterpart to Android's `CallForegroundService.kt`, needed so a backgrounded call's audio session isn't suspended by iOS) — **deliberately not** `voip`, which requires real PushKit/CallKit integration (neither exists here) and Apple review rejects a `voip` declaration with no VoIP-push usage behind it. (2) Added the missing `NSBluetoothAlwaysUsageDescription` (used by the existing Bluetooth-route detection in `call_screen.dart`'s `onAudioRoutingChanged`, which had no permission string backing it on iOS). **The higher-value fix: Agora's `onError` handler was `debugPrint`-only** — invisible on any real device outside an attached Xcode debugger, meaning a genuine Agora-level failure (bad token, a native SDK/linking problem, a codec/transport issue) would produce exactly what was reported: a call that *looks* fully connected with total silence and zero visible signal. `call_screen.dart` now also surfaces each distinct Agora error code once via a red `SnackBar` (`_lastSurfacedAgoraError` de-dupes so repeated warnings don't spam) — the next real-device test will show *something* diagnostic instead of nothing. `dart analyze` clean; `plutil -lint` confirms the Info.plist is still valid. **Not fixed, because not yet diagnosable without a real device + this SnackBar's output**: whether the true cause is a stuck-`.denied` mic-permission state left over from an earlier build on the same device (the most likely candidate — worth a full delete-and-reinstall of the app to test, since a new build won't re-prompt for a bundle id iOS already has an answer for), an Agora native iOS SDK/pod integration gap, or something else. Next step once this build is on a device: reproduce the call and read what the new SnackBar says.
  - **The iOS build was first exercised 2026-09-12 (compiles clean, `--no-codesign` + `--simulator --debug` both succeed) — export to a real TestFlight-ready `.ipa` failed on that first attempt (`No Accounts` / `No signing certificate "iOS Distribution" found`, only a Development cert present, no Apple ID signed into Xcode) but has since succeeded and is now confirmed working end to end.** `PRODUCT_BUNDLE_IDENTIFIER` is `com.uniscope.uniscopeMobile`, `DEVELOPMENT_TEAM` is `H5G7S347ZN` (`CODE_SIGN_STYLE = Automatic`). **A later `flutter build ipa --release` the same day succeeded fully** once Xcode had an authenticated Apple ID — Xcode's automatic signing fetched a real **"Cloud Managed Apple Distribution"** certificate on its own (no local Distribution identity needed in the keychain; `security find-identity -v -p codesigning` still shows only Development certs even on a successful export, which was initially confusing — the cloud-managed cert doesn't appear there), producing a genuine signed `app-store-connect`-method `.ipa` (`DistributionSummary.plist` confirms the cert type + a `2027-07-29` expiry). **Rebuilt again 2026-09-12 with the build-identifier defines** (`--dart-define=GIT_SHA=$(git rev-parse --short HEAD) --dart-define=BUILD_TIME=...`) carrying that day's push/call-audio fixes (commit `7b98f6b`) — `build/ios/ipa/uniscope_mobile.ipa`, 41,452,548 bytes, version `1.0.0` build `7`. **Still not yet actually uploaded to App Store Connect/TestFlight** — the `.ipa` exceeds `SendUserFile`'s 30MB cap (same wall the release APK has always hit), so it only exists locally on this Mac at that path; getting it to App Store Connect needs either the Transporter app or `xcrun altool --upload-app` run locally with the user's own API key (never entered by the assistant). The Xcode Apple-ID/signing setup, once done, appears to persist across sessions on this machine — a future session hitting the "No Accounts" error again should first retry rather than assume it's still unresolved.
    - **That same rebuild shipped with OTP completely broken — a real, self-inflicted mistake, caught only because the user actually tried logging in on-device.** Neither `flutter build ipa` invocation above passed `--dart-define=API_URL=...`, so `kApiBaseUrl` (`dio_client.dart`) fell back to its dev default, `http://localhost:3001/api/v1` — a real iPhone then tried to reach a server running on *itself*, so every network call failed silently, OTP included, with no useful error surfaced anywhere in the app. **Fixed and reconfirmed live 2026-09-12**: rebuilt with `--dart-define=API_URL=https://uniscope-production.up.railway.app/api/v1` added; verified two ways — `strings` on the compiled `App.framework/App` binary shows the production URL baked in and zero occurrences of `localhost:3001`, and a live `curl` against the exact same endpoint path the app calls (`POST /auth/otp/request`) returned 200. **New `mobile_flutter/scripts/build_ios_release.sh`** wraps `flutter build ipa --release` with `API_URL` (defaults to the production Railway URL, overridable via env), `GIT_SHA`, and `BUILD_TIME` always supplied, plus a post-build `strings` check that fails loudly if `localhost:3001` is still present in the binary — **use this script, not a bare `flutter build ipa`, for any real-device/TestFlight/App-Store build going forward**, so this exact mistake can't recur silently. (`scripts/build_web.sh` already followed this same "wrap the raw build command so a required define can't be forgotten" pattern for web; this is the iOS equivalent.)
  - **Camera capture for verification-document upload, 2026-09-11 fix, still not click-tested on either platform.** See the "Verification-document camera never opened during registration" bullet in the client-priority section above for the full fix — both upload spots now offer a real "Take a photo" option (previously gallery-only), Android got the missing `CAMERA` manifest permission, iOS already had `NSCameraUsageDescription` populated so needed no change. Neither path has been exercised on a device or simulator (a camera doesn't exist in a headless browser or in the iOS Simulator either — `image_picker`'s camera source reports unavailable there regardless of whether the underlying wiring is correct) — worth a real-device check next time this screen is touched.

## Foreground Service Permissions — undeclared `FOREGROUND_SERVICE_MEDIA_PROJECTION` (2026-09-12)

Play Console's "Foreground service permissions" review step flagged two undeclared permissions: `FOREGROUND_SERVICE_MEDIA_PROJECTION` and `FOREGROUND_SERVICE_MICROPHONE`. Only the microphone one is real — `CallForegroundService.kt` genuinely uses it to keep the Agora audio connection alive while a call is backgrounded, answer that question with "Background audio input". **`FOREGROUND_SERVICE_MEDIA_PROJECTION` was never used by any app code** — traced to `io.agora.rtc:full-screen-sharing-special` (a transitive dependency of the "full" Agora SDK variant this app vendors for its native audio-call libraries, see `android/app/libs/README.md`), which bundles a screen-sharing module and its manifest permission whether or not the app ever calls into it. Uniscope has no screen-share feature anywhere. Fixed by stripping the permission at the manifest-merger level rather than declaring a fabricated "why" for it in the Play Console form (which risks a future policy flag if the underlying code is actually reviewed): `android/app/src/main/AndroidManifest.xml` gained `xmlns:tools` + `<uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PROJECTION" tools:node="remove"/>`. Rebuilt via `build_android_release.sh` and verified live in the merged manifest (`build/app/intermediates/merged_manifests/release/processReleaseManifest/AndroidManifest.xml`) — only `FOREGROUND_SERVICE`/`FOREGROUND_SERVICE_MICROPHONE` remain, media-projection is gone. New `.aab`: 186.8MB, production `API_URL` verified baked in (no `localhost:3001`). No feature impact — nothing in the app used or exposed screen sharing before this change; if that's ever built as a real feature later, the permission (and its own Play Console declaration) would need to be added back deliberately.

## Build identifier in Settings (2026-09-12)

A client report ("the review-gate fix still doesn't work") turned into a multi-hour investigation that ultimately traced to an unprovable question: was the phone being tested actually running the build with the fix? The 267MB release APK has never fit through `SendUserFile`'s 30MB cap, so there was no reliable way to confirm a given device had the latest code. Fixed the ambiguity at the source: `mobile_flutter/lib/core/build_info.dart` exposes `kGitSha`/`kBuildTime`, both compile-time `String.fromEnvironment` constants (default `'dev'`/`'unknown'` for a plain `flutter run`). Settings screen (`settings_screen.dart`) now shows a small muted "Build \<sha\>" / timestamp footer below the Danger Zone. **To make this real, every release build must pass the defines**: `flutter build apk --release --dart-define=GIT_SHA=$(git rev-parse --short HEAD) --dart-define=BUILD_TIME="$(date -u +'%Y-%m-%d %H:%M')"` (same flags for `flutter build ipa`/`build ios`). A build without these defines shows "Build dev" — itself a useful signal that it wasn't built via the standard release command. `dart analyze` clean.

## App Store review demo accounts (2026-09-12)

Two accounts exist specifically for Apple/Google reviewers to log in and exercise a mentor and an aspirant flow: **`+919999999999`** (MENTOR — verified, college-review gate cleared, "Accepting calls" on) and **`+918888888888`** (ASPIRANT — 500 Uniminutes wallet balance). **These were repurposed from two real pre-existing accounts** ("test" / mentor id `2797c547-60ed-4f03-8cab-d83f2a154565`, unbanned + verified; "Steady Raven #2798" / aspirant id `f65fc1a7-2b48-427e-9228-9b75ffb8d489`, demoted MENTOR→ASPIRANT) — not fresh rows, so their `displayName`/`uniqueId` still carry that history; ask before renaming if that matters for the reviewer-facing polish. The mentor's college-review gate was bypassed via `mustReviewCollege: false` rather than posting a fake review onto a real college's public board.

**Neither number is real/deliverable**, so logging into them for real needed a fix: `backend/src/auth/demo-accounts.ts` (`DEMO_ACCOUNT_PHONES`, `DEMO_ACCOUNT_OTP_CODE = '424242'`) + `AuthService.requestOtp`/`verifyOtp` — for exactly these two phone numbers, OTP is bypassed entirely (no real provider call, no real SMS) and the fixed code `424242` is accepted instead; every other phone number is completely untouched and still goes through the real `msg91` provider. This is deliberately **not** the same as flipping the whole backend into `mock` mode (which would accept a fixed code for *any* phone number — a real security loosening for live users); the allowlist keeps production login for real users exactly as strict as before. Live-verified against the real deployed backend + DB: demo mentor request→verify with `424242` mints a real token resolving to the correct mentor account; wrong code → 401; demo aspirant same; a real phone number (`+919442849817`) still gets a genuine MSG91 `serviceId`, proving the bypass doesn't leak. `tsc` clean.

**Known limitation:** `isMentorAvailable`'s 24h auto-expiry (see the mentor-availability business rule above) is untouched for this account — "Accepting calls" was turned on 2026-09-12 and will read as off again ~24h later unless re-flipped or the app's own gate (already cleared) is re-tapped. If the review window runs longer than that, re-run the toggle or ask for a permanent exemption to be added (not done here, since it would mean carving an exception into a locked business rule without being asked).

## App Store screenshot resizing (2026-09-12)

Real device screenshots taken on an iPhone 15/16 Pro Max come out at **1290×2796** (that device's native resolution) — not one of the exact pixel sizes App Store Connect's screenshot uploader accepts. Apple's accepted sizes (as used for this app): **1242×2688** / **2688×1242** (6.5" class — iPhone 11 Pro Max / XS Max) and **1284×2778** / **2778×1284** (6.7" class — iPhone 12–14 Pro Max). A raw 1290×2796 screenshot uploads nowhere until it's converted to one of these exactly.

**Method used (macOS `sips`, no third-party tool needed) — scale-to-fill then center-crop, never a plain stretch:** a straight non-uniform resize to an exact target would visibly distort the UI, since the source's aspect ratio (1290:2796 ≈ 0.4614) is close to but not identical to the targets' (~0.4620–0.4622). Two-step process per file:
```bash
sips --resampleWidth <target_width> input.png --out /tmp/step.png   # proportional resize, matches the target width, height comes out slightly over
sips -c <target_height> <target_width> /tmp/step.png --out output.png  # center-crop down to the exact target
```
This trims only a few pixels (single digits to ~15px) evenly off the top/bottom — imperceptible, no stretching. Verify the result with `sips -g pixelWidth -g pixelHeight output.png`. Batched this way for 8 screenshots total this session (all converted to 1242×2688), each confirmed byte-exact against the target dimensions before being sent to the user via `SendUserFile` (output files can't be handed over directly from a pasted chat image — the source has to actually exist on disk first, e.g. saved to `~/Downloads`, before any resizing can happen).

## Local dev gotchas specific to this session

- **Flutter web caching**: `flutter build web` doesn't content-hash `main.dart.js`, so Chrome's HTTP disk cache can silently serve a stale copy after a rebuild even in a fresh tab with no service worker (confirmed via `transferSize: 0` in `performance.getEntriesByType('resource')`). Fixed two ways: (1) `mobile_flutter/scripts/no_cache_server.py` replaces plain `python -m http.server` in `.claude/launch.json`, sending `Cache-Control: no-store` and falling back unmatched paths to `index.html` (needed for go_router's path-based routing to survive a direct/deep-link load); (2) `mobile_flutter/scripts/build_web.sh` wraps `flutter build web` and appends a content-hash query string (`?v=<hash>`) to `mainJsPath` in the generated `flutter_bootstrap.js`, so every rebuild gets a guaranteed-fresh cache key. Use `build_web.sh` instead of calling `flutter build web` directly when testing web changes.
- **Backend hot-reload doesn't see new Prisma Client types**: after `npx prisma generate`, the already-running `nest start --watch` process keeps using its stale in-memory `PrismaService` types (`Property 'x' does not exist on type 'PrismaService'` for a model that very much exists) even though a fresh `tsc --noEmit` in a new shell sees it fine. Fix: stop and restart the "Backend (NestJS)" preview server (`preview_stop` + `preview_start`) after any migration, don't just wait for the watcher.
- **JWT role claims are stale after a role change**: `PATCH /users/me/role` doesn't reissue a token — the old JWT's `role` claim still says the previous role until the client calls `POST /auth/token/refresh` (which re-reads role from the DB). Relevant whenever testing a role transition (e.g. ASPIRANT → MENTOR) via curl: refresh before calling role-gated endpoints or you'll get a 403 that looks like a bug but isn't.

## Design reference

Figma file (Medconnect design): `https://www.figma.com/design/OObflKZgWgc2UaRZiMsPMP/Medconnect--TO-DEV`. Reviewed screen-by-screen against the built app twice — an early pass, then a full re-pull via the Figma MCP (`get_metadata` on the `SIGNUP, LOGIN, VERIFICATION, PINSETUP` / `Mentor` / `Aspirant` sections) that surfaced the structured onboarding wizards. The mobile UI is functional but styled with placeholder theme tokens, not the real design system. Non-visual gaps (imply schema/backend/flow work, not just restyling) are listed in "What's NOT built yet" above. Noise to ignore when reviewing this file: several frames ("Total Active Chits", "Branches", "Aadhaar, PAN", a "Prized" filter tab) are leftover fintech-template placeholder content, not real Uniscope features.

## Conventions to follow

- Explicit allowlist response projections (`toXResponse()` functions) for every entity — never return a raw Prisma row, to avoid leaking fields like `phoneHash`/`realNameEncrypted` as the schema evolves.
- 404 (not 403) for resources a user isn't a party to, everywhere — avoids leaking resource existence to unrelated users.
- Idempotency keys on every ledger write (`applyLedgerEntry`), derived from a stable source (hold id, report id, Razorpay payment id) — retries must never double-apply.
- Every new module gets tested against the **real** running backend + real Supabase DB (not just `tsc` passing) before being considered done — create throwaway test users/sessions via a `node -e` script with the Prisma client, `curl` the actual endpoints, verify DB state, then clean up the test data.
- **Never write "done"/"live"/"complete" — in CLAUDE.md, a commit message, or to the user — for an external action (a deploy, a dashboard/API write, a production config change) without re-reading that action's own result first.** A tool call that appears to have been issued is not the same as one confirmed to have succeeded — it can be blocked, partially applied, or silently no-op. This project has shipped at least one real instance of this: MSG91 was written up as "live in production" when the underlying Railway variable write had actually been blocked by a permission check, never re-verified before the claim was made. The fix is mechanical, not a matter of trying harder: after any external write, make one more call that reads the state back (list-variables, get-status, a fresh `curl` against the real endpoint, whatever fits) and only then write the status claim — cite what that read-back actually showed, don't infer success from the write call's own response alone.
- **For any question about live infrastructure (what host, what config, what's actually deployed) — query the live platform first, never infer it from a committed config file.** A file like `render.yaml` sitting in the repo does not mean that platform is in use; it can be stale from an earlier migration (exactly what happened — production had already moved to Railway while `render.yaml` sat untouched claiming Render). If a tool exists to ask the real platform (Railway's MCP tools in this project), use it before trusting anything committed to git about infrastructure.
- **Touch only the files a task actually requires — no incidental/exploratory edits riding along.** Before staging anything, run `git status`/`git diff --stat` and account for every file in it: either the current task's own logic required changing it, or it's a pre-existing uncommitted change from before this task that's been individually reviewed (read the diff, understand what it does) and knowingly included — never swept in just because it happened to be sitting there. A repo-wide sweep (grep-and-fix-everywhere, "clean up every mention of X") only happens when the user explicitly asked for that scope, as with the Render removal — a task scoped to "fix the OTP length bug" touches the OTP provider and its DTO, not every file that happens to mention OTP.
- **A change follows this order before it's considered done, every time:** (1) scope — name the specific files this task needs, nothing more; (2) implement; (3) compile/lint (`tsc`, `dart analyze`, whatever the language uses); (4) if it's new/changed backend behavior, live-test it against the real backend + real DB (see the module-testing rule above); (5) if it's an external write (deploy, dashboard, API), read the result back before claiming success (see the rule above); (6) re-check `git status`/`git diff` against the scope from step 1 before staging; (7) update the relevant section(s) of **this file** (CLAUDE.md) so it reflects the new reality — see the rule immediately below for why this is a real step, not an afterthought; (8) commit (code + the CLAUDE.md update together, one commit — not a separate later cleanup pass); (9) push. Skipping straight from "wrote the code" to "committed" is how an unreviewed stale file, an unconfirmed external write, or a stale CLAUDE.md claim ends up shipped.
- **Keep this file current — every session, every push, no exceptions.** CLAUDE.md is the one thing every session (and every remote/parallel worker, and every automated task runner) reads first, so it going stale is worse than it not existing: a session that trusts a stale claim here (e.g. "Firebase isn't configured," "MSG91 not yet flipped," a placeholder stream code) will waste real time re-discovering what another session already verified, or — worse — repeat work already done, or tell the user something is still pending when it's actually shipped. So: whenever a task actually lands (a real fix, a status genuinely changing from pending to done, a decision superseding an earlier one, a new admin/mobile/backend feature landing from *any* session working in this repo), update the relevant section(s) of this file in the **same commit/push** that lands the change — step 7 above, not a separate later cleanup pass that may never happen. This applies with no exceptions to every session and every remote/automated task runner working in this repo, not just interactive ones, and to work landing from concurrent sessions you didn't personally do — if you notice an undocumented merged commit while doing something else, document it on the spot rather than leaving it for whoever next needs the current picture. A stale claim caught while doing unrelated work is always worth fixing immediately (this has happened repeatedly across this project's history — OTP, Firebase, unique IDs, the mentor verification badge, the admin panel's payouts/pagination/user-detail screens, and the Discover pagination-cap + college-state-data fixes were all found undocumented mid-conversation and corrected here, sometimes by a different session than the one that shipped them), never left for someone else to re-discover.
