# Copy to .env.local and fill in. Next.js loads .env.local automatically.

# Backend base URL the browser calls directly (public POST /enrollments/*
# and public GET /universities) — same convention as admin/.env.local.
NEXT_PUBLIC_API_URL=http://localhost:3001/api/v1
